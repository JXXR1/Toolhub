TOOL = {
    "slug": "svg-optimizer",
    "category": "media",
    "icon": "🪶",
    "tags": ["svg", "optimizer", "minify", "compress", "icon", "vector"],
    "i18n": {
        "en": {
            "name": "SVG Optimizer",
            "tagline": "Minify SVG files in your browser. Strips comments, useless whitespace, default attributes, and rounds long decimals. Files never leave your device.",
            "description": "Free SVG optimizer that runs entirely in your browser. Paste or upload an SVG, get a minified version with sensible defaults: comments removed, whitespace collapsed, redundant attributes dropped, coordinates rounded to 2 decimals. Shows before/after size.",
        },
        "de": {
            "name": "SVG-Optimierer",
            "tagline": "SVG-Dateien im Browser minifizieren. Entfernt Kommentare, überflüssige Leerzeichen, Standard-Attribute und rundet lange Dezimalzahlen. Datei verlässt dein Gerät nie.",
            "description": "Kostenloser SVG-Optimierer, der komplett im Browser läuft. SVG einfügen oder hochladen und eine minifizierte Version mit sinnvollen Voreinstellungen erhalten: Kommentare entfernt, Leerzeichen reduziert, redundante Attribute weg, Koordinaten auf 2 Dezimalstellen gerundet. Zeigt Vorher/Nachher-Größe.",
        },
        "es": {
            "name": "Optimizador SVG",
            "tagline": "Minifica archivos SVG en tu navegador. Elimina comentarios, espacios inútiles, atributos por defecto y redondea decimales largos. El archivo no sale de tu dispositivo.",
            "description": "Optimizador SVG gratuito que se ejecuta enteramente en tu navegador. Pega o sube un SVG y obtén una versión minificada con valores por defecto sensatos: comentarios eliminados, espacios colapsados, atributos redundantes quitados, coordenadas redondeadas a 2 decimales. Muestra tamaño antes/después.",
        },
        "fr": {
            "name": "Optimiseur SVG",
            "tagline": "Minifiez vos fichiers SVG dans le navigateur. Supprime commentaires, espaces inutiles, attributs par défaut et arrondit les décimales longues. Le fichier ne quitte jamais votre appareil.",
            "description": "Optimiseur SVG gratuit qui tourne entièrement dans votre navigateur. Collez ou téléchargez un SVG et récupérez une version minifiée avec des valeurs par défaut raisonnables : commentaires supprimés, espaces réduits, attributs redondants retirés, coordonnées arrondies à 2 décimales. Affiche la taille avant/après.",
        },
        "it": {
            "name": "Ottimizzatore SVG",
            "tagline": "Minifica file SVG direttamente nel browser. Rimuove commenti, spazi inutili, attributi predefiniti e arrotonda i decimali lunghi. I file non lasciano mai il dispositivo.",
            "description": "Ottimizzatore SVG gratuito che gira interamente nel browser. Incolla o carica un SVG e ottieni una versione minificata con default sensati: commenti rimossi, spazi compressi, attributi ridondanti tolti, coordinate arrotondate a 2 decimali. Mostra dimensione prima/dopo.",
        },
        "pt": {
            "name": "Otimizador de SVG",
            "tagline": "Minifica arquivos SVG no seu browser. Tira comentários, espaços inúteis, atributos padrão e arredonda decimais longos. O arquivo nunca sai do seu dispositivo.",
            "description": "Otimizador SVG grátis que roda inteiro no seu browser. Cole ou envie um SVG e receba uma versão minificada com defaults razoáveis: comentários removidos, espaços colapsados, atributos redundantes descartados, coordenadas arredondadas a 2 casas decimais. Mostra tamanho antes/depois.",
        },
        "pl": {
            "name": "Optymalizator SVG",
            "tagline": "Minifikuj pliki SVG w przeglądarce. Usuwa komentarze, niepotrzebne spacje, domyślne atrybuty i zaokrągla długie liczby dziesiętne. Pliki nigdy nie opuszczają urządzenia.",
            "description": "Darmowy optymalizator SVG działający w całości w przeglądarce. Wklej albo wgraj SVG i dostań zminifikowaną wersję z sensownymi domyślnymi: komentarze usunięte, białe znaki zwinięte, zbędne atrybuty wyrzucone, współrzędne zaokrąglone do 2 miejsc po przecinku. Pokazuje rozmiar przed/po.",
        },
        "ja": {
            "name": "SVG オプティマイザー",
            "tagline": "SVG ファイルをブラウザだけで圧縮。コメント・無駄な空白・デフォルト属性を削除し、長い小数を丸めます。ファイルは端末から出ません。",
            "description": "ブラウザ内だけで動く無料 SVG オプティマイザー。SVG を貼り付けるかアップロードすると、コメント削除・空白圧縮・冗長な属性除去・座標の小数 2 桁丸めといった妥当なデフォルトでミニファイされたバージョンを返します。前後のサイズも表示します。",
        },
        "nl": {
            "name": "SVG-optimizer",
            "tagline": "Minify SVG-bestanden direct in je browser. Verwijdert commentaar, nutteloze whitespace, default attributen en rondt lange decimalen af. Bestand verlaat je apparaat nooit.",
            "description": "Gratis SVG-optimizer die volledig in je browser draait. Plak of upload een SVG en krijg een geminificeerde versie met zinnige defaults: commentaar weg, whitespace ingeklapt, redundante attributen verwijderd, coördinaten afgerond op 2 decimalen. Toont before/after-grootte.",
        },
        "tr": {
            "name": "SVG optimize edici",
            "tagline": "SVG dosyalarını doğrudan tarayıcında küçült. Yorumları, gereksiz boşlukları, varsayılan öznitelikleri siler ve uzun ondalıkları yuvarlar. Dosya cihazından ayrılmaz.",
            "description": "Tamamen tarayıcında çalışan ücretsiz SVG optimize edici. Bir SVG yapıştır veya yükle, makul varsayılanlarla küçültülmüş bir sürüm al: yorumlar kaldırıldı, boşluklar daraltıldı, gereksiz öznitelikler silindi, koordinatlar 2 ondalığa yuvarlandı. Önce/sonra boyutunu gösterir.",
        },
        "id": {
            "name": "Pengoptimal SVG",
            "tagline": "Minifikasi file SVG langsung di browser-mu. Membuang komentar, whitespace tak guna, atribut default, dan membulatkan desimal panjang. File tidak pernah meninggalkan perangkatmu.",
            "description": "Pengoptimal SVG gratis yang berjalan sepenuhnya di browser-mu. Tempel atau unggah SVG, dapatkan versi yang sudah diminifikasi dengan default masuk akal: komentar dihilangkan, whitespace dipadatkan, atribut redundan dibuang, koordinat dibulatkan ke 2 desimal. Menunjukkan ukuran sebelum/sesudah.",
        },
        "vi": {
            "name": "Tối ưu hóa SVG",
            "tagline": "Nén file SVG ngay trong trình duyệt. Loại bỏ chú thích, khoảng trắng vô dụng, thuộc tính mặc định và làm tròn số thập phân dài. File không bao giờ rời thiết bị của bạn.",
            "description": "Trình tối ưu hóa SVG miễn phí chạy hoàn toàn trong trình duyệt. Dán hoặc tải lên một SVG, nhận phiên bản đã nén với các mặc định hợp lý: bỏ chú thích, gộp khoảng trắng, loại bỏ thuộc tính dư thừa, làm tròn tọa độ đến 2 chữ số thập phân. Hiển thị kích thước trước/sau.",
        },
        "hi": {
            "name": "SVG Optimizer",
            "tagline": "SVG files को सीधे अपने browser में minify करें। Comments, बेकार whitespace, default attributes हटाता है और लंबे decimals को round करता है। Files कभी आपके device से बाहर नहीं जातीं।",
            "description": "मुफ़्त SVG optimizer जो पूरी तरह आपके browser में चलता है। SVG paste या upload करें, sensible defaults के साथ एक minified version पाएँ: comments हटाए गए, whitespace collapsed, redundant attributes dropped, coordinates 2 decimals तक rounded। Before/after size दिखाता है।",
        },
        "sk": {
            "name": "Optimalizátor SVG",
            "tagline": "Minifikuj SVG súbory priamo v prehliadači. Odstraňuje komentáre, zbytočné medzery, predvolené atribúty a zaokrúhľuje dlhé desatinné čísla. Súbory nikdy neopustia tvoje zariadenie.",
            "description": "Bezplatný SVG optimalizátor, ktorý beží úplne v prehliadači. Vlož alebo nahraj SVG a dostaneš minifikovanú verziu s rozumnými predvoľbami: komentáre preč, biele znaky stlačené, redundantné atribúty odstránené, súradnice zaokrúhlené na 2 desatinné miesta. Zobrazí veľkosť pred/po.",
        },
        "cs": {
            "name": "Optimalizátor SVG",
            "tagline": "Minifikuj SVG soubory přímo v prohlížeči. Odstraňuje komentáře, zbytečné mezery, výchozí atributy a zaokrouhluje dlouhá desetinná čísla. Soubory nikdy neopustí tvé zařízení.",
            "description": "Zdarma SVG optimalizátor, který běží zcela v prohlížeči. Vlož nebo nahraj SVG a dostaneš minifikovanou verzi s rozumnými výchozími hodnotami: komentáře pryč, bílé znaky sloučené, redundantní atributy odstraněné, souřadnice zaokrouhlené na 2 desetinná místa. Zobrazí velikost před/po.",
        },
    },
    "body": """
<div class="tool-card">
  <label>SVG input</label>
  <textarea id="svgo-in" spellcheck="false" oninput="svgoRun()" placeholder="Paste SVG markup here, or pick a file below…" style="width:100%;min-height:160px;font-family:var(--font-mono);font-size:0.85rem"></textarea>
  <input type="file" id="svgo-file" accept=".svg,image/svg+xml" onchange="svgoLoadFile()" style="margin-top:0.5rem;padding:0.5rem;background:var(--code-bg);border:1px dashed var(--border);border-radius:6px;width:100%">
  <div style="font-size:0.78rem;color:var(--text-muted);margin-top:0.4rem">🔒 Files never leave your browser — no upload, no server, no tracking.</div>
</div>
<div class="tool-card">
  <label>{LBL_OPTIONS}</label>
  <div style="display:flex;flex-wrap:wrap;gap:0.9rem;font-size:0.92rem">
    <label style="font-weight:normal"><input type="checkbox" id="svgo-comments" checked onchange="svgoRun()"> Remove comments</label>
    <label style="font-weight:normal"><input type="checkbox" id="svgo-ws" checked onchange="svgoRun()"> Collapse whitespace</label>
    <label style="font-weight:normal"><input type="checkbox" id="svgo-defaults" checked onchange="svgoRun()"> Drop default attrs</label>
    <label style="font-weight:normal"><input type="checkbox" id="svgo-round" checked onchange="svgoRun()"> Round coords (2 dp)</label>
  </div>
</div>
<div class="tool-card">
  <label>Optimized SVG</label>
  <div class="output-row">
    <pre class="output" id="svgo-out" style="max-height:240px;overflow:auto;font-size:0.82rem">{LBL_NO_INPUT}</pre>
    <button class="copy-btn secondary" onclick="copyOutput('svgo-out', this)">{LBL_COPY}</button>
  </div>
  <div class="meta" id="svgo-stats" style="margin-top:0.5rem"></div>
  <div class="button-row" style="margin-top:0.6rem"><button id="svgo-dl-btn" onclick="svgoDownload()" disabled>{LBL_DOWNLOAD}</button></div>
</div>
""",
    "script": """
<script>
function svgoLoadFile(){
  const f = document.getElementById('svgo-file').files[0];
  if (!f) return;
  const r = new FileReader();
  r.onload = () => { document.getElementById('svgo-in').value = r.result; svgoRun(); };
  r.readAsText(f);
}
function svgoRun(){
  let s = document.getElementById('svgo-in').value;
  const out = document.getElementById('svgo-out');
  const stats = document.getElementById('svgo-stats');
  const btn = document.getElementById('svgo-dl-btn');
  if (!s.trim()){ out.textContent = '{LBL_NO_INPUT}'; stats.textContent = ''; btn.disabled = true; return; }
  const opts = {
    comments: document.getElementById('svgo-comments').checked,
    whitespace: document.getElementById('svgo-ws').checked,
    defaults: document.getElementById('svgo-defaults').checked,
    roundCoords: document.getElementById('svgo-round').checked,
  };
  const before = new Blob([s]).size;
  if (opts.comments) s = s.replace(/<!--[\\s\\S]*?-->/g, '');
  if (opts.whitespace) s = s.replace(/>\\s+</g, '><').replace(/\\s{2,}/g, ' ').trim();
  if (opts.defaults){
    s = s.replace(/\\s+(stroke|fill-rule|fill-opacity|stroke-opacity|stroke-linejoin|stroke-linecap)="(?:none|nonzero|1|miter|butt)"/g, '');
    s = s.replace(/\\s+xmlns:xlink="http:\\/\\/www\\.w3\\.org\\/1999\\/xlink"(?![\\s\\S]*xlink:)/g, '');
  }
  if (opts.roundCoords){
    s = s.replace(/(\\d+\\.\\d{3,})/g, (m) => {
      const n = parseFloat(m).toFixed(2);
      return n.replace(/\\.?0+$/, '') || '0';
    });
  }
  const after = new Blob([s]).size;
  out.textContent = s;
  const saved = before > 0 ? ((1 - after/before) * 100).toFixed(1) : '0.0';
  stats.textContent = before + ' → ' + after + ' bytes  (saved ' + saved + '%)';
  btn.disabled = false;
  window.__svgoLast = s;
}
function svgoDownload(){
  const s = window.__svgoLast || '';
  if (!s) return;
  const blob = new Blob([s], {type: 'image/svg+xml'});
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = 'optimized.svg';
  a.click();
  setTimeout(() => URL.revokeObjectURL(url), 1000);
}
</script>
""",
    "help": {
        "en": """
<h2>What is this for?</h2>
<p>This tool runs a tiny set of hand-rolled regex passes over an SVG to shrink its byte size: it strips XML comments, collapses runs of whitespace between tags, drops a handful of attributes that already match their default values, and rounds long fractional coordinates to 2 decimal places. Each pass is a checkbox you can disable. Everything happens in your browser — no upload, no server.</p>

<h3>When to use it</h3>
<ul>
  <li>Cleaning up an SVG icon exported by Figma, Illustrator, or Sketch before checking it into a repo.</li>
  <li>Trimming the &lt;svg&gt; payload before inlining it into HTML or CSS (where every byte ships with every page).</li>
  <li>Stripping the chatty XML comments that some design tools embed in their export ("Generator: Adobe Illustrator…").</li>
  <li>Quick sanity-pass before encoding an SVG as a data: URL.</li>
</ul>

<h3>Common gotchas</h3>
<ul>
  <li><strong>Not as aggressive as SVGO.</strong> The real <a href="https://github.com/svg/svgo">SVGO</a> library can do path optimisation, viewBox cleanup, and dozens of other passes. This tool deliberately does only the safe, obvious passes — to keep it 0-dependency and 100% in-browser.</li>
  <li><strong>Default-attr dropping is conservative.</strong> It only removes a short list of well-known defaults (<code>stroke="none"</code>, <code>fill-rule="nonzero"</code>, etc.). If your SVG depends on those being explicit (e.g. for CSS-targeted styling), uncheck the option.</li>
  <li><strong>Coordinate rounding is lossy.</strong> Rounding to 2 dp is safe for icons and logos; for high-precision diagrams or charts, you may want to disable it.</li>
  <li><strong>Whitespace collapse can break <code>&lt;text&gt;</code>.</strong> SVG <code>&lt;text&gt;</code> elements that rely on multiple spaces will lose them. For icon SVGs (no text), this is never an issue.</li>
</ul>
""",
        "de": """
<h2>Wofür ist das?</h2>
<p>Dieses Tool führt eine kleine Reihe handgeschriebener Regex-Durchläufe über ein SVG aus, um die Bytegröße zu verringern: XML-Kommentare entfernen, Whitespace zwischen Tags zusammenfassen, ein paar Attribute löschen, die ohnehin Default-Werte tragen, und lange Dezimal-Koordinaten auf 2 Stellen runden. Jeder Durchlauf ist als Checkbox abschaltbar. Alles passiert im Browser — kein Upload, kein Server.</p>

<h3>Wann verwenden</h3>
<ul>
  <li>Aufräumen eines aus Figma, Illustrator oder Sketch exportierten SVG-Icons, bevor es ins Repo geht.</li>
  <li>SVG-Payload trimmen, bevor es inline in HTML oder CSS landet (wo jedes Byte mit jeder Seite ausgeliefert wird).</li>
  <li>XML-Kommentare entfernen, die Design-Tools einbauen ("Generator: Adobe Illustrator…").</li>
  <li>Schneller Sanity-Check, bevor das SVG als data:-URL kodiert wird.</li>
</ul>

<h3>Häufige Stolpersteine</h3>
<ul>
  <li><strong>Nicht so aggressiv wie SVGO.</strong> Die echte <a href="https://github.com/svg/svgo">SVGO</a>-Bibliothek kann Pfad-Optimierung, viewBox-Cleanup und Dutzende anderer Passes. Dieses Tool macht bewusst nur die sicheren, offensichtlichen Schritte — um null Abhängigkeiten und 100 % Browser-only zu bleiben.</li>
  <li><strong>Default-Attr-Entfernen ist konservativ.</strong> Es entfernt nur eine kurze Liste bekannter Defaults (<code>stroke="none"</code>, <code>fill-rule="nonzero"</code>, etc.). Wenn dein SVG diese explizit braucht (z. B. für CSS-Styling), schalte die Option ab.</li>
  <li><strong>Koordinaten-Rundung ist verlustbehaftet.</strong> 2 Dezimalstellen sind für Icons und Logos sicher; für Diagramme mit hoher Präzision lieber abschalten.</li>
  <li><strong>Whitespace-Zusammenfassen kann <code>&lt;text&gt;</code> brechen.</strong> SVG-<code>&lt;text&gt;</code>-Elemente mit mehrfachen Leerzeichen verlieren sie. Bei Icon-SVGs (kein Text) ist das nie ein Problem.</li>
</ul>
""",
        "es": """
<h2>¿Para qué sirve?</h2>
<p>Esta herramienta aplica una pequeña serie de pases regex hechos a mano sobre un SVG para reducir su tamaño en bytes: quita comentarios XML, colapsa espacios entre etiquetas, elimina un puñado de atributos que ya coinciden con sus valores por defecto, y redondea coordenadas decimales largas a 2 cifras. Cada pase es una casilla que puedes desactivar. Todo ocurre en tu navegador — sin subida, sin servidor.</p>

<h3>Cuándo usarlo</h3>
<ul>
  <li>Limpiar un icono SVG exportado de Figma, Illustrator o Sketch antes de meterlo en un repo.</li>
  <li>Recortar el payload &lt;svg&gt; antes de embeberlo inline en HTML o CSS (donde cada byte viaja con cada página).</li>
  <li>Quitar los comentarios XML que algunas herramientas dejan en su export ("Generator: Adobe Illustrator…").</li>
  <li>Pase rápido antes de codificar un SVG como data: URL.</li>
</ul>

<h3>Errores comunes</h3>
<ul>
  <li><strong>No tan agresivo como SVGO.</strong> La librería <a href="https://github.com/svg/svgo">SVGO</a> real puede optimizar paths, limpiar viewBox y docenas de pases más. Esta herramienta deliberadamente solo hace los pases seguros y obvios — para mantenerla con 0 dependencias y 100 % en navegador.</li>
  <li><strong>El descarte de atributos por defecto es conservador.</strong> Solo retira una lista corta (<code>stroke="none"</code>, <code>fill-rule="nonzero"</code>, etc.). Si tu SVG depende de tenerlos explícitos, desmarca la opción.</li>
  <li><strong>El redondeo de coordenadas es con pérdida.</strong> Redondear a 2 decimales es seguro para iconos y logos; para diagramas de alta precisión quizás quieras desactivarlo.</li>
  <li><strong>Colapsar espacios puede romper <code>&lt;text&gt;</code>.</strong> Los elementos <code>&lt;text&gt;</code> que dependen de varios espacios los perderán. Para SVG de iconos (sin texto), nunca es problema.</li>
</ul>
""",
        "fr": """
<h2>À quoi ça sert ?</h2>
<p>Cet outil exécute une petite série de passes regex faites à la main sur un SVG pour réduire sa taille en octets : suppression des commentaires XML, regroupement des blancs entre balises, retrait d'une poignée d'attributs qui valent déjà leur valeur par défaut, et arrondi des coordonnées décimales longues à 2 chiffres. Chaque passe est une case que vous pouvez désactiver. Tout se passe dans le navigateur — aucun upload, aucun serveur.</p>

<h3>Quand l'utiliser</h3>
<ul>
  <li>Nettoyer une icône SVG exportée depuis Figma, Illustrator ou Sketch avant de l'archiver dans un dépôt.</li>
  <li>Réduire le payload &lt;svg&gt; avant de l'inliner dans HTML ou CSS (où chaque octet voyage avec chaque page).</li>
  <li>Retirer les commentaires XML que certains outils de design laissent ("Generator: Adobe Illustrator…").</li>
  <li>Passe rapide avant d'encoder un SVG en data: URL.</li>
</ul>

<h3>Pièges courants</h3>
<ul>
  <li><strong>Pas aussi agressif que SVGO.</strong> La vraie librairie <a href="https://github.com/svg/svgo">SVGO</a> peut optimiser les paths, nettoyer le viewBox et faire des dizaines d'autres passes. Cet outil ne fait délibérément que les passes sûres et évidentes — pour rester sans dépendance et 100 % navigateur.</li>
  <li><strong>La suppression d'attributs par défaut est conservatrice.</strong> Elle ne retire qu'une courte liste connue (<code>stroke="none"</code>, <code>fill-rule="nonzero"</code>, etc.). Si votre SVG en dépend explicitement, décochez l'option.</li>
  <li><strong>L'arrondi des coordonnées est avec perte.</strong> Arrondir à 2 décimales est sûr pour icônes et logos ; pour des diagrammes haute précision, pensez à le désactiver.</li>
  <li><strong>Le regroupement des blancs peut casser <code>&lt;text&gt;</code>.</strong> Les éléments <code>&lt;text&gt;</code> qui dépendent de plusieurs espaces les perdront. Pour des SVG d'icônes (sans texte), aucun souci.</li>
</ul>
""",
        "it": """
<h2>A cosa serve?</h2>
<p>Questo strumento esegue una piccola serie di passate regex fatte a mano su un SVG per ridurne la dimensione in byte: rimuove i commenti XML, collassa gli spazi tra i tag, toglie qualche attributo che già combacia con il valore di default e arrotonda le coordinate decimali lunghe a 2 cifre. Ogni passata è una checkbox disattivabile. Tutto avviene nel browser — niente upload, niente server.</p>

<h3>Quando usarlo</h3>
<ul>
  <li>Ripulire un'icona SVG esportata da Figma, Illustrator o Sketch prima di committarla in un repo.</li>
  <li>Ridurre il payload &lt;svg&gt; prima di inlinarlo in HTML o CSS (dove ogni byte viaggia con ogni pagina).</li>
  <li>Togliere i commenti XML che alcuni tool di design lasciano nell'export ("Generator: Adobe Illustrator…").</li>
  <li>Passata veloce prima di codificare un SVG come data: URL.</li>
</ul>

<h3>Errori comuni</h3>
<ul>
  <li><strong>Non aggressivo come SVGO.</strong> La vera libreria <a href="https://github.com/svg/svgo">SVGO</a> fa ottimizzazione di path, pulizia del viewBox e decine di altre passate. Questo strumento fa solo le passate sicure e ovvie — per restare a zero dipendenze e 100 % nel browser.</li>
  <li><strong>Il drop degli attributi di default è conservativo.</strong> Rimuove solo una lista breve (<code>stroke="none"</code>, <code>fill-rule="nonzero"</code>, ecc.). Se il tuo SVG si basa sull'averli espliciti, deseleziona l'opzione.</li>
  <li><strong>L'arrotondamento delle coordinate è lossy.</strong> 2 decimali sono sicuri per icone e loghi; per diagrammi ad alta precisione meglio disattivare.</li>
  <li><strong>Il collasso degli spazi può rompere <code>&lt;text&gt;</code>.</strong> Gli elementi <code>&lt;text&gt;</code> che dipendono da spazi multipli li perderanno. Per SVG di icone (senza testo) non è mai un problema.</li>
</ul>
""",
        "pt": """
<h2>Pra que serve?</h2>
<p>Esta ferramenta roda uma pequena série de passes regex feitos à mão sobre um SVG pra encolher o tamanho em bytes: tira comentários XML, colapsa whitespace entre tags, descarta um punhado de atributos que já batem com seu valor padrão e arredonda coordenadas decimais longas pra 2 casas. Cada passe é um checkbox que você pode desligar. Tudo acontece no seu browser — sem upload, sem servidor.</p>

<h3>Quando usar</h3>
<ul>
  <li>Limpar um ícone SVG exportado do Figma, Illustrator ou Sketch antes de commitar no repo.</li>
  <li>Reduzir o payload &lt;svg&gt; antes de inliná-lo em HTML ou CSS (onde cada byte viaja com cada página).</li>
  <li>Remover os comentários XML que algumas ferramentas de design deixam ("Generator: Adobe Illustrator…").</li>
  <li>Passada rápida antes de codificar um SVG como data: URL.</li>
</ul>

<h3>Cuidados comuns</h3>
<ul>
  <li><strong>Não é tão agressivo quanto o SVGO.</strong> A biblioteca <a href="https://github.com/svg/svgo">SVGO</a> de verdade faz otimização de path, limpeza de viewBox e dezenas de outros passes. Esta ferramenta faz só os passes seguros e óbvios — pra ficar com 0 dependências e 100 % no browser.</li>
  <li><strong>O drop de atributos padrão é conservador.</strong> Só remove uma lista curta (<code>stroke="none"</code>, <code>fill-rule="nonzero"</code>, etc.). Se seu SVG depende disso explícito, desmarque a opção.</li>
  <li><strong>Arredondar coordenada é com perda.</strong> 2 casas decimais é seguro pra ícones e logos; pra diagramas de alta precisão talvez queira desativar.</li>
  <li><strong>Colapsar whitespace pode quebrar <code>&lt;text&gt;</code>.</strong> Elementos <code>&lt;text&gt;</code> que dependem de espaços múltiplos os perderão. Pra SVG de ícone (sem texto), nunca é problema.</li>
</ul>
""",
        "pl": """
<h2>Do czego to służy?</h2>
<p>To narzędzie wykonuje krótki ciąg ręcznie napisanych przebiegów regex na SVG, żeby zmniejszyć rozmiar w bajtach: usuwa komentarze XML, zwija białe znaki między tagami, wyrzuca kilka atrybutów, które już pokrywają się z wartościami domyślnymi, i zaokrągla długie współrzędne dziesiętne do 2 miejsc. Każdy przebieg to checkbox, który można wyłączyć. Wszystko dzieje się w przeglądarce — bez uploadu, bez serwera.</p>

<h3>Kiedy tego użyć</h3>
<ul>
  <li>Sprzątanie ikony SVG wyeksportowanej z Figmy, Illustratora albo Sketcha, zanim trafi do repo.</li>
  <li>Trymowanie payloadu &lt;svg&gt; przed inline'owaniem go w HTML albo CSS (gdzie każdy bajt podróżuje z każdą stroną).</li>
  <li>Wycięcie komentarzy XML, które niektóre narzędzia projektowe zostawiają w eksporcie ("Generator: Adobe Illustrator…").</li>
  <li>Szybki sanity-pass przed zakodowaniem SVG jako data: URL.</li>
</ul>

<h3>Częste pułapki</h3>
<ul>
  <li><strong>Nie tak agresywne jak SVGO.</strong> Prawdziwa biblioteka <a href="https://github.com/svg/svgo">SVGO</a> potrafi optymalizować ścieżki, czyścić viewBox i robić dziesiątki innych przebiegów. To narzędzie celowo robi tylko bezpieczne, oczywiste przebiegi — żeby zostać bez zależności i w 100 % w przeglądarce.</li>
  <li><strong>Usuwanie domyślnych atrybutów jest konserwatywne.</strong> Wyrzuca tylko krótką listę znanych domyślnych (<code>stroke="none"</code>, <code>fill-rule="nonzero"</code>, itd.). Jeśli twój SVG ich potrzebuje jawnie, odznacz opcję.</li>
  <li><strong>Zaokrąglanie współrzędnych jest stratne.</strong> 2 miejsca po przecinku są bezpieczne dla ikon i logo; dla diagramów wysokiej precyzji lepiej wyłącz.</li>
  <li><strong>Zwijanie spacji może popsuć <code>&lt;text&gt;</code>.</strong> Elementy <code>&lt;text&gt;</code>, które opierają się na wielu spacjach, je stracą. Dla SVG-ikon (bez tekstu) to nigdy nie problem.</li>
</ul>
""",
        "ja": """
<h2>用途</h2>
<p>このツールは手作りの正規表現パスを少しだけ SVG にかけ、バイトサイズを縮めます。XML コメントを削除し、タグ間の連続空白を圧縮し、デフォルト値と同じ属性を取り除き、長い小数座標を 2 桁に丸めます。各パスはチェックボックスで個別に無効化できます。すべてブラウザ内で完結 — アップロードもサーバー処理もありません。</p>

<h3>使うべきタイミング</h3>
<ul>
  <li>Figma・Illustrator・Sketch から書き出した SVG アイコンをリポジトリにコミットする前のクリーンアップ。</li>
  <li>HTML や CSS にインラインで埋め込む前の &lt;svg&gt; ペイロードのスリム化（インラインだと毎ページ全バイトが配信されます）。</li>
  <li>デザインツールがエクスポートに残しがちな XML コメント（"Generator: Adobe Illustrator…"）の除去。</li>
  <li>SVG を data: URL にエンコードする前の軽い前処理。</li>
</ul>

<h3>よくある注意点</h3>
<ul>
  <li><strong>本家 SVGO ほど積極的ではありません。</strong> 本物の <a href="https://github.com/svg/svgo">SVGO</a> はパス最適化・viewBox 整理など数十のパスを行います。本ツールは依存ゼロ・100 % ブラウザ内に留めるため、意図的に安全で自明なパスだけを実装しています。</li>
  <li><strong>デフォルト属性の削除は保守的です。</strong> よく知られたデフォルト（<code>stroke="none"</code>、<code>fill-rule="nonzero"</code> など）の短いリストだけが対象です。これらを CSS スタイリング等で明示的に必要としている場合はチェックを外してください。</li>
  <li><strong>座標の丸めは情報を失います。</strong> 小数 2 桁はアイコンやロゴでは安全ですが、高精度のダイアグラムやチャートでは無効にすることを検討してください。</li>
  <li><strong>空白の圧縮は <code>&lt;text&gt;</code> を壊し得ます。</strong> 複数スペースに依存する <code>&lt;text&gt;</code> 要素は壊れます。テキストのないアイコン SVG では問題になりません。</li>
</ul>
""",
        "nl": """
<h2>Waarvoor is dit?</h2>
<p>Deze tool draait een klein setje handgeschreven regex-passes over een SVG om de bytegrootte te krimpen: XML-commentaar strippen, runs van whitespace tussen tags inklappen, een handvol attributen droppen die toch al hun default-waarde hebben, en lange decimale coördinaten afronden op 2 cijfers. Elke pass is een checkbox die je kunt uitzetten. Alles gebeurt in je browser — geen upload, geen server.</p>

<h3>Wanneer gebruiken</h3>
<ul>
  <li>Opschonen van een SVG-icoon dat uit Figma, Illustrator of Sketch is geëxporteerd voor je het in een repo zet.</li>
  <li>Trimmen van de &lt;svg&gt;-payload voordat je hem inline in HTML of CSS zet (waar elke byte met elke pagina meegaat).</li>
  <li>Wegwerken van de XML-commentaarregels die sommige design-tools in hun export laten ("Generator: Adobe Illustrator…").</li>
  <li>Snelle sanity-pass voor je een SVG als data: URL encodeert.</li>
</ul>

<h3>Veelvoorkomende valkuilen</h3>
<ul>
  <li><strong>Niet zo agressief als SVGO.</strong> De echte <a href="https://github.com/svg/svgo">SVGO</a>-bibliotheek doet path-optimalisatie, viewBox-cleanup en tientallen andere passes. Deze tool doet bewust alleen de veilige, voor-de-hand-liggende passes — om 0-dependencies en 100 % in-browser te blijven.</li>
  <li><strong>Default-attr droppen is conservatief.</strong> Verwijdert alleen een korte lijst bekende defaults (<code>stroke="none"</code>, <code>fill-rule="nonzero"</code>, etc.). Als je SVG ze expliciet nodig heeft, vink de optie uit.</li>
  <li><strong>Coördinaten afronden is lossy.</strong> 2 decimalen is veilig voor icons en logos; voor diagrammen met hoge precisie kun je 'm beter uitzetten.</li>
  <li><strong>Whitespace inklappen kan <code>&lt;text&gt;</code> breken.</strong> <code>&lt;text&gt;</code>-elementen die op meerdere spaties leunen verliezen die. Voor icon-SVG's (geen tekst) is dit nooit een probleem.</li>
</ul>
""",
        "tr": """
<h2>Bu ne işe yarar?</h2>
<p>Bu araç bir SVG üzerine elle yazılmış küçük bir regex pass seti çalıştırarak byte boyutunu küçültür: XML yorumlarını siler, etiketler arası boşlukları daraltır, zaten varsayılan değerini taşıyan birkaç özniteliği atar ve uzun ondalıklı koordinatları 2 basamağa yuvarlar. Her pass devre dışı bırakılabilen bir kutudur. Her şey tarayıcında olur — upload yok, sunucu yok.</p>

<h3>Ne zaman kullanılır</h3>
<ul>
  <li>Figma, Illustrator veya Sketch'ten dışa aktarılmış bir SVG ikonunu repoya koymadan önce temizlemek.</li>
  <li>HTML veya CSS'e inline almadan önce &lt;svg&gt; payload'ını kırpmak (her byte her sayfa ile beraber gönderiliyor).</li>
  <li>Tasarım araçlarının dışa aktarıma bıraktığı XML yorumlarını silmek ("Generator: Adobe Illustrator…").</li>
  <li>Bir SVG'yi data: URL olarak kodlamadan önce hızlı bir sanity pass.</li>
</ul>

<h3>Sık yapılan hatalar</h3>
<ul>
  <li><strong>SVGO kadar saldırgan değil.</strong> Gerçek <a href="https://github.com/svg/svgo">SVGO</a> kütüphanesi path optimizasyonu, viewBox temizliği ve onlarca başka pass yapabilir. Bu araç sıfır bağımlılık ve %100 tarayıcı içi kalmak için kasten yalnızca güvenli, açık passleri yapar.</li>
  <li><strong>Varsayılan öznitelik atımı muhafazakârdır.</strong> Yalnızca kısa bilinen varsayılanlar listesini (<code>stroke="none"</code>, <code>fill-rule="nonzero"</code>, vb.) kaldırır. SVG'in bunların açıkça olmasına dayanıyorsa seçeneği kapat.</li>
  <li><strong>Koordinat yuvarlaması kayıplıdır.</strong> 2 ondalık ikonlar ve logolar için güvenlidir; yüksek hassasiyetli diyagramlarda kapatmayı düşün.</li>
  <li><strong>Boşluk daraltma <code>&lt;text&gt;</code>'i bozabilir.</strong> Çoklu boşluklara dayanan <code>&lt;text&gt;</code> elemanları onları kaybeder. İkon SVG'lerinde (metin yok) bu hiç sorun değil.</li>
</ul>
""",
        "id": """
<h2>Untuk apa ini?</h2>
<p>Tool ini menjalankan sekumpulan kecil pass regex hand-rolled di atas sebuah SVG untuk mengecilkan ukurannya dalam byte: menghapus komentar XML, memadatkan rangkaian whitespace antar tag, membuang segelintir atribut yang nilainya sudah sama dengan default, dan membulatkan koordinat desimal panjang ke 2 angka. Tiap pass adalah checkbox yang bisa kamu matikan. Semua terjadi di browser-mu — tanpa upload, tanpa server.</p>

<h3>Kapan digunakan</h3>
<ul>
  <li>Membersihkan ikon SVG yang diekspor dari Figma, Illustrator, atau Sketch sebelum di-commit ke repo.</li>
  <li>Memangkas payload &lt;svg&gt; sebelum di-inline ke HTML atau CSS (di mana setiap byte ikut dengan setiap halaman).</li>
  <li>Membuang komentar XML yang ditinggalkan beberapa tool desain di ekspor ("Generator: Adobe Illustrator…").</li>
  <li>Sanity-pass cepat sebelum mengencode SVG sebagai data: URL.</li>
</ul>

<h3>Kesalahan umum</h3>
<ul>
  <li><strong>Tidak seagresif SVGO.</strong> Pustaka <a href="https://github.com/svg/svgo">SVGO</a> asli bisa melakukan optimasi path, pembersihan viewBox, dan puluhan pass lain. Tool ini sengaja hanya melakukan pass yang aman dan jelas — supaya tetap 0-dependency dan 100 % di browser.</li>
  <li><strong>Pembuangan default-attr konservatif.</strong> Hanya menghilangkan daftar pendek default yang umum (<code>stroke="none"</code>, <code>fill-rule="nonzero"</code>, dll.). Kalau SVG-mu butuh itu eksplisit, matikan opsi.</li>
  <li><strong>Pembulatan koordinat itu lossy.</strong> 2 desimal aman untuk ikon dan logo; untuk diagram presisi tinggi, mungkin kamu mau menonaktifkannya.</li>
  <li><strong>Memadatkan whitespace bisa merusak <code>&lt;text&gt;</code>.</strong> Elemen <code>&lt;text&gt;</code> yang mengandalkan banyak spasi akan kehilangan itu. Untuk SVG ikon (tanpa teks), ini tidak pernah jadi masalah.</li>
</ul>
""",
        "vi": """
<h2>Công cụ này để làm gì?</h2>
<p>Công cụ này chạy một nhóm nhỏ các pass regex viết tay trên một SVG để giảm dung lượng byte: loại bỏ comment XML, gộp khoảng trắng giữa các thẻ, bỏ một vài thuộc tính vốn đã trùng giá trị mặc định, và làm tròn các tọa độ thập phân dài về 2 chữ số. Mỗi pass là một checkbox bạn có thể tắt. Mọi thứ diễn ra trong trình duyệt — không upload, không server.</p>

<h3>Khi nào nên dùng</h3>
<ul>
  <li>Dọn dẹp một icon SVG xuất từ Figma, Illustrator hoặc Sketch trước khi commit vào repo.</li>
  <li>Cắt gọn payload &lt;svg&gt; trước khi inline nó vào HTML hoặc CSS (nơi mỗi byte đi cùng mỗi trang).</li>
  <li>Bỏ những comment XML mà một số công cụ thiết kế để lại trong file export ("Generator: Adobe Illustrator…").</li>
  <li>Một pass sanity-check nhanh trước khi mã hóa SVG thành data: URL.</li>
</ul>

<h3>Lưu ý thường gặp</h3>
<ul>
  <li><strong>Không quyết liệt bằng SVGO.</strong> Thư viện <a href="https://github.com/svg/svgo">SVGO</a> thật có thể tối ưu path, dọn viewBox và hàng chục pass khác. Công cụ này cố tình chỉ làm những pass an toàn, hiển nhiên — để giữ 0 dependency và 100 % trong trình duyệt.</li>
  <li><strong>Việc bỏ thuộc tính mặc định là bảo thủ.</strong> Chỉ xóa một danh sách ngắn các mặc định thường gặp (<code>stroke="none"</code>, <code>fill-rule="nonzero"</code>, v.v.). Nếu SVG của bạn cần chúng tường minh, bỏ chọn tùy chọn.</li>
  <li><strong>Làm tròn tọa độ là có mất mát.</strong> 2 chữ số thập phân an toàn cho icon và logo; với biểu đồ độ chính xác cao thì nên tắt.</li>
  <li><strong>Gộp whitespace có thể làm hỏng <code>&lt;text&gt;</code>.</strong> Các phần tử <code>&lt;text&gt;</code> dựa vào nhiều khoảng trắng sẽ mất chúng. Với SVG icon (không có chữ), điều này không bao giờ là vấn đề.</li>
</ul>
""",
        "hi": """
<h2>यह किसके लिए है?</h2>
<p>यह tool एक SVG पर हाथ से लिखे regex passes का छोटा set चलाता है ताकि उसका byte size कम हो: XML comments हटाता है, tags के बीच whitespace को collapse करता है, कुछ ऐसे attributes drop करता है जो पहले से अपनी default value रखते हैं, और लंबे decimal coordinates को 2 places पर round करता है। हर pass एक checkbox है जिसे आप disable कर सकते हैं। सब कुछ आपके browser में होता है — कोई upload नहीं, कोई server नहीं।</p>

<h3>कब इस्तेमाल करें</h3>
<ul>
  <li>Figma, Illustrator, या Sketch से export किया SVG icon repo में जाने से पहले साफ़ करना।</li>
  <li>HTML या CSS में inline करने से पहले &lt;svg&gt; payload trim करना (जहां हर byte हर page के साथ ship होता है)।</li>
  <li>कुछ design tools जो XML comments अपने export में छोड़ देते हैं ("Generator: Adobe Illustrator…") उन्हें हटाना।</li>
  <li>SVG को data: URL के रूप में encode करने से पहले एक quick sanity-pass।</li>
</ul>

<h3>आम गलतियाँ</h3>
<ul>
  <li><strong>SVGO जितना aggressive नहीं।</strong> असली <a href="https://github.com/svg/svgo">SVGO</a> library path optimization, viewBox cleanup, और दर्जनों अन्य passes कर सकती है। यह tool जानबूझकर सिर्फ safe, obvious passes करता है — 0-dependency और 100 % in-browser बने रहने के लिए।</li>
  <li><strong>Default-attr dropping conservative है।</strong> यह सिर्फ़ well-known defaults की एक छोटी list को हटाता है (<code>stroke="none"</code>, <code>fill-rule="nonzero"</code>, आदि)। अगर आपका SVG उन पर explicit रूप से depend करता है, तो option uncheck करें।</li>
  <li><strong>Coordinate rounding lossy है।</strong> 2 dp तक round करना icons और logos के लिए safe है; high-precision diagrams या charts के लिए शायद आप इसे disable करना चाहें।</li>
  <li><strong>Whitespace collapse <code>&lt;text&gt;</code> को तोड़ सकता है।</strong> SVG <code>&lt;text&gt;</code> elements जो multiple spaces पर निर्भर हैं वे उन्हें खो देंगे। Icon SVGs (no text) के लिए, यह कभी issue नहीं है।</li>
</ul>
""",
        "sk": """
<h2>Načo to slúži?</h2>
<p>Tento nástroj prejde SVG malou sériou ručne písaných regex passov, aby zmenšil veľkosť v bajtoch: odstráni XML komentáre, stlačí biele znaky medzi tagmi, odstráni hŕstku atribútov, ktoré sa zhodujú s ich predvolenými hodnotami, a zaokrúhli dlhé desatinné súradnice na 2 miesta. Každý pass je checkbox, ktorý môžeš vypnúť. Všetko sa deje v prehliadači — žiadny upload, žiadny server.</p>

<h3>Kedy to použiť</h3>
<ul>
  <li>Vyčistenie SVG ikony exportovanej z Figma, Illustrator alebo Sketch pred jej commitnutím do repa.</li>
  <li>Orezanie &lt;svg&gt; payloadu pred inline vložením do HTML alebo CSS (kde každý bajt putuje s každou stránkou).</li>
  <li>Odstránenie XML komentárov, ktoré niektoré dizajnové nástroje nechávajú v exporte ("Generator: Adobe Illustrator…").</li>
  <li>Rýchly sanity pass pred zakódovaním SVG ako data: URL.</li>
</ul>

<h3>Časté chyby</h3>
<ul>
  <li><strong>Nie taký agresívny ako SVGO.</strong> Skutočná knižnica <a href="https://github.com/svg/svgo">SVGO</a> vie optimalizovať cesty, čistiť viewBox a robiť desiatky ďalších passov. Tento nástroj robí zámerne iba bezpečné a očividné passy — aby zostal bez závislostí a 100 % v prehliadači.</li>
  <li><strong>Odstránenie predvolených atribútov je konzervatívne.</strong> Odstraňuje len krátky zoznam známych predvolených (<code>stroke="none"</code>, <code>fill-rule="nonzero"</code>, atď.). Ak ich tvoje SVG potrebuje explicitne, odškrtni možnosť.</li>
  <li><strong>Zaokrúhľovanie súradníc je stratové.</strong> 2 desatinné miesta sú bezpečné pre ikony a logá; pre vysoko presné diagramy možno zvážiš vypnutie.</li>
  <li><strong>Stlačenie bielych znakov môže rozbiť <code>&lt;text&gt;</code>.</strong> SVG <code>&lt;text&gt;</code> elementy, ktoré sa spoliehajú na viaceré medzery, ich stratia. Pri ikonových SVG (bez textu) to nikdy nie je problém.</li>
</ul>
""",
        "cs": """
<h2>K čemu to slouží?</h2>
<p>Tento nástroj projede SVG malou sérií ručně psaných regex passů, aby zmenšil velikost v bajtech: odstraní XML komentáře, stlačí bílé znaky mezi tagy, odstraní hrstku atributů, které se shodují s jejich výchozími hodnotami, a zaokrouhlí dlouhé desetinné souřadnice na 2 místa. Každý pass je checkbox, který můžeš vypnout. Vše se děje v prohlížeči — žádný upload, žádný server.</p>

<h3>Kdy to použít</h3>
<ul>
  <li>Vyčištění SVG ikony exportované z Figma, Illustrator nebo Sketch před jejím commitnutím do repa.</li>
  <li>Oříznutí &lt;svg&gt; payloadu před inline vložením do HTML nebo CSS (kde každý bajt putuje s každou stránkou).</li>
  <li>Odstranění XML komentářů, které některé designové nástroje nechávají v exportu ("Generator: Adobe Illustrator…").</li>
  <li>Rychlý sanity pass před zakódováním SVG jako data: URL.</li>
</ul>

<h3>Časté chyby</h3>
<ul>
  <li><strong>Není tak agresivní jako SVGO.</strong> Skutečná knihovna <a href="https://github.com/svg/svgo">SVGO</a> umí optimalizovat cesty, čistit viewBox a dělat desítky dalších passů. Tento nástroj dělá záměrně jen bezpečné a očividné passy — aby zůstal bez závislostí a 100 % v prohlížeči.</li>
  <li><strong>Odstranění výchozích atributů je konzervativní.</strong> Odstraňuje jen krátký seznam známých výchozích (<code>stroke="none"</code>, <code>fill-rule="nonzero"</code>, atd.). Pokud je tvé SVG potřebuje explicitně, odškrtni možnost.</li>
  <li><strong>Zaokrouhlení souřadnic je ztrátové.</strong> 2 desetinná místa jsou bezpečná pro ikony a loga; pro diagramy s vysokou přesností možná zvážíš vypnutí.</li>
  <li><strong>Stlačení bílých znaků může rozbít <code>&lt;text&gt;</code>.</strong> SVG <code>&lt;text&gt;</code> elementy, které spoléhají na vícero mezer, je ztratí. U ikonových SVG (bez textu) to nikdy není problém.</li>
</ul>
""",
    },
    "related": ["image-compress", "image-convert", "css-minifier"],
    "howto": {"flow": "transform", "action": "minify", "noun": {"en": "SVG", "de": "SVG", "es": "SVG", "fr": "SVG", "it": "SVG", "pt": "SVG", "pl": "SVG", "ja": "SVG", "nl": "SVG", "tr": "SVG", "id": "SVG", "vi": "SVG", "hi": "SVG", "sk": "SVG", "cs": "SVG"}},
}
