TOOL = {
    "slug": "regex-llm-output",
    "category": "developer",
    "icon": "🪝",
    "tags": ["regex", "llm", "extract", "parse", "output", "ai", "structured", "code-fence", "markdown"],
    "i18n": {
        "en": {
            "name": "Regex LLM Output Extractor",
            "tagline": "Extract structured data (JSON, code blocks, key-value pairs) from messy LLM responses. Pick from common templates or write your own regex. Live preview.",
            "description": "Free regex builder for LLM output extraction. Common cases pre-built: JSON inside ```json fences, plain JSON object, numbered list, key: value pairs, classification labels. Paste an LLM response, pick a template (or write your own regex), see matched groups live. The tool nobody admits they need but everyone writes a fresh one of for every project.",
        },
        "de": {
            "name": "Regex LLM-Output-Extraktor",
            "tagline": "Extrahiere strukturierte Daten (JSON, Code-Blöcke, Key-Value-Paare) aus unsauberen LLM-Antworten. Wähle aus gängigen Vorlagen oder schreib eigene Regex. Live-Vorschau.",
            "description": "Kostenloser Regex-Builder zum Extrahieren aus LLM-Ausgaben. Gängige Fälle vorgebaut: JSON in ```json-Fences, blankes JSON-Objekt, nummerierte Liste, key: value-Paare, Klassifikations-Labels. LLM-Antwort einfügen, Template wählen (oder eigene Regex schreiben), Capture-Gruppen live sehen. Das Tool, das niemand zugeben will zu brauchen, aber alle für jedes Projekt neu schreiben.",
        },
        "es": {
            "name": "Extractor Regex de Salida LLM",
            "tagline": "Extrae datos estructurados (JSON, bloques de código, pares clave-valor) de respuestas desordenadas de LLM. Elige de plantillas comunes o escribe tu propio regex. Vista previa en vivo.",
            "description": "Constructor regex gratuito para extraer salida de LLM. Casos comunes prefabricados: JSON dentro de ```json fences, objeto JSON plano, lista numerada, pares key: value, etiquetas de clasificación. Pega una respuesta de LLM, elige una plantilla (o escribe tu propio regex), ve los grupos capturados en vivo. La herramienta que nadie admite necesitar pero todos reescriben para cada proyecto.",
        },
        "fr": {
            "name": "Extracteur Regex de Sortie LLM",
            "tagline": "Extrayez des données structurées (JSON, blocs de code, paires clé-valeur) de réponses LLM brouillonnes. Choisissez parmi des modèles courants ou écrivez votre propre regex. Aperçu en direct.",
            "description": "Constructeur de regex gratuit pour l'extraction de sorties LLM. Cas courants pré-construits : JSON dans des ```json fences, objet JSON brut, liste numérotée, paires key: value, étiquettes de classification. Collez une réponse LLM, choisissez un modèle (ou écrivez votre propre regex), voyez les groupes capturés en direct. L'outil que personne n'admet avoir besoin mais que tout le monde réécrit pour chaque projet.",
        },
        "it": {
            "name": "Estrattore Regex Output LLM",
            "tagline": "Estrai dati strutturati (JSON, blocchi di codice, coppie chiave-valore) da risposte LLM disordinate. Scegli tra template comuni o scrivi la tua regex. Anteprima live.",
            "description": "Costruttore regex gratuito per estrarre output di LLM. Casi comuni pre-costruiti: JSON dentro fence ```json, oggetto JSON nudo, lista numerata, coppie key: value, etichette di classificazione. Incolla una risposta LLM, scegli un template (o scrivi la tua regex), vedi i gruppi catturati live. Lo strumento che nessuno ammette di avere bisogno ma tutti riscrivono per ogni progetto.",
        },
        "pt": {
            "name": "Extrator Regex de Saída LLM",
            "tagline": "Extraia dados estruturados (JSON, blocos de código, pares chave-valor) de respostas confusas de LLM. Escolha entre templates comuns ou escreva sua própria regex. Preview ao vivo.",
            "description": "Builder regex grátis pra extrair saída de LLM. Casos comuns prontos: JSON dentro de fences ```json, objeto JSON puro, lista numerada, pares key: value, labels de classificação. Cole uma resposta de LLM, escolha um template (ou escreva sua própria regex), veja os grupos capturados ao vivo. A ferramenta que ninguém admite precisar mas todo mundo reescreve pra cada projeto.",
        },
        "pl": {
            "name": "Ekstraktor Regex Output LLM",
            "tagline": "Wyciągaj strukturalne dane (JSON, code blocki, pary klucz-wartość) z bałaganu w odpowiedziach LLM. Wybierz z popularnych szablonów albo napisz własną regex. Podgląd na żywo.",
            "description": "Darmowy builder regex do ekstrakcji z outputu LLM. Popularne przypadki gotowe: JSON wewnątrz fence'ów ```json, goły obiekt JSON, lista numerowana, pary key: value, etykiety klasyfikacji. Wklej odpowiedź LLM, wybierz szablon (albo napisz własną regex), zobacz przechwycone grupy na żywo. Narzędzie, którego nikt nie przyzna, że potrzebuje, ale wszyscy pisanie je od nowa do każdego projektu.",
        },
        "ja": {
            "name": "正規表現 LLM 出力抽出ツール",
            "tagline": "雑な LLM 応答から構造化データ（JSON・コードブロック・key-value）を抽出。よくあるテンプレートから選ぶか、独自の正規表現を書く。ライブプレビュー付き。",
            "description": "LLM 出力抽出用の無料正規表現ビルダー。よくあるケースを内蔵：```json フェンス内 JSON、素の JSON オブジェクト、番号付きリスト、key: value 形式、分類ラベル。LLM の応答を貼り付け、テンプレートを選ぶ（または自分で正規表現を書く）と、キャプチャグループがライブで見えます。誰も必要だとは認めないけれど、プロジェクト毎に皆が書き直しているあのツール。",
        },
        "nl": {
            "name": "Regex LLM Output Extractor",
            "tagline": "Extraheer gestructureerde data (JSON, codeblokken, key-value-paren) uit rommelige LLM-responses. Kies uit gangbare templates of schrijf je eigen regex. Live preview.",
            "description": "Gratis regex-builder voor LLM-output-extractie. Gangbare gevallen voorgebouwd: JSON binnen ```json-fences, kaal JSON-object, genummerde lijst, key: value-paren, classificatielabels. Plak een LLM-response, kies een template (of schrijf je eigen regex), zie de gecapturede groepen live. De tool die niemand wil toegeven nodig te hebben maar die iedereen voor elk project opnieuw schrijft.",
        },
        "tr": {
            "name": "Regex LLM Çıktı Çıkarıcı",
            "tagline": "Dağınık LLM yanıtlarından yapılandırılmış veriyi (JSON, kod blokları, anahtar-değer çiftleri) çıkar. Yaygın şablonlardan seç veya kendi regex'ini yaz. Canlı önizleme.",
            "description": "LLM çıktısı çıkarımı için ücretsiz regex builder. Yaygın durumlar hazır: ```json fence içinde JSON, çıplak JSON nesnesi, numaralı liste, key: value çiftleri, sınıflandırma etiketleri. Bir LLM yanıtını yapıştır, bir şablon seç (veya kendi regex'ini yaz), eşleşen grupları canlı gör. Kimsenin ihtiyacı olduğunu kabul etmediği ama herkesin her proje için yeniden yazdığı araç.",
        },
        "id": {
            "name": "Ekstraktor Regex Output LLM",
            "tagline": "Ekstrak data terstruktur (JSON, code block, pasangan key-value) dari response LLM yang berantakan. Pilih dari template umum atau tulis regex sendiri. Preview live.",
            "description": "Builder regex gratis untuk ekstraksi output LLM. Kasus umum sudah jadi: JSON di dalam ```json fence, objek JSON polos, list bernomor, pasangan key: value, label klasifikasi. Tempel response LLM, pilih template (atau tulis regex sendiri), lihat grup yang tertangkap secara live. Tool yang tidak ada yang ngaku butuh tapi semua orang tulis ulang untuk tiap project.",
        },
        "vi": {
            "name": "Regex LLM Output Extractor",
            "tagline": "Trích xuất dữ liệu có cấu trúc (JSON, code block, cặp key-value) từ response LLM lộn xộn. Chọn từ template phổ biến hoặc viết regex của riêng bạn. Preview trực tiếp.",
            "description": "Trình tạo regex miễn phí để trích xuất output LLM. Các trường hợp phổ biến đã làm sẵn: JSON trong ```json fence, đối tượng JSON trần, danh sách đánh số, cặp key: value, nhãn phân loại. Dán response LLM, chọn template (hoặc viết regex của bạn), xem các nhóm bắt được trực tiếp. Công cụ không ai thừa nhận cần nhưng mọi người đều viết lại cho mỗi dự án.",
        },
        "hi": {
            "name": "Regex LLM Output Extractor",
            "tagline": "गन्दे LLM responses से structured data (JSON, code blocks, key-value pairs) निकालें। आम templates में से चुनें या अपना regex लिखें। Live preview।",
            "description": "मुफ़्त regex builder LLM output extraction के लिए। आम cases pre-built हैं: ```json fences के अंदर JSON, plain JSON object, numbered list, key: value pairs, classification labels। LLM response paste करें, template चुनें (या अपना regex लिखें), matched groups live देखें। वह tool जिसकी ज़रूरत कोई admit नहीं करता पर हर project के लिए हर कोई नया लिखता है।",
        },
        "sk": {
            "name": "Regex extraktor LLM výstupu",
            "tagline": "Vytiahni štruktúrované dáta (JSON, kódové bloky, páry kľúč-hodnota) z neprehľadných LLM odpovedí. Vyber zo šablón alebo napíš vlastný regex. Živý náhľad.",
            "description": "Bezplatný regex builder na extrakciu výstupu z LLM. Bežné prípady pripravené: JSON v ```json fence, holý JSON objekt, číslovaný zoznam, páry key: value, klasifikačné labely. Vlož LLM odpoveď, vyber šablónu (alebo si napíš vlastný regex), pozri zachytené skupiny naživo. Nástroj, ktorý nikto neprizná, že potrebuje, ale každý ho píše odznova pre každý projekt.",
        },
        "cs": {
            "name": "Regex extraktor LLM výstupu",
            "tagline": "Vytáhni strukturovaná data (JSON, kódové bloky, páry klíč-hodnota) z nepřehledných LLM odpovědí. Vyber ze šablon nebo napiš vlastní regex. Živý náhled.",
            "description": "Zdarma regex builder na extrakci výstupu z LLM. Běžné případy připravené: JSON v ```json fence, holý JSON objekt, číslovaný seznam, páry key: value, klasifikační labely. Vlož LLM odpověď, vyber šablonu (nebo si napiš vlastní regex), podívej se na zachycené skupiny živě. Nástroj, který nikdo nepřizná, že potřebuje, ale každý ho píše znovu pro každý projekt.",
        },
    },
    "body": """
<div class="tool-card">
  <label for="rlo-input">LLM response</label>
  <textarea id="rlo-input" oninput="rloRun()" spellcheck="false" placeholder="Paste an LLM response here…">Sure, here is the result you asked for:

```json
{
  "title": "Toolhub",
  "category": "developer-tools",
  "languages": 15,
  "tools": 76
}
```

Let me know if you need anything else!</textarea>
</div>
<div class="tool-card">
  <div class="row-2col">
    <div>
      <label for="rlo-template">Template</label>
      <select id="rlo-template" onchange="rloTemplate()">
        <option value="json-fence">JSON in ```json code fence</option>
        <option value="json-any-fence">JSON in any code fence</option>
        <option value="yaml-fence">YAML in ```yaml code fence</option>
        <option value="any-fence">Any fenced code block</option>
        <option value="plain-json">Plain JSON object (no fence)</option>
        <option value="numbered-list">Numbered list items</option>
        <option value="kv-pairs">key: value pairs</option>
        <option value="classification">Single classification label</option>
        <option value="custom">Custom regex</option>
      </select>
    </div>
    <div>
      <label for="rlo-flags">Flags</label>
      <input type="text" id="rlo-flags" value="gs" oninput="rloRun()" placeholder="gimsuy">
    </div>
  </div>
  <label style="margin-top:0.6rem">Regex</label>
  <input type="text" id="rlo-pattern" oninput="rloRun()" style="font-family:ui-monospace,monospace">
  <div class="meta" id="rlo-template-hint" style="margin-top:0.4rem"></div>
</div>
<div class="tool-card">
  <label>{LBL_RESULT}</label>
  <pre class="output" id="rlo-highlight" style="white-space:pre-wrap;word-break:break-word;max-height:300px;overflow:auto"></pre>
  <div class="meta" id="rlo-meta" style="margin-top:0.4rem"></div>
</div>
<div class="tool-card">
  <label>Captured groups</label>
  <pre class="output" id="rlo-groups" style="font-size:0.85rem;white-space:pre-wrap;word-break:break-word;max-height:240px;overflow:auto">{LBL_NO_INPUT}</pre>
  <div class="button-row" style="margin-top:0.5rem">
    <button class="secondary" onclick="rloCopy('regex')">Copy regex</button>
    <button class="secondary" onclick="rloCopy('match')">Copy first match</button>
    <button class="secondary" onclick="rloCopy('json')">Copy parsed JSON</button>
    <button class="secondary" onclick="rloCopy('js')">Copy as JS code</button>
  </div>
  <div class="meta" id="rlo-copied" style="margin-top:0.4rem"></div>
</div>
""",
    "script": """
<style>
.rlo-mark{background:rgba(99,102,241,0.18);border:1px solid var(--accent);border-radius:3px;padding:0 2px}
.rlo-mark-num{color:var(--accent);font-weight:600;font-size:0.78em;vertical-align:super}
</style>
<script>
const RLO_TEMPLATES = {
  'json-fence': {
    pattern: '```json\\\\s*([\\\\s\\\\S]*?)```',
    flags: 'g',
    hint: 'Captures group 1 = the JSON content inside ```json … ``` fences. Use the gs flags so . matches newlines (or use the [\\\\s\\\\S] form, which always does).'
  },
  'json-any-fence': {
    pattern: '```(?:json|JSON)?\\\\s*(\\\\{[\\\\s\\\\S]*?\\\\}|\\\\[[\\\\s\\\\S]*?\\\\])\\\\s*```',
    flags: 'g',
    hint: 'Captures the first { … } or [ … ] block inside a fenced code section, language tag optional.'
  },
  'yaml-fence': {
    pattern: '```ya?ml\\\\s*([\\\\s\\\\S]*?)```',
    flags: 'gi',
    hint: 'Captures group 1 = YAML inside ```yaml or ```yml fences.'
  },
  'any-fence': {
    pattern: '```([a-zA-Z0-9_-]*)\\\\n([\\\\s\\\\S]*?)```',
    flags: 'g',
    hint: 'Captures group 1 = language tag (may be empty), group 2 = code body.'
  },
  'plain-json': {
    pattern: '\\\\{[\\\\s\\\\S]*\\\\}',
    flags: '',
    hint: 'Greedy match for a top-level JSON object — first { to last }. Beware: this fails if the response contains multiple JSON objects or stray braces. For multiple, switch to a non-greedy form and use the g flag.'
  },
  'numbered-list': {
    pattern: '^\\\\s*\\\\d+[\\\\.)]\\\\s*(.+)$',
    flags: 'gm',
    hint: 'Captures group 1 = the text of each "1. foo", "2) bar" item. Use the m flag so ^/$ match per line.'
  },
  'kv-pairs': {
    pattern: '^\\\\s*([A-Za-z][\\\\w\\\\s-]*)\\\\s*:\\\\s*(.+?)\\\\s*$',
    flags: 'gm',
    hint: 'Captures group 1 = key, group 2 = value, line by line. Skips lines that don\\'t start with a letter.'
  },
  'classification': {
    pattern: '\\\\b(POSITIVE|NEGATIVE|NEUTRAL|YES|NO|TRUE|FALSE|SAFE|UNSAFE)\\\\b',
    flags: 'i',
    hint: 'Common single-label classifier outputs. Edit the alternation to fit your labels.'
  },
  'custom': {
    pattern: '',
    flags: 'g',
    hint: 'Write your own pattern. The default flag g enables findAll; add i for case-insensitive, m for per-line anchors, s for dotAll.'
  }
};
let RLO_LAST = {first: '', all: [], json: null, regex: ''};
function rloEsc(s){ return String(s ?? '').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;'); }
function rloTemplate(){
  const key = document.getElementById('rlo-template').value;
  const t = RLO_TEMPLATES[key];
  if (t){
    document.getElementById('rlo-pattern').value = t.pattern;
    document.getElementById('rlo-flags').value = t.flags;
    document.getElementById('rlo-template-hint').textContent = t.hint;
  }
  rloRun();
}
function rloRun(){
  const pat = document.getElementById('rlo-pattern').value;
  const flags = document.getElementById('rlo-flags').value;
  const text = document.getElementById('rlo-input').value;
  const hi = document.getElementById('rlo-highlight');
  const meta = document.getElementById('rlo-meta');
  const groupsEl = document.getElementById('rlo-groups');
  if (!pat){
    hi.innerHTML = rloEsc(text);
    meta.textContent = '(enter a regex above)';
    groupsEl.textContent = '';
    RLO_LAST = {first: '', all: [], json: null, regex: ''};
    return;
  }
  let regex;
  try { regex = new RegExp(pat, flags); }
  catch(e){
    hi.textContent = '✗ ' + e.message;
    hi.classList.add('error');
    meta.textContent = '';
    groupsEl.textContent = '';
    return;
  }
  hi.classList.remove('error');
  const matches = [];
  if (regex.global){
    let m;
    let safety = 0;
    while ((m = regex.exec(text)) !== null){
      matches.push({index: m.index, full: m[0], groups: m.slice(1)});
      if (m[0] === '') regex.lastIndex++;
      if (++safety > 5000) break;
    }
  } else {
    const m = regex.exec(text);
    if (m) matches.push({index: m.index, full: m[0], groups: m.slice(1)});
  }
  // Highlight in text
  let out = '';
  let last = 0;
  matches.forEach((m, i) => {
    out += rloEsc(text.slice(last, m.index));
    out += '<span class="rlo-mark">' + rloEsc(m.full) + '<span class="rlo-mark-num">' + (i+1) + '</span></span>';
    last = m.index + m.full.length;
  });
  out += rloEsc(text.slice(last));
  hi.innerHTML = out || '<span style="color:var(--text-muted)">(no matches)</span>';
  meta.textContent = matches.length + ' match' + (matches.length === 1 ? '' : 'es');
  // Groups
  if (matches.length === 0){
    groupsEl.textContent = '(no matches)';
    RLO_LAST = {first: '', all: matches, json: null, regex: '/' + pat + '/' + flags};
  } else {
    const lines = matches.slice(0, 50).map((m, i) => {
      let s = '── Match ' + (i+1) + ' at index ' + m.index + ' ──\\n';
      s += 'full: ' + (m.full.length > 200 ? m.full.slice(0,200) + '…' : m.full);
      m.groups.forEach((g, j) => {
        const val = g == null ? '<undefined>' : (g.length > 200 ? g.slice(0,200) + '…' : g);
        s += '\\n  $' + (j+1) + ': ' + val;
      });
      return s;
    });
    if (matches.length > 50) lines.push('… (' + (matches.length - 50) + ' more)');
    groupsEl.textContent = lines.join('\\n\\n');
    // Try parse first capture group (or full match) as JSON
    let parsed = null;
    const first = matches[0];
    const tryStrings = [];
    for (let j = 0; j < first.groups.length; j++){
      if (first.groups[j]) tryStrings.push(first.groups[j]);
    }
    tryStrings.push(first.full);
    for (const s of tryStrings){
      try { parsed = JSON.parse(s.trim()); break; }
      catch(_) {}
    }
    RLO_LAST = {first: first.full, all: matches, json: parsed, regex: '/' + pat + '/' + flags};
    if (parsed !== null){
      groupsEl.textContent += '\\n\\n── Parsed JSON ──\\n' + JSON.stringify(parsed, null, 2);
    }
  }
}
function rloCopy(kind){
  let text = '';
  if (kind === 'regex'){ text = RLO_LAST.regex; }
  else if (kind === 'match'){ text = RLO_LAST.first; }
  else if (kind === 'json'){
    if (RLO_LAST.json === null){ rloMsg('Parsed JSON is empty.'); return; }
    text = JSON.stringify(RLO_LAST.json, null, 2);
  }
  else if (kind === 'js'){
    const pat = document.getElementById('rlo-pattern').value;
    const flags = document.getElementById('rlo-flags').value;
    text = 'const match = text.match(/' + pat + '/' + flags + ');';
  }
  if (!text){ rloMsg('Nothing to copy yet.'); return; }
  navigator.clipboard.writeText(text).then(() => rloMsg('✓ Copied (' + text.length + ' chars)'));
}
function rloMsg(s){
  const el = document.getElementById('rlo-copied');
  el.textContent = s;
  setTimeout(() => { if (el.textContent === s) el.textContent = ''; }, 2000);
}
document.addEventListener('DOMContentLoaded', rloTemplate);
</script>
""",
    "help": {
        "en": """
<h2>What is this for?</h2>
<p>You asked the LLM for JSON. It gave you JSON wrapped in <code>```json</code> fences with a "Sure, here you go!" preamble. Or it gave you "the answer is 42" when you wanted just 42. Or it gave you a numbered list when you asked for a comma-separated string. Welcome to the parsing problem nobody ships a real solution for. This tool keeps a small library of common extraction patterns plus a free-form regex editor, so you can iterate on the pattern with your actual response text right in the browser.</p>

<h3>When to use it</h3>
<ul>
  <li><strong>Designing an output parser for an agent pipeline.</strong> Throw a few real responses in, build a regex that handles all of them, paste it into your code.</li>
  <li><strong>Debugging a broken parser.</strong> Your prod pipeline started failing because the model added "Here's the JSON:" before the fence. See exactly where your regex stops matching.</li>
  <li><strong>Quick one-off extraction.</strong> You have ten LLM responses in a doc; need to extract the structured bit from each. Paste, match, copy, move on.</li>
</ul>

<h3>The templates</h3>
<ul>
  <li><strong>JSON in ```json code fence</strong> — the most common case. Captures group 1 = the body. Use the <code>g</code> flag if there might be multiple fences.</li>
  <li><strong>JSON in any code fence</strong> — same idea but the language tag is optional and we only capture if the body looks like <code>{ … }</code> or <code>[ … ]</code>.</li>
  <li><strong>YAML in code fence</strong> — same as JSON-fence but matches <code>```yaml</code> / <code>```yml</code>.</li>
  <li><strong>Any fenced code block</strong> — captures language tag + body. Use when you don't know what's inside.</li>
  <li><strong>Plain JSON object</strong> — greedy match from first <code>{</code> to last <code>}</code>. Brittle but works for "just JSON, no chrome" responses.</li>
  <li><strong>Numbered list items</strong> — <code>^\\s*\\d+[.)]\\s*(.+)$</code> with <code>gm</code>. Captures the text of each list item, no numbering.</li>
  <li><strong>key: value pairs</strong> — line-by-line. Captures key (group 1) and value (group 2). Stops at the first colon.</li>
  <li><strong>Single classification label</strong> — useful for sentiment / safety classifiers that should reply with one word.</li>
  <li><strong>Custom regex</strong> — clear the pattern and write your own.</li>
</ul>

<h3>The "parsed JSON" line</h3>
<p>If the first capture group (or, failing that, the full match) parses as JSON, the tool prints the parsed result below the groups. That tells you not just "did the regex match" but "did it match the part you wanted, in a JSON-decodable way." If parsing fails, that line stays blank.</p>

<h3>Common gotchas</h3>
<ul>
  <li><strong>JavaScript regex, not PCRE.</strong> No <code>\\K</code>, no recursive patterns. Lookbehind requires modern browsers (post-2018 — fine for this tool but be aware if you copy the pattern elsewhere).</li>
  <li><strong>The model wraps its JSON in commentary.</strong> Don't try to find <code>{</code> at start of string — find the fence or use a tolerant non-greedy capture.</li>
  <li><strong>Trailing commas in "JSON" output.</strong> Some models slip trailing commas in despite training. The regex match will work; <code>JSON.parse</code> will fail. Strip trailing commas before parsing.</li>
  <li><strong>Single-quoted "JSON".</strong> Same story — looks like JSON, isn't valid JSON, regex doesn't care, <code>JSON.parse</code> does.</li>
  <li><strong>Nested fences.</strong> If the model puts a markdown code-fence example inside its response, you can get a false-positive match on the inner fence. Test with realistic data.</li>
  <li><strong>Don't use regex for serious nested-bracket extraction.</strong> If the model returns an object containing an object containing an array of objects, write a proper JSON-aware extractor — or just ask for the fence form and parse the fence body.</li>
  <li><strong>Privacy.</strong> The text and pattern stay in the page. No upload, no API call.</li>
</ul>
""",
        "de": """
<h2>Wozu ist das gut?</h2>
<p>Du hast das LLM nach JSON gefragt. Es hat dir JSON gegeben, eingewickelt in <code>```json</code>-Fences mit „Hier bitte!"-Vorrede. Oder „die Antwort ist 42", wenn du nur 42 wolltest. Oder eine nummerierte Liste statt einer kommagetrennten Zeichenkette. Willkommen beim Parsing-Problem, das niemand richtig löst. Dieses Tool hat eine kleine Bibliothek typischer Extraktions-Patterns plus einen freien Regex-Editor, damit du das Muster gegen deinen echten Antworttext im Browser iterieren kannst.</p>

<h3>Wann nützlich</h3>
<ul>
  <li><strong>Output-Parser für eine Agent-Pipeline entwerfen.</strong> Ein paar echte Antworten reinwerfen, eine Regex bauen, die alle abdeckt, ab ins Codebase.</li>
  <li><strong>Defekten Parser debuggen.</strong> Deine Prod-Pipeline kippt, weil das Modell plötzlich „Hier das JSON:" vor den Fence schreibt. Sieh genau, wo deine Regex aussteigt.</li>
  <li><strong>Schnelle Einmal-Extraktion.</strong> Zehn LLM-Antworten in einem Doc; aus jeder den strukturierten Teil ziehen. Einfügen, matchen, kopieren, weiter.</li>
</ul>

<h3>Die Templates</h3>
<ul>
  <li><strong>JSON in ```json-Fence</strong> — der Standardfall. Gruppe 1 = der Inhalt. Bei mehreren Fences das <code>g</code>-Flag.</li>
  <li><strong>JSON in beliebigem Fence</strong> — wie oben, Sprach-Tag optional, Capture nur wenn Inhalt nach <code>{ … }</code> / <code>[ … ]</code> aussieht.</li>
  <li><strong>YAML in Fence</strong> — wie JSON-Fence, aber <code>```yaml</code> / <code>```yml</code>.</li>
  <li><strong>Beliebiger Fenced-Code-Block</strong> — captured Sprach-Tag + Inhalt. Wenn du nicht weißt, was drin ist.</li>
  <li><strong>Reines JSON-Objekt</strong> — gieriger Match vom ersten <code>{</code> bis zum letzten <code>}</code>. Sprödes Mittel, funktioniert für „nur JSON"-Antworten.</li>
  <li><strong>Nummerierte Listen-Items</strong> — <code>^\\s*\\d+[.)]\\s*(.+)$</code> mit <code>gm</code>. Text jedes Items ohne Nummer.</li>
  <li><strong>key: value-Paare</strong> — zeilenweise. Gruppe 1 = Key, Gruppe 2 = Wert. Stoppt am ersten Doppelpunkt.</li>
  <li><strong>Single Classification Label</strong> — nützlich für Sentiment- / Safety-Klassifikatoren, die mit einem Wort antworten sollen.</li>
  <li><strong>Custom Regex</strong> — Pattern leeren und selbst schreiben.</li>
</ul>

<h3>Die „Parsed JSON"-Zeile</h3>
<p>Wenn die erste Capture-Gruppe (oder andernfalls der gesamte Match) als JSON parst, zeigt das Tool das Ergebnis unter den Gruppen an. Das beantwortet nicht nur „hat die Regex gematcht" sondern „hat sie den richtigen Teil JSON-dekodierbar gematcht". Parst es nicht, bleibt die Zeile leer.</p>

<h3>Typische Stolperfallen</h3>
<ul>
  <li><strong>JavaScript-Regex, kein PCRE.</strong> Kein <code>\\K</code>, keine Rekursion. Lookbehind nur in modernen Browsern (seit 2018 — hier fein, woanders aufpassen).</li>
  <li><strong>Das Modell verpackt sein JSON in Kommentare.</strong> Nicht <code>{</code> am String-Anfang suchen — Fence finden oder toleranten non-greedy Capture.</li>
  <li><strong>Trailing Commas im „JSON".</strong> Manche Modelle schmuggeln Trailing Commas rein. Die Regex matched, <code>JSON.parse</code> nicht. Vor dem Parsen strippen.</li>
  <li><strong>Einfache Anführungszeichen im „JSON".</strong> Sieht aus wie JSON, ist keins. Regex egal, <code>JSON.parse</code> nicht.</li>
  <li><strong>Verschachtelte Fences.</strong> Wenn das Modell ein Markdown-Beispiel mit Fence innerhalb der Antwort liefert, gibt's Fehl-Treffer auf den inneren. Mit realistischen Daten testen.</li>
  <li><strong>Regex ist kein Tool für ernstes Bracket-Parsing.</strong> Bei tief verschachtelten Objekten: lieber JSON-bewussten Extraktor schreiben — oder Fence-Form anfragen und Inhalt parsen.</li>
  <li><strong>Privacy.</strong> Text und Pattern bleiben auf der Seite. Kein Upload, kein API-Call.</li>
</ul>
""",
        "es": """
<h2>¿Para qué sirve?</h2>
<p>Le pediste JSON al LLM. Te dio JSON envuelto en fences <code>```json</code> con un preámbulo "¡Claro, aquí tienes!". O te dio "la respuesta es 42" cuando solo querías 42. O te dio una lista numerada cuando pediste una string separada por comas. Bienvenido al problema de parsing que nadie resuelve de verdad. Esta herramienta lleva una librería pequeña de patterns comunes de extracción más un editor de regex libre, para que itere el pattern con tu texto real en el navegador.</p>

<h3>Cuándo usarla</h3>
<ul>
  <li><strong>Diseñar un parser de output para una pipeline de agente.</strong> Mete unas cuantas respuestas reales, construye una regex que las cubra todas, pégala en tu código.</li>
  <li><strong>Debug de un parser roto.</strong> Tu pipeline de prod empezó a fallar porque el modelo añadió "Aquí está el JSON:" antes del fence. Mira exactamente dónde tu regex deja de matchear.</li>
  <li><strong>Extracción puntual rápida.</strong> Tienes diez respuestas de LLM en un doc; necesitas sacar la parte estructurada de cada una. Pegar, matchear, copiar, seguir.</li>
</ul>

<h3>Las plantillas</h3>
<ul>
  <li><strong>JSON en ```json fence</strong> — el caso más común. Captura el grupo 1 = el cuerpo. Usa <code>g</code> si puede haber varios fences.</li>
  <li><strong>JSON en cualquier fence</strong> — misma idea pero el tag de lenguaje es opcional y solo capturamos si el cuerpo parece <code>{ … }</code> o <code>[ … ]</code>.</li>
  <li><strong>YAML en fence</strong> — igual que JSON-fence pero matchea <code>```yaml</code> / <code>```yml</code>.</li>
  <li><strong>Cualquier bloque fenced</strong> — captura tag de lenguaje + cuerpo. Úsalo cuando no sabes qué hay dentro.</li>
  <li><strong>Objeto JSON plano</strong> — match greedy del primer <code>{</code> al último <code>}</code>. Frágil pero funciona para respuestas de "solo JSON".</li>
  <li><strong>Items de lista numerada</strong> — <code>^\\s*\\d+[.)]\\s*(.+)$</code> con <code>gm</code>. Captura el texto de cada item, sin numeración.</li>
  <li><strong>Pares key: value</strong> — línea por línea. Grupo 1 = key, grupo 2 = value. Para en el primer dos puntos.</li>
  <li><strong>Etiqueta de clasificación única</strong> — útil para clasificadores de sentimiento / safety que deben responder con una palabra.</li>
  <li><strong>Regex custom</strong> — limpia el pattern y escribe el tuyo.</li>
</ul>

<h3>La línea "Parsed JSON"</h3>
<p>Si el primer grupo capturado (o, fallando eso, el match completo) parsea como JSON, la herramienta imprime el resultado parseado debajo de los grupos. Eso te dice no solo "¿matcheó la regex?" sino "¿matcheó la parte que querías, de forma JSON-decodable?". Si el parsing falla, esa línea queda en blanco.</p>

<h3>Errores comunes</h3>
<ul>
  <li><strong>Regex de JavaScript, no PCRE.</strong> Sin <code>\\K</code>, sin patterns recursivos. Lookbehind requiere navegadores modernos (post-2018 — bien aquí pero ojo si copias el pattern a otro lado).</li>
  <li><strong>El modelo envuelve su JSON en comentarios.</strong> No intentes buscar <code>{</code> al inicio del string — busca el fence o usa un capture no-greedy tolerante.</li>
  <li><strong>Trailing commas en el "JSON" de salida.</strong> Algunos modelos cuelan trailing commas a pesar del training. El match regex funcionará; <code>JSON.parse</code> fallará. Quita trailing commas antes de parsear.</li>
  <li><strong>"JSON" con comillas simples.</strong> Misma historia — parece JSON, no es JSON válido, a la regex le da igual, a <code>JSON.parse</code> no.</li>
  <li><strong>Fences anidados.</strong> Si el modelo mete un ejemplo de fence markdown dentro de su respuesta, puedes tener falso-positivo en el fence interno. Prueba con datos realistas.</li>
  <li><strong>No uses regex para extracción seria de brackets anidados.</strong> Si el modelo devuelve un objeto con un objeto con un array de objetos, escribe un extractor JSON-aware — o pide el formato fence y parsea el cuerpo.</li>
  <li><strong>Privacidad.</strong> El texto y el pattern se quedan en la página. Sin subida, sin llamada a API.</li>
</ul>
""",
        "fr": """
<h2>À quoi ça sert ?</h2>
<p>Vous avez demandé du JSON au LLM. Il vous a renvoyé du JSON enveloppé dans des fences <code>```json</code> avec un préambule « Bien sûr, voici ! ». Ou « la réponse est 42 » alors que vous vouliez juste 42. Ou une liste numérotée alors que vous demandiez une chaîne séparée par des virgules. Bienvenue dans le problème de parsing que personne ne résout vraiment. Cet outil garde une petite bibliothèque de patterns d'extraction courants plus un éditeur de regex libre, pour itérer le motif sur votre vrai texte de réponse dans le navigateur.</p>

<h3>Quand l'utiliser</h3>
<ul>
  <li><strong>Concevoir un output parser pour un pipeline d'agent.</strong> Balancez quelques réponses réelles dedans, construisez une regex qui les couvre toutes, collez-la dans votre code.</li>
  <li><strong>Déboguer un parser cassé.</strong> Votre pipeline prod a commencé à échouer parce que le modèle a ajouté « Voici le JSON : » avant le fence. Voyez précisément où votre regex décroche.</li>
  <li><strong>Extraction one-shot rapide.</strong> Vous avez dix réponses LLM dans un doc ; il faut extraire la partie structurée de chacune. Coller, matcher, copier, suivant.</li>
</ul>

<h3>Les modèles</h3>
<ul>
  <li><strong>JSON dans un fence ```json</strong> — le cas le plus courant. Capture le groupe 1 = le corps. <code>g</code> s'il peut y avoir plusieurs fences.</li>
  <li><strong>JSON dans n'importe quel fence</strong> — même idée mais le tag de langue est optionnel, et on capture seulement si le corps ressemble à <code>{ … }</code> ou <code>[ … ]</code>.</li>
  <li><strong>YAML dans fence</strong> — pareil que JSON-fence mais matche <code>```yaml</code> / <code>```yml</code>.</li>
  <li><strong>N'importe quel bloc de code fenced</strong> — capture tag de langue + corps. À utiliser quand on ne sait pas ce qu'il y a dedans.</li>
  <li><strong>Objet JSON brut</strong> — match greedy du premier <code>{</code> au dernier <code>}</code>. Fragile mais fonctionne pour les réponses « juste du JSON ».</li>
  <li><strong>Items de liste numérotée</strong> — <code>^\\s*\\d+[.)]\\s*(.+)$</code> avec <code>gm</code>. Capture le texte de chaque item, sans numérotation.</li>
  <li><strong>Paires key: value</strong> — ligne par ligne. Groupe 1 = clé, groupe 2 = valeur. S'arrête au premier deux-points.</li>
  <li><strong>Étiquette de classification unique</strong> — utile pour classificateurs sentiment / safety qui doivent répondre par un mot.</li>
  <li><strong>Regex custom</strong> — videz le motif et écrivez le vôtre.</li>
</ul>

<h3>La ligne « Parsed JSON »</h3>
<p>Si le premier groupe capturé (ou, à défaut, le match complet) parse comme JSON, l'outil affiche le résultat parsé sous les groupes. Ça vous dit non seulement « la regex a-t-elle matché » mais « a-t-elle matché la partie voulue, de façon décodable en JSON ». Si le parsing échoue, cette ligne reste vide.</p>

<h3>Pièges courants</h3>
<ul>
  <li><strong>Regex JavaScript, pas PCRE.</strong> Pas de <code>\\K</code>, pas de motifs récursifs. Le lookbehind exige des navigateurs modernes (post-2018 — bien ici mais attention si vous copiez le motif ailleurs).</li>
  <li><strong>Le modèle emballe son JSON dans du commentaire.</strong> N'essayez pas de chercher <code>{</code> en début de chaîne — trouvez le fence ou utilisez un capture non-greedy tolérant.</li>
  <li><strong>Virgules en trop dans le « JSON » de sortie.</strong> Certains modèles glissent des trailing commas malgré le training. La regex matchera ; <code>JSON.parse</code> échouera. Retirez les trailing commas avant de parser.</li>
  <li><strong>« JSON » à apostrophes simples.</strong> Même histoire — ressemble à du JSON, n'est pas du JSON valide, la regex s'en fiche, pas <code>JSON.parse</code>.</li>
  <li><strong>Fences imbriqués.</strong> Si le modèle met un exemple de fence markdown dans sa réponse, faux-positif possible sur le fence interne. Testez avec des données réalistes.</li>
  <li><strong>N'utilisez pas la regex pour de l'extraction sérieuse de brackets imbriqués.</strong> Si le modèle renvoie un objet contenant un objet contenant un tableau d'objets, écrivez un extracteur JSON-aware — ou demandez la forme fence et parsez le corps.</li>
  <li><strong>Confidentialité.</strong> Le texte et le motif restent dans la page. Pas d'upload, pas d'appel API.</li>
</ul>
""",
        "it": """
<h2>A cosa serve?</h2>
<p>Hai chiesto JSON al LLM. Ti ha dato JSON avvolto in fence <code>```json</code> con un preambolo "Certo, eccolo!". O ti ha dato "la risposta è 42" quando volevi solo 42. O ti ha dato una lista numerata quando hai chiesto una stringa separata da virgole. Benvenuto al problema di parsing che nessuno risolve davvero. Questo strumento tiene una piccola libreria di pattern di estrazione comuni più un editor di regex libero, così iteri il pattern sul tuo testo di risposta reale nel browser.</p>

<h3>Quando usarlo</h3>
<ul>
  <li><strong>Progettare un output parser per una pipeline ad agente.</strong> Butta dentro qualche risposta reale, costruisci una regex che le copre tutte, incollala nel codice.</li>
  <li><strong>Debugare un parser rotto.</strong> La tua pipeline di prod ha iniziato a fallire perché il modello ha aggiunto "Ecco il JSON:" prima del fence. Vedi esattamente dove la tua regex molla.</li>
  <li><strong>Estrazione veloce una tantum.</strong> Hai dieci risposte LLM in un doc; devi estrarre la parte strutturata da ciascuna. Incolla, matcha, copia, avanti.</li>
</ul>

<h3>I template</h3>
<ul>
  <li><strong>JSON in fence ```json</strong> — il caso più comune. Cattura il gruppo 1 = il corpo. Usa <code>g</code> se possono esserci più fence.</li>
  <li><strong>JSON in qualsiasi fence</strong> — stessa idea ma il tag della lingua è opzionale e catturiamo solo se il corpo somiglia a <code>{ … }</code> o <code>[ … ]</code>.</li>
  <li><strong>YAML in fence</strong> — come JSON-fence ma matcha <code>```yaml</code> / <code>```yml</code>.</li>
  <li><strong>Qualunque blocco fenced</strong> — cattura tag lingua + corpo. Quando non sai cosa c'è dentro.</li>
  <li><strong>Oggetto JSON nudo</strong> — match greedy dal primo <code>{</code> all'ultimo <code>}</code>. Fragile ma funziona per risposte "solo JSON".</li>
  <li><strong>Item di lista numerata</strong> — <code>^\\s*\\d+[.)]\\s*(.+)$</code> con <code>gm</code>. Cattura il testo di ogni item, senza numerazione.</li>
  <li><strong>Coppie key: value</strong> — riga per riga. Gruppo 1 = chiave, gruppo 2 = valore. Si ferma al primo due-punti.</li>
  <li><strong>Etichetta di classificazione singola</strong> — utile per classificatori di sentiment / safety che devono rispondere con una parola.</li>
  <li><strong>Regex custom</strong> — svuota il pattern e scrivi il tuo.</li>
</ul>

<h3>La riga "Parsed JSON"</h3>
<p>Se il primo gruppo catturato (o, in mancanza, il match completo) parsa come JSON, lo strumento stampa il risultato parsato sotto i gruppi. Ti dice non solo "la regex ha matchato" ma "ha matchato la parte che volevi, in modo decodificabile come JSON". Se il parsing fallisce, quella riga resta vuota.</p>

<h3>Trappole comuni</h3>
<ul>
  <li><strong>Regex JavaScript, non PCRE.</strong> Niente <code>\\K</code>, niente pattern ricorsivi. Lookbehind richiede browser moderni (post-2018 — ok qui ma attenzione se copi altrove).</li>
  <li><strong>Il modello avvolge il suo JSON in commento.</strong> Non cercare <code>{</code> a inizio stringa — trova il fence o usa un capture non-greedy tollerante.</li>
  <li><strong>Virgole in eccesso nel "JSON" di output.</strong> Alcuni modelli infilano trailing commas nonostante il training. La regex matcha; <code>JSON.parse</code> fallisce. Togli le trailing commas prima di parsare.</li>
  <li><strong>"JSON" con apostrofi singoli.</strong> Stessa storia — sembra JSON, non è JSON valido, alla regex non importa, a <code>JSON.parse</code> sì.</li>
  <li><strong>Fence annidati.</strong> Se il modello mette un esempio di fence markdown dentro la risposta, puoi avere falso-positivo sul fence interno. Testa con dati realistici.</li>
  <li><strong>Non usare regex per estrazione seria di parentesi annidate.</strong> Se il modello torna un oggetto che contiene un oggetto che contiene un array di oggetti, scrivi un estrattore JSON-aware — o chiedi la forma fence e parsa il corpo.</li>
  <li><strong>Privacy.</strong> Testo e pattern restano nella pagina. Niente upload, niente chiamata API.</li>
</ul>
""",
        "pt": """
<h2>Para que serve?</h2>
<p>Você pediu JSON pro LLM. Ele te deu JSON envolvido em fences <code>```json</code> com um preâmbulo "Claro, aqui está!". Ou te deu "a resposta é 42" quando você queria só 42. Ou te deu uma lista numerada quando você pediu uma string separada por vírgulas. Bem-vindo ao problema de parsing que ninguém resolve direito. Esta ferramenta mantém uma pequena biblioteca de patterns comuns de extração mais um editor de regex livre, pra você iterar o pattern com seu texto de resposta real no browser.</p>

<h3>Quando usar</h3>
<ul>
  <li><strong>Desenhar um parser de output pra uma pipeline de agente.</strong> Joga umas respostas reais dentro, monta uma regex que cobre todas, cola no código.</li>
  <li><strong>Debugar um parser quebrado.</strong> Sua pipeline de prod começou a falhar porque o modelo adicionou "Aqui está o JSON:" antes do fence. Veja exatamente onde sua regex para de matchear.</li>
  <li><strong>Extração rápida pontual.</strong> Você tem dez respostas de LLM num doc; precisa extrair a parte estruturada de cada uma. Colar, matchear, copiar, seguir.</li>
</ul>

<h3>Os templates</h3>
<ul>
  <li><strong>JSON em fence ```json</strong> — o caso mais comum. Captura grupo 1 = o corpo. Use <code>g</code> se pode ter vários fences.</li>
  <li><strong>JSON em qualquer fence</strong> — mesma ideia mas o tag de linguagem é opcional, e só capturamos se o corpo parece <code>{ … }</code> ou <code>[ … ]</code>.</li>
  <li><strong>YAML em fence</strong> — igual JSON-fence mas matcha <code>```yaml</code> / <code>```yml</code>.</li>
  <li><strong>Qualquer bloco fenced</strong> — captura tag de linguagem + corpo. Use quando não sabe o que tem dentro.</li>
  <li><strong>Objeto JSON puro</strong> — match guloso do primeiro <code>{</code> até o último <code>}</code>. Frágil mas funciona pra respostas "só JSON".</li>
  <li><strong>Itens de lista numerada</strong> — <code>^\\s*\\d+[.)]\\s*(.+)$</code> com <code>gm</code>. Captura o texto de cada item, sem numeração.</li>
  <li><strong>Pares key: value</strong> — linha a linha. Grupo 1 = chave, grupo 2 = valor. Para no primeiro dois-pontos.</li>
  <li><strong>Label de classificação único</strong> — útil pra classificadores de sentimento / safety que respondem com uma palavra só.</li>
  <li><strong>Regex custom</strong> — limpe o pattern e escreva o seu.</li>
</ul>

<h3>A linha "Parsed JSON"</h3>
<p>Se o primeiro grupo capturado (ou, na falta, o match completo) parseia como JSON, a ferramenta imprime o resultado parseado embaixo dos grupos. Isso te diz não só "a regex matcheou" mas "matcheou a parte que você queria, de jeito JSON-decodable". Se o parsing falha, essa linha fica em branco.</p>

<h3>Pegadinhas comuns</h3>
<ul>
  <li><strong>Regex JavaScript, não PCRE.</strong> Sem <code>\\K</code>, sem patterns recursivos. Lookbehind precisa de browsers modernos (pós-2018 — ok aqui, mas atenção se copiar pra outro lugar).</li>
  <li><strong>O modelo embrulha o JSON dele em comentário.</strong> Não tente buscar <code>{</code> no início da string — encontre o fence ou use um capture não-guloso tolerante.</li>
  <li><strong>Trailing commas no "JSON" de saída.</strong> Alguns modelos enfiam trailing commas apesar do training. A regex vai matchear; <code>JSON.parse</code> vai falhar. Tire as trailing commas antes de parsear.</li>
  <li><strong>"JSON" com aspas simples.</strong> Mesma história — parece JSON, não é JSON válido, a regex não liga, <code>JSON.parse</code> liga.</li>
  <li><strong>Fences aninhados.</strong> Se o modelo coloca um exemplo de fence markdown dentro da resposta, dá pra ter falso-positivo no fence interno. Teste com dados realistas.</li>
  <li><strong>Não use regex pra extração séria de brackets aninhados.</strong> Se o modelo retorna um objeto contendo um objeto contendo um array de objetos, escreve um extrator JSON-aware — ou peça a forma fence e parseie o corpo.</li>
  <li><strong>Privacidade.</strong> Texto e pattern ficam na página. Sem upload, sem chamada de API.</li>
</ul>
""",
        "pl": """
<h2>Do czego to służy?</h2>
<p>Poprosiłeś LLM o JSON. Dał ci JSON owinięty w fence'y <code>```json</code> z preambułą „Pewnie, oto!". Albo „odpowiedź to 42", kiedy chciałeś tylko 42. Albo numerowaną listę, kiedy prosiłeś o string oddzielony przecinkami. Witaj w problemie parsowania, którego nikt naprawdę nie rozwiązuje. To narzędzie trzyma małą bibliotekę popularnych patternów ekstrakcji plus wolny edytor regex, żebyś mógł iterować pattern na swoim realnym tekście odpowiedzi w przeglądarce.</p>

<h3>Kiedy tego użyć</h3>
<ul>
  <li><strong>Projektowanie parsera outputu dla pipeline'u agenta.</strong> Wrzuć kilka prawdziwych odpowiedzi, zbuduj regex, który je wszystkie obsłuży, wklej do kodu.</li>
  <li><strong>Debug zepsutego parsera.</strong> Twój prodowy pipeline zaczął padać, bo model dodał „Oto JSON:" przed fence'em. Zobacz dokładnie, gdzie twoja regex przestaje matchować.</li>
  <li><strong>Szybka jednorazowa ekstrakcja.</strong> Masz dziesięć odpowiedzi LLM w docu; trzeba wyciągnąć strukturalną część z każdej. Wklej, dopasuj, skopiuj, dalej.</li>
</ul>

<h3>Szablony</h3>
<ul>
  <li><strong>JSON w fence ```json</strong> — najczęstszy przypadek. Łapie grupę 1 = treść. Użyj <code>g</code>, jeśli może być wiele fence'ów.</li>
  <li><strong>JSON w dowolnym fence</strong> — ten sam pomysł, ale tag języka jest opcjonalny, łapiemy tylko, jeśli treść wygląda jak <code>{ … }</code> lub <code>[ … ]</code>.</li>
  <li><strong>YAML w fence</strong> — jak JSON-fence, ale matchuje <code>```yaml</code> / <code>```yml</code>.</li>
  <li><strong>Dowolny fenced code block</strong> — łapie tag języka + treść. Gdy nie wiesz, co jest w środku.</li>
  <li><strong>Goły obiekt JSON</strong> — chciwy match od pierwszego <code>{</code> do ostatniego <code>}</code>. Kruchy, ale działa dla odpowiedzi „tylko JSON".</li>
  <li><strong>Elementy listy numerowanej</strong> — <code>^\\s*\\d+[.)]\\s*(.+)$</code> z <code>gm</code>. Łapie tekst każdego elementu bez numeracji.</li>
  <li><strong>Pary key: value</strong> — linia po linii. Grupa 1 = klucz, grupa 2 = wartość. Zatrzymuje się przy pierwszym dwukropku.</li>
  <li><strong>Pojedynczy label klasyfikacji</strong> — przydatne dla klasyfikatorów sentymentu / safety, które mają odpowiedzieć jednym słowem.</li>
  <li><strong>Custom regex</strong> — wyczyść pattern i napisz własny.</li>
</ul>

<h3>Linia „Parsed JSON"</h3>
<p>Jeśli pierwsza grupa łapiąca (a w razie czego cały match) parsuje się jako JSON, narzędzie wyświetla sparsowany wynik pod grupami. To mówi nie tylko „czy regex zmatchował", ale „czy zmatchował tę część, której chciałeś, w sposób JSON-dekodowalny". Jeśli parsing pada, linia zostaje pusta.</p>

<h3>Częste pułapki</h3>
<ul>
  <li><strong>Regex JavaScript, nie PCRE.</strong> Brak <code>\\K</code>, brak rekurencji. Lookbehind wymaga nowoczesnych przeglądarek (po 2018 — tu ok, ale uważaj, jeśli kopiujesz pattern gdzie indziej).</li>
  <li><strong>Model owija swój JSON w komentarz.</strong> Nie próbuj szukać <code>{</code> na początku stringa — znajdź fence albo użyj tolerancyjnego non-greedy capture.</li>
  <li><strong>Trailing commas w „JSON" wyjścia.</strong> Niektóre modele wsadzają trailing commas mimo treningu. Regex matchnie; <code>JSON.parse</code> padnie. Wytnij trailing commas przed parsowaniem.</li>
  <li><strong>„JSON" z apostrofami.</strong> Ta sama historia — wygląda jak JSON, nim nie jest, regex'a nie obchodzi, <code>JSON.parse</code> obchodzi.</li>
  <li><strong>Zagnieżdżone fence'y.</strong> Jeśli model wstawi przykład fence'a markdown wewnątrz odpowiedzi, możesz mieć fałszywy match na wewnętrznym. Testuj z realistycznymi danymi.</li>
  <li><strong>Nie używaj regex do poważnej ekstrakcji zagnieżdżonych nawiasów.</strong> Jeśli model zwraca obiekt zawierający obiekt zawierający tablicę obiektów, napisz prawdziwy JSON-świadomy ekstraktor — albo poproś o formę fence i sparsuj treść.</li>
  <li><strong>Prywatność.</strong> Tekst i pattern zostają na stronie. Bez uploadu, bez wywołania API.</li>
</ul>
""",
        "ja": """
<h2>用途</h2>
<p>LLM に JSON を頼んだ。<code>```json</code> フェンスに包まれ、「もちろん、こちらです！」という前置きまでついた JSON が返ってきた。あるいは「答えは 42 です」と返ってきた、42 だけが欲しかったのに。あるいはカンマ区切り文字列をくれと言ったのに、番号付きリストが返ってきた。誰も本気で解決していないパースの問題へようこそ。本ツールは典型的な抽出パターンの小さなライブラリと、自由記述の正規表現エディタを備え、実際の応答テキストに対してブラウザ内でパターンを反復できます。</p>

<h3>使うべきタイミング</h3>
<ul>
  <li><strong>エージェントパイプライン用の出力パーサ設計。</strong> 実際の応答をいくつか投入し、それら全部を捕まえる正規表現を組み、コードへ貼り付けます。</li>
  <li><strong>壊れたパーサのデバッグ。</strong> モデルが「JSON はこちらです：」とフェンスの前に書くようになって本番が落ち始めた。どこから正規表現が外れているのかを正確に把握できます。</li>
  <li><strong>素早い一回限りの抽出。</strong> ドキュメント内に 10 件の LLM 応答、それぞれから構造化部分を抜き出したい。貼って、マッチして、コピーして、次へ。</li>
</ul>

<h3>テンプレート一覧</h3>
<ul>
  <li><strong>```json フェンス内 JSON</strong> — 最も一般的。グループ 1 = 本体。複数フェンスがあり得る場合は <code>g</code> フラグを。</li>
  <li><strong>任意のフェンス内 JSON</strong> — 同じ趣旨、言語タグは任意、本体が <code>{ … }</code> または <code>[ … ]</code> っぽいときだけキャプチャ。</li>
  <li><strong>YAML フェンス</strong> — JSON-fence と同じ、ただし <code>```yaml</code> / <code>```yml</code> に一致。</li>
  <li><strong>任意の fenced コードブロック</strong> — 言語タグと本体をキャプチャ。中身が何か分からないときに。</li>
  <li><strong>素の JSON オブジェクト</strong> — 先頭 <code>{</code> から末尾 <code>}</code> までの貪欲マッチ。脆いが「ただの JSON」レスポンスには有効。</li>
  <li><strong>番号付きリスト項目</strong> — <code>^\\s*\\d+[.)]\\s*(.+)$</code> に <code>gm</code>。各項目のテキスト（番号なし）をキャプチャ。</li>
  <li><strong>key: value ペア</strong> — 行単位。グループ 1 = キー、グループ 2 = 値。最初のコロンで区切ります。</li>
  <li><strong>単一の分類ラベル</strong> — 1 語で答えるべき感情分析やセーフティ判定の出力に。</li>
  <li><strong>カスタム正規表現</strong> — パターンをクリアして自分で書く。</li>
</ul>

<h3>「Parsed JSON」行について</h3>
<p>最初のキャプチャグループ（なければマッチ全体）が JSON としてパースできれば、ツールはそれをグループの下に表示します。これで「正規表現がマッチしたか」だけでなく「あなたが欲しかった部分を JSON 復元可能な形でマッチしたか」が分かります。パースに失敗するとこの行は空のままです。</p>

<h3>よくある注意点</h3>
<ul>
  <li><strong>PCRE ではなく JavaScript の正規表現です。</strong> <code>\\K</code> なし、再帰なし。Lookbehind は新しめのブラウザ向け（2018 年以降 — ここでは問題ありませんが、よそにコピーするときは注意）。</li>
  <li><strong>モデルは JSON をコメントで包みます。</strong> 文字列頭の <code>{</code> を探そうとせず、フェンスを探すか、寛容な非貪欲キャプチャを使いましょう。</li>
  <li><strong>「JSON」末尾のカンマ。</strong> 学習にもかかわらず、いくつかのモデルは末尾カンマを混ぜます。正規表現はマッチしても、<code>JSON.parse</code> は失敗。パース前に末尾カンマを除去してください。</li>
  <li><strong>シングルクォートの「JSON」。</strong> 同じ話 — JSON のように見えて JSON ではなく、正規表現は気にせず、<code>JSON.parse</code> は気にします。</li>
  <li><strong>入れ子フェンス。</strong> モデルが応答内に Markdown のフェンス例を入れると、内側のフェンスで誤マッチが起きます。現実的なデータでテストを。</li>
  <li><strong>真剣な入れ子ブラケット抽出に正規表現は向きません。</strong> モデルが入れ子オブジェクトを返すなら、JSON 認識の抽出器を書きましょう — あるいはフェンス形式で要求しその中身をパースします。</li>
  <li><strong>プライバシー。</strong> テキストとパターンはページ内に留まります。アップロードも API 呼び出しもありません。</li>
</ul>
""",
        "nl": """
<h2>Waarvoor is dit?</h2>
<p>Je vroeg het LLM om JSON. Het gaf je JSON gewikkeld in <code>```json</code>-fences met een "Zeker, alsjeblieft!"-preambule. Of het gaf "het antwoord is 42" terwijl je gewoon 42 wilde. Of een genummerde lijst terwijl je een komma-gescheiden string vroeg. Welkom bij het parsingprobleem dat niemand fatsoenlijk oplost. Deze tool houdt een kleine bibliotheek van gangbare extractiepatterns plus een vrije regex-editor, zodat je het patroon op je echte response-tekst kunt iteren in de browser.</p>

<h3>Wanneer gebruiken</h3>
<ul>
  <li><strong>Een output-parser ontwerpen voor een agent-pipeline.</strong> Gooi wat echte responses erin, bouw een regex die ze allemaal pakt, plak 'm in je code.</li>
  <li><strong>Een kapotte parser debuggen.</strong> Je prod-pipeline begon te falen omdat het model "Hier is de JSON:" voor het fence zette. Zie precies waar je regex afhaakt.</li>
  <li><strong>Snelle eenmalige extractie.</strong> Je hebt tien LLM-responses in een doc; je wilt het gestructureerde stuk uit elk halen. Plakken, matchen, kopiëren, door.</li>
</ul>

<h3>De templates</h3>
<ul>
  <li><strong>JSON in ```json-fence</strong> — de meest voorkomende case. Captured groep 1 = de body. Gebruik <code>g</code> als er meerdere fences kunnen zijn.</li>
  <li><strong>JSON in elk fence</strong> — zelfde idee maar de taal-tag is optioneel en we capturen alleen als de body op <code>{ … }</code> of <code>[ … ]</code> lijkt.</li>
  <li><strong>YAML in fence</strong> — als JSON-fence maar matcht <code>```yaml</code> / <code>```yml</code>.</li>
  <li><strong>Elk fenced codeblok</strong> — captured taal-tag + body. Gebruik als je niet weet wat erin zit.</li>
  <li><strong>Kaal JSON-object</strong> — greedy match van eerste <code>{</code> tot laatste <code>}</code>. Broos maar werkt voor "alleen JSON"-responses.</li>
  <li><strong>Genummerde lijst-items</strong> — <code>^\\s*\\d+[.)]\\s*(.+)$</code> met <code>gm</code>. Captured de tekst van elk item, zonder nummering.</li>
  <li><strong>key: value-paren</strong> — regel voor regel. Groep 1 = key, groep 2 = value. Stopt bij de eerste dubbele punt.</li>
  <li><strong>Enkele classificatielabel</strong> — handig voor sentiment- / safety-classifiers die met één woord moeten antwoorden.</li>
  <li><strong>Custom regex</strong> — leeg het patroon en schrijf je eigen.</li>
</ul>

<h3>De "Parsed JSON"-regel</h3>
<p>Als de eerste capture-groep (of bij gebrek daaraan de hele match) als JSON parseert, drukt de tool het geparseerde resultaat onder de groepen af. Dat vertelt je niet alleen "matchte de regex" maar "matchte hij het stuk dat je wilde, op een JSON-decodeerbare manier". Faalt het parsen, blijft die regel leeg.</p>

<h3>Veelvoorkomende valkuilen</h3>
<ul>
  <li><strong>JavaScript-regex, geen PCRE.</strong> Geen <code>\\K</code>, geen recursieve patterns. Lookbehind vereist moderne browsers (post-2018 — prima hier maar let op als je het patroon elders kopieert).</li>
  <li><strong>Het model wikkelt zijn JSON in commentaar.</strong> Zoek niet naar <code>{</code> aan het begin van de string — vind het fence of gebruik een tolerante non-greedy capture.</li>
  <li><strong>Trailing comma's in "JSON"-output.</strong> Sommige modellen smokkelen trailing comma's binnen ondanks training. De regex matcht; <code>JSON.parse</code> faalt. Strip trailing comma's voor je parseert.</li>
  <li><strong>"JSON" met enkele aanhalingstekens.</strong> Zelfde verhaal — lijkt op JSON, is geen geldig JSON, de regex maakt het niet uit, <code>JSON.parse</code> wel.</li>
  <li><strong>Geneste fences.</strong> Als het model een markdown-fence-voorbeeld in zijn response zet, krijg je een vals-positieve match op het binnenste fence. Test met realistische data.</li>
  <li><strong>Gebruik regex niet voor serieuze geneste-bracket-extractie.</strong> Als het model een object met een object met een array van objecten teruggeeft, schrijf een echte JSON-bewuste extractor — of vraag de fence-vorm en parse de body.</li>
  <li><strong>Privacy.</strong> Tekst en patroon blijven op de pagina. Geen upload, geen API-call.</li>
</ul>
""",
        "tr": """
<h2>Bu ne işe yarar?</h2>
<p>LLM'den JSON istedin. Sana <code>```json</code> fence'lerine sarılı, "Tabii, buyur!" girişiyle JSON verdi. Ya da sadece 42 isterken "cevap 42" verdi. Ya da virgülle ayrılmış string isterken numaralı liste verdi. Kimsenin gerçek bir çözüm yayınlamadığı parsing problemine hoş geldin. Bu araç yaygın çıkarım pattern'lerinin küçük bir kütüphanesini ve serbest bir regex editörünü tutar, böylece pattern'ini tarayıcıda gerçek response metnin üzerinde iyileştirebilirsin.</p>

<h3>Ne zaman kullanılır</h3>
<ul>
  <li><strong>Bir agent pipeline'ı için çıktı parser'ı tasarlama.</strong> İçine birkaç gerçek yanıt at, hepsini ele alan bir regex inşa et, koduna yapıştır.</li>
  <li><strong>Bozuk bir parser'ı debug etme.</strong> Prod pipeline'ın model fence öncesi "İşte JSON:" eklediği için fail etmeye başladı. Regex'inin tam olarak nerede uyuşmayı kestiğini gör.</li>
  <li><strong>Hızlı tek seferlik çıkarım.</strong> Bir doc'da on LLM yanıtın var; her birinden yapılandırılmış kısmı çıkarman gerekiyor. Yapıştır, eşleştir, kopyala, devam.</li>
</ul>

<h3>Şablonlar</h3>
<ul>
  <li><strong>```json code fence içinde JSON</strong> — en yaygın durum. Grup 1 = gövde. Birden fazla fence olabilirse <code>g</code> bayrağını kullan.</li>
  <li><strong>Herhangi bir code fence içinde JSON</strong> — aynı fikir ama dil etiketi opsiyonel ve sadece gövde <code>{ … }</code> veya <code>[ … ]</code> görünüyorsa yakalıyoruz.</li>
  <li><strong>Fence içinde YAML</strong> — JSON-fence ile aynı ama <code>```yaml</code> / <code>```yml</code> ile eşleşir.</li>
  <li><strong>Herhangi fenced code block</strong> — dil etiketi + gövdeyi yakalar. İçinde ne olduğunu bilmediğinde kullan.</li>
  <li><strong>Çıplak JSON nesnesi</strong> — ilk <code>{</code>'tan son <code>}</code>'a kadar aç gözlü eşleşme. Kırılgan ama "sadece JSON" yanıtları için işe yarar.</li>
  <li><strong>Numaralı liste öğeleri</strong> — <code>gm</code> ile <code>^\\s*\\d+[.)]\\s*(.+)$</code>. Her öğenin metnini numarasız yakalar.</li>
  <li><strong>key: value çiftleri</strong> — satır satır. Grup 1 = anahtar, grup 2 = değer. İlk iki noktada durur.</li>
  <li><strong>Tekil sınıflandırma etiketi</strong> — tek kelime ile yanıtlaması gereken sentiment / safety sınıflandırıcılar için.</li>
  <li><strong>Custom regex</strong> — pattern'i temizle ve kendininkini yaz.</li>
</ul>

<h3>"Parsed JSON" satırı</h3>
<p>İlk yakalama grubu (yoksa tam eşleşme) JSON olarak parse edilirse, araç parse edilmiş sonucu grupların altına yazar. Bu sadece "regex eşleşti mi" değil, "istediğin parçayı JSON çözülebilir biçimde yakaladı mı" sorusunu cevaplar. Parse fail olursa, o satır boş kalır.</p>

<h3>Yaygın tuzaklar</h3>
<ul>
  <li><strong>JavaScript regex, PCRE değil.</strong> <code>\\K</code> yok, özyinelemeli pattern yok. Lookbehind modern tarayıcı gerektirir (2018 sonrası — burada iyi ama pattern'i başka yere kopyalarsan dikkat).</li>
  <li><strong>Model JSON'unu yorumla sarar.</strong> String başında <code>{</code> arama — fence bul veya toleranslı non-greedy capture kullan.</li>
  <li><strong>"JSON" çıktısında trailing virgüller.</strong> Bazı modeller training'e rağmen trailing virgül kaçırır. Regex eşleşir; <code>JSON.parse</code> fail olur. Parse etmeden önce trailing virgülleri kaldır.</li>
  <li><strong>Tek tırnaklı "JSON".</strong> Aynı hikaye — JSON gibi görünür, geçerli JSON değil, regex umursamaz, <code>JSON.parse</code> umursar.</li>
  <li><strong>İç içe fence'ler.</strong> Model yanıtının içine markdown fence örneği koyarsa, içteki fence'te yanlış-pozitif eşleşme alabilirsin. Gerçekçi verilerle test et.</li>
  <li><strong>Ciddi iç içe parantez çıkarımı için regex kullanma.</strong> Model bir nesne içeren bir nesne içeren bir nesne dizisi döndürürse, gerçek JSON farkındalıklı çıkarıcı yaz — veya fence formunu iste ve fence gövdesini parse et.</li>
  <li><strong>Gizlilik.</strong> Metin ve pattern sayfada kalır. Upload yok, API çağrısı yok.</li>
</ul>
""",
        "id": """
<h2>Untuk apa ini?</h2>
<p>Kamu minta JSON ke LLM. Dia kasih JSON dibungkus fence <code>```json</code> dengan pembuka "Tentu, ini dia!". Atau dia kasih "jawabannya 42" padahal kamu cuma mau 42. Atau dia kasih list bernomor padahal kamu minta string dipisah koma. Selamat datang di masalah parsing yang nggak ada solusi resmi-nya. Tool ini punya pustaka kecil pattern ekstraksi umum plus editor regex bebas, jadi kamu bisa iterate pattern dengan teks response asli langsung di browser.</p>

<h3>Kapan digunakan</h3>
<ul>
  <li><strong>Merancang parser output untuk pipeline agen.</strong> Lempar beberapa response asli, bangun regex yang menangani semuanya, tempel ke kode.</li>
  <li><strong>Debug parser yang rusak.</strong> Pipeline prod-mu mulai gagal karena model menambahkan "Berikut JSON-nya:" sebelum fence. Lihat persis di mana regex-mu berhenti match.</li>
  <li><strong>Ekstraksi cepat sekali jalan.</strong> Kamu punya sepuluh response LLM di sebuah doc; perlu ekstrak bagian terstruktur dari masing-masing. Tempel, match, salin, lanjut.</li>
</ul>

<h3>Template</h3>
<ul>
  <li><strong>JSON dalam fence ```json</strong> — kasus paling umum. Tangkap grup 1 = isi. Pakai <code>g</code> kalau bisa ada banyak fence.</li>
  <li><strong>JSON dalam fence apa pun</strong> — ide sama tapi tag bahasa opsional dan kita hanya tangkap kalau isi terlihat seperti <code>{ … }</code> atau <code>[ … ]</code>.</li>
  <li><strong>YAML dalam fence</strong> — sama dengan JSON-fence tapi match <code>```yaml</code> / <code>```yml</code>.</li>
  <li><strong>Block fenced apa pun</strong> — tangkap tag bahasa + isi. Pakai saat tidak tahu apa di dalamnya.</li>
  <li><strong>Objek JSON polos</strong> — match greedy dari <code>{</code> pertama ke <code>}</code> terakhir. Rapuh tapi jalan untuk response "cuma JSON".</li>
  <li><strong>Item list bernomor</strong> — <code>^\\s*\\d+[.)]\\s*(.+)$</code> dengan <code>gm</code>. Tangkap teks tiap item, tanpa nomor.</li>
  <li><strong>Pasangan key: value</strong> — baris demi baris. Grup 1 = key, grup 2 = value. Berhenti di titik dua pertama.</li>
  <li><strong>Label klasifikasi tunggal</strong> — berguna untuk classifier sentiment / safety yang harus jawab satu kata.</li>
  <li><strong>Regex custom</strong> — bersihkan pattern dan tulis sendiri.</li>
</ul>

<h3>Baris "Parsed JSON"</h3>
<p>Kalau grup tangkap pertama (atau, kalau gagal, full match) bisa di-parse sebagai JSON, tool mencetak hasil yang sudah di-parse di bawah grup. Itu memberitahumu bukan cuma "apakah regex match" tapi "apakah dia match bagian yang kamu mau, dalam cara yang bisa di-decode JSON". Kalau parsing gagal, baris itu kosong.</p>

<h3>Kesalahan umum</h3>
<ul>
  <li><strong>Regex JavaScript, bukan PCRE.</strong> Tidak ada <code>\\K</code>, tidak ada pattern rekursif. Lookbehind butuh browser modern (pasca-2018 — fine di sini tapi hati-hati kalau kamu salin pattern ke tempat lain).</li>
  <li><strong>Model membungkus JSON-nya dengan komentar.</strong> Jangan coba cari <code>{</code> di awal string — temukan fence atau pakai capture non-greedy yang toleran.</li>
  <li><strong>Koma trailing di "JSON" output.</strong> Beberapa model menyelipkan koma trailing meski sudah di-training. Match regex akan jalan; <code>JSON.parse</code> akan gagal. Hapus koma trailing sebelum parsing.</li>
  <li><strong>"JSON" dengan single quote.</strong> Cerita sama — terlihat seperti JSON, bukan JSON valid, regex nggak peduli, <code>JSON.parse</code> peduli.</li>
  <li><strong>Fence bersarang.</strong> Kalau model masukin contoh fence markdown di dalam response-nya, kamu bisa dapat false-positive match di fence dalam. Tes dengan data realistis.</li>
  <li><strong>Jangan pakai regex untuk ekstraksi serius bracket bersarang.</strong> Kalau model balas objek berisi objek berisi array objek, tulis ekstraktor JSON-aware — atau minta bentuk fence dan parse isi fence.</li>
  <li><strong>Privasi.</strong> Teks dan pattern tetap di halaman. Tanpa upload, tanpa panggilan API.</li>
</ul>
""",
        "vi": """
<h2>Công cụ này để làm gì?</h2>
<p>Bạn yêu cầu LLM trả JSON. Nó trả JSON bọc trong fence <code>```json</code> với lời mở đầu "Chắc chắn rồi, đây ạ!". Hoặc nó trả "câu trả lời là 42" khi bạn chỉ muốn 42. Hoặc nó trả danh sách đánh số khi bạn yêu cầu chuỗi phân cách bằng dấu phẩy. Chào mừng đến với vấn đề parsing mà không ai giải quyết thực sự. Công cụ này giữ một thư viện nhỏ các pattern trích xuất phổ biến cùng trình soạn regex tự do, để bạn lặp pattern với văn bản phản hồi thực ngay trong trình duyệt.</p>

<h3>Khi nào nên dùng</h3>
<ul>
  <li><strong>Thiết kế parser output cho pipeline agent.</strong> Ném vào vài response thực, dựng regex xử lý tất cả, dán vào code.</li>
  <li><strong>Debug parser hỏng.</strong> Pipeline prod bắt đầu fail vì model thêm "Đây là JSON:" trước fence. Xem chính xác regex của bạn dừng match ở đâu.</li>
  <li><strong>Trích xuất nhanh một lần.</strong> Bạn có mười response LLM trong một doc; cần trích phần có cấu trúc từ mỗi cái. Dán, match, sao chép, tiếp.</li>
</ul>

<h3>Các template</h3>
<ul>
  <li><strong>JSON trong fence ```json</strong> — trường hợp phổ biến nhất. Bắt nhóm 1 = nội dung. Dùng <code>g</code> nếu có thể có nhiều fence.</li>
  <li><strong>JSON trong bất kỳ fence nào</strong> — cùng ý nhưng thẻ ngôn ngữ tùy chọn và chỉ bắt nếu nội dung trông như <code>{ … }</code> hoặc <code>[ … ]</code>.</li>
  <li><strong>YAML trong fence</strong> — giống JSON-fence nhưng match <code>```yaml</code> / <code>```yml</code>.</li>
  <li><strong>Bất kỳ code block fenced</strong> — bắt thẻ ngôn ngữ + nội dung. Dùng khi không biết bên trong là gì.</li>
  <li><strong>Đối tượng JSON trần</strong> — match tham lam từ <code>{</code> đầu tiên đến <code>}</code> cuối. Mong manh nhưng hoạt động cho response "chỉ JSON".</li>
  <li><strong>Mục danh sách đánh số</strong> — <code>^\\s*\\d+[.)]\\s*(.+)$</code> với <code>gm</code>. Bắt văn bản của mỗi mục, không có số.</li>
  <li><strong>Cặp key: value</strong> — dòng một. Nhóm 1 = key, nhóm 2 = value. Dừng ở dấu hai chấm đầu tiên.</li>
  <li><strong>Nhãn phân loại đơn</strong> — hữu ích cho classifier sentiment / safety phải trả lời bằng một từ.</li>
  <li><strong>Custom regex</strong> — xóa pattern và viết của bạn.</li>
</ul>

<h3>Dòng "Parsed JSON"</h3>
<p>Nếu nhóm bắt đầu tiên (hoặc, nếu thất bại, full match) parse được dưới dạng JSON, công cụ in kết quả đã parse dưới các nhóm. Điều đó nói cho bạn không chỉ "regex có match không" mà còn "nó có match phần bạn muốn theo cách có thể decode JSON không". Nếu parsing fail, dòng đó trống.</p>

<h3>Lưu ý thường gặp</h3>
<ul>
  <li><strong>Regex JavaScript, không phải PCRE.</strong> Không có <code>\\K</code>, không có pattern đệ quy. Lookbehind cần trình duyệt hiện đại (sau 2018 — ổn ở đây nhưng cẩn thận nếu sao chép pattern đi nơi khác).</li>
  <li><strong>Model bọc JSON của nó trong bình luận.</strong> Đừng cố tìm <code>{</code> ở đầu chuỗi — tìm fence hoặc dùng capture non-greedy linh hoạt.</li>
  <li><strong>Trailing comma trong "JSON" output.</strong> Một số model lén đưa trailing comma vào dù đã training. Match regex sẽ chạy; <code>JSON.parse</code> sẽ fail. Bỏ trailing comma trước khi parse.</li>
  <li><strong>"JSON" với dấu nháy đơn.</strong> Cùng câu chuyện — trông như JSON, không phải JSON hợp lệ, regex không quan tâm, <code>JSON.parse</code> quan tâm.</li>
  <li><strong>Fence lồng nhau.</strong> Nếu model đưa ví dụ fence markdown vào response, bạn có thể bị false-positive match ở fence trong. Test với dữ liệu thực tế.</li>
  <li><strong>Đừng dùng regex cho trích xuất nghiêm túc bracket lồng nhau.</strong> Nếu model trả về object chứa object chứa mảng object, viết trình trích xuất JSON-aware đúng nghĩa — hoặc yêu cầu dạng fence và parse nội dung fence.</li>
  <li><strong>Riêng tư.</strong> Văn bản và pattern ở lại trang. Không upload, không gọi API.</li>
</ul>
""",
        "hi": """
<h2>यह किसके लिए है?</h2>
<p>आपने LLM से JSON माँगा। उसने आपको <code>```json</code> fences में लिपटा JSON दिया एक "ज़रूर, यह रहा!" preamble के साथ। या आपने 42 चाहा था और उसने "जवाब 42 है" दे दिया। या comma-separated string माँगने पर numbered list दे दी। उस parsing problem में आपका स्वागत है जिसका कोई असली solution ship नहीं करता। यह tool common extraction patterns की एक छोटी library रखता है plus free-form regex editor, ताकि आप अपने actual response text के साथ pattern को browser में iterate कर सकें।</p>

<h3>कब इस्तेमाल करें</h3>
<ul>
  <li><strong>Agent pipeline के लिए output parser design करना।</strong> कुछ असली responses डालें, एक regex बनाएँ जो उन सभी को संभाले, अपने code में paste करें।</li>
  <li><strong>टूटे parser को debug करना।</strong> आपकी prod pipeline fail होने लगी क्योंकि model ने fence से पहले "यह रहा JSON:" जोड़ दिया। ठीक देखें कि आपका regex कहाँ match करना बंद करता है।</li>
  <li><strong>तेज़ one-off extraction।</strong> एक doc में दस LLM responses हैं; हर एक से structured हिस्सा निकालना है। Paste, match, copy, आगे बढ़ें।</li>
</ul>

<h3>Templates</h3>
<ul>
  <li><strong>```json code fence में JSON</strong> — सबसे आम case। Group 1 = body को capture करता है। यदि कई fences हो सकते हैं तो <code>g</code> flag का use करें।</li>
  <li><strong>किसी भी code fence में JSON</strong> — वही idea पर language tag optional है और हम केवल तब capture करते हैं जब body <code>{ … }</code> या <code>[ … ]</code> जैसी दिखे।</li>
  <li><strong>Code fence में YAML</strong> — JSON-fence जैसा पर <code>```yaml</code> / <code>```yml</code> match करता है।</li>
  <li><strong>कोई भी fenced code block</strong> — language tag + body capture करता है। जब आप नहीं जानते अंदर क्या है तो use करें।</li>
  <li><strong>Plain JSON object</strong> — पहले <code>{</code> से अंतिम <code>}</code> तक greedy match। नाज़ुक पर "बस JSON, कोई chrome नहीं" responses के लिए काम करता है।</li>
  <li><strong>Numbered list items</strong> — <code>gm</code> के साथ <code>^\\s*\\d+[.)]\\s*(.+)$</code>। हर item का text capture करता है, numbering के बिना।</li>
  <li><strong>key: value pairs</strong> — line by line। Group 1 = key, group 2 = value। पहले colon पर रुकता है।</li>
  <li><strong>Single classification label</strong> — sentiment / safety classifiers के लिए उपयोगी जो एक word से जवाब देने चाहिए।</li>
  <li><strong>Custom regex</strong> — pattern साफ़ करें और अपना लिखें।</li>
</ul>

<h3>"Parsed JSON" line</h3>
<p>अगर पहला capture group (या असफल होने पर full match) JSON के रूप में parse हो जाता है, tool groups के नीचे parsed result print कर देता है। यह बताता है केवल "क्या regex match हुआ" नहीं, बल्कि "क्या उसने वह हिस्सा match किया जो आप चाहते थे, JSON-decodable तरीके से"। अगर parsing fail होता है, वह line खाली रहती है।</p>

<h3>आम गलतियाँ</h3>
<ul>
  <li><strong>JavaScript regex, PCRE नहीं।</strong> कोई <code>\\K</code> नहीं, कोई recursive patterns नहीं। Lookbehind को modern browsers चाहिए (2018 के बाद — यहाँ ठीक है पर pattern को कहीं और copy करते समय ध्यान रखें)।</li>
  <li><strong>Model अपने JSON को commentary में लपेटता है।</strong> String के शुरू में <code>{</code> ढूँढने की कोशिश न करें — fence ढूँढें या tolerant non-greedy capture का use करें।</li>
  <li><strong>"JSON" output में trailing commas।</strong> कुछ models training के बावजूद trailing commas डाल देते हैं। Regex match काम करेगा; <code>JSON.parse</code> fail होगा। Parse से पहले trailing commas हटा दें।</li>
  <li><strong>Single-quoted "JSON"।</strong> वही कहानी — JSON जैसा दिखता है, valid JSON नहीं है, regex को परवाह नहीं, <code>JSON.parse</code> को है।</li>
  <li><strong>Nested fences।</strong> यदि model अपने response के अंदर markdown code-fence example डालता है, आपको inner fence पर false-positive match मिल सकता है। Realistic data के साथ test करें।</li>
  <li><strong>Serious nested-bracket extraction के लिए regex का इस्तेमाल न करें।</strong> यदि model एक object लौटाता है जिसमें objects का array है, एक proper JSON-aware extractor लिखें — या fence form माँगें और fence body को parse करें।</li>
  <li><strong>Privacy।</strong> Text और pattern page में रहते हैं। कोई upload नहीं, कोई API call नहीं।</li>
</ul>
""",
        "sk": """
<h2>Načo to slúži?</h2>
<p>Pýtal si si od LLM JSON. Dal ti JSON zabalený do <code>```json</code> fence-ov s predslovom „Iste, tu máš!". Alebo ti dal „odpoveď je 42", keď si chcel len 42. Alebo ti dal číslovaný zoznam, keď si žiadal reťazec oddelený čiarkami. Vitaj v probléme parsovania, ktorý nikto poriadne nerieši. Tento nástroj drží malú knižnicu bežných extrakčných patternov plus voľný regex editor, aby si iteroval pattern na svojom skutočnom texte odpovede priamo v prehliadači.</p>

<h3>Kedy to použiť</h3>
<ul>
  <li><strong>Návrh output parsera pre agentskú pipeline.</strong> Hoď tam pár skutočných odpovedí, postav regex, ktorý všetky pokryje, vlož do kódu.</li>
  <li><strong>Debug rozbitého parsera.</strong> Tvoja prod pipeline začala padať, lebo model pridal pred fence „Tu je JSON:". Pozri presne, kde tvoj regex prestáva matchovať.</li>
  <li><strong>Rýchla jednorazová extrakcia.</strong> V dokumente máš desať LLM odpovedí; treba z každej vytiahnuť štruktúrovanú časť. Vlož, dopasuj, skopíruj, ďalej.</li>
</ul>

<h3>Šablóny</h3>
<ul>
  <li><strong>JSON v ```json fence</strong> — najčastejší prípad. Zachytáva skupinu 1 = telo. Použi <code>g</code>, ak môže byť viacero fence-ov.</li>
  <li><strong>JSON v ľubovoľnom fence</strong> — tá istá myšlienka, ale jazykový tag je voliteľný a zachytávame len ak telo vyzerá ako <code>{ … }</code> alebo <code>[ … ]</code>.</li>
  <li><strong>YAML vo fence</strong> — ako JSON-fence, ale matchuje <code>```yaml</code> / <code>```yml</code>.</li>
  <li><strong>Akýkoľvek fenced kód blok</strong> — zachytí jazykový tag + telo. Použi, keď nevieš, čo je vnútri.</li>
  <li><strong>Holý JSON objekt</strong> — pažravé matchovanie od prvého <code>{</code> po posledné <code>}</code>. Krehké, ale funguje pre odpovede „len JSON".</li>
  <li><strong>Položky číslovaného zoznamu</strong> — <code>^\\s*\\d+[.)]\\s*(.+)$</code> s <code>gm</code>. Zachytí text každej položky bez čísla.</li>
  <li><strong>Páry key: value</strong> — riadok po riadku. Skupina 1 = kľúč, skupina 2 = hodnota. Zastaví pri prvej dvojbodke.</li>
  <li><strong>Jeden klasifikačný label</strong> — užitočné pre sentimentové / safety klasifikátory, ktoré majú odpovedať jedným slovom.</li>
  <li><strong>Custom regex</strong> — vymaž pattern a napíš vlastný.</li>
</ul>

<h3>Riadok „Parsed JSON"</h3>
<p>Ak prvá zachytená skupina (alebo, ak sa nepodarí, celé matchovanie) sa parsuje ako JSON, nástroj vypíše parsovaný výsledok pod skupiny. To ti hovorí nielen „matchol regex", ale „matchol tú časť, ktorú si chcel, JSON-dekódovateľne". Ak parsovanie zlyhá, ten riadok ostane prázdny.</p>

<h3>Časté pasce</h3>
<ul>
  <li><strong>JavaScript regex, nie PCRE.</strong> Žiadne <code>\\K</code>, žiadne rekurzívne patterny. Lookbehind vyžaduje moderné prehliadače (po 2018 — tu OK, ale pozor, ak pattern kopíruješ inde).</li>
  <li><strong>Model svoj JSON obaľuje komentárom.</strong> Nesnaž sa nájsť <code>{</code> na začiatku reťazca — nájdi fence alebo použi tolerantný non-greedy capture.</li>
  <li><strong>Trailing čiarky v „JSON" výstupe.</strong> Niektoré modely podsúvajú trailing čiarky aj napriek tréningu. Regex match prejde; <code>JSON.parse</code> spadne. Pred parsovaním trailing čiarky odstráň.</li>
  <li><strong>„JSON" s apostrofmi.</strong> Tá istá story — vyzerá ako JSON, nie je platný JSON, regex je to jedno, <code>JSON.parse</code> nie.</li>
  <li><strong>Vnorené fence-y.</strong> Ak model do odpovede dá príklad fence markdown-u, môžeš mať falošne pozitívny match na vnútornom fence. Testuj realistickými dátami.</li>
  <li><strong>Nepoužívaj regex na seriózne extrahovanie vnorených zátvoriek.</strong> Ak model vráti objekt obsahujúci objekt obsahujúci pole objektov, napíš poriadny JSON-aware extraktor — alebo si vypýtaj fence formu a parsuj telo fence-u.</li>
  <li><strong>Súkromie.</strong> Text a pattern ostávajú na stránke. Žiadny upload, žiadne API volanie.</li>
</ul>
""",
        "cs": """
<h2>K čemu to slouží?</h2>
<p>Ptal ses LLM na JSON. Dal ti JSON zabalený do <code>```json</code> fence-ů s předmluvou „Jasně, tady to máš!". Nebo ti dal „odpověď je 42", když jsi chtěl jen 42. Nebo ti dal číslovaný seznam, když jsi žádal řetězec oddělený čárkami. Vítej u problému parsování, který nikdo pořádně neřeší. Tenhle nástroj drží malou knihovnu běžných extrakčních patternů plus volný regex editor, abys iteroval pattern na svém skutečném textu odpovědi rovnou v prohlížeči.</p>

<h3>Kdy to použít</h3>
<ul>
  <li><strong>Návrh output parseru pro agentský pipeline.</strong> Hoď tam pár reálných odpovědí, postav regex, který je všechny pokryje, vlož do kódu.</li>
  <li><strong>Debug rozbitého parseru.</strong> Tvůj prod pipeline začal padat, protože model přidal před fence „Tady je JSON:". Podívej se přesně, kde tvůj regex přestává matchovat.</li>
  <li><strong>Rychlá jednorázová extrakce.</strong> V dokumentu máš deset LLM odpovědí; potřebuješ z každé vytáhnout strukturovanou část. Vlož, najdi, zkopíruj, dál.</li>
</ul>

<h3>Šablony</h3>
<ul>
  <li><strong>JSON v ```json fence</strong> — nejčastější případ. Zachytává skupinu 1 = tělo. Použij <code>g</code>, pokud může být víc fence-ů.</li>
  <li><strong>JSON v libovolném fence</strong> — stejná myšlenka, ale jazykový tag je volitelný, a zachytáváme jen pokud tělo vypadá jako <code>{ … }</code> nebo <code>[ … ]</code>.</li>
  <li><strong>YAML ve fence</strong> — jako JSON-fence, ale matchuje <code>```yaml</code> / <code>```yml</code>.</li>
  <li><strong>Jakýkoli fenced kód blok</strong> — zachytí jazykový tag + tělo. Použij, když nevíš, co je uvnitř.</li>
  <li><strong>Holý JSON objekt</strong> — chamtivé matchování od prvního <code>{</code> po poslední <code>}</code>. Křehké, ale funguje pro odpovědi „jen JSON".</li>
  <li><strong>Položky číslovaného seznamu</strong> — <code>^\\s*\\d+[.)]\\s*(.+)$</code> s <code>gm</code>. Zachytí text každé položky bez čísla.</li>
  <li><strong>Páry key: value</strong> — řádek po řádku. Skupina 1 = klíč, skupina 2 = hodnota. Zastaví u první dvojtečky.</li>
  <li><strong>Jeden klasifikační label</strong> — užitečné pro sentimentové / safety klasifikátory, které mají odpovědět jedním slovem.</li>
  <li><strong>Custom regex</strong> — smaž pattern a napiš vlastní.</li>
</ul>

<h3>Řádek „Parsed JSON"</h3>
<p>Pokud první zachycená skupina (nebo, pokud selže, celý match) parsuje jako JSON, nástroj vypíše parsovaný výsledek pod skupiny. To ti řekne nejen „zda regex matchnul", ale „zda matchnul tu část, kterou jsi chtěl, JSON-dekódovatelně". Když parsování selže, ten řádek zůstane prázdný.</p>

<h3>Časté pasti</h3>
<ul>
  <li><strong>JavaScript regex, ne PCRE.</strong> Žádné <code>\\K</code>, žádné rekurzivní patterny. Lookbehind vyžaduje moderní prohlížeče (po 2018 — tady OK, ale pozor, pokud pattern kopíruješ jinam).</li>
  <li><strong>Model svůj JSON obaluje komentářem.</strong> Nesnaž se hledat <code>{</code> na začátku řetězce — najdi fence nebo použij tolerantní non-greedy capture.</li>
  <li><strong>Trailing čárky v „JSON" výstupu.</strong> Některé modely podstrkávají trailing čárky navzdory tréninku. Regex match projde; <code>JSON.parse</code> spadne. Před parsováním trailing čárky odstraň.</li>
  <li><strong>„JSON" s apostrofy.</strong> Stejná story — vypadá jako JSON, není platný JSON, regexu je to jedno, <code>JSON.parse</code> ne.</li>
  <li><strong>Vnořené fence-y.</strong> Pokud model do odpovědi dá příklad markdown fence, můžeš dostat falešně pozitivní match na vnitřním fence. Testuj realistickými daty.</li>
  <li><strong>Nepoužívej regex pro seriózní extrakci vnořených závorek.</strong> Pokud model vrátí objekt obsahující objekt obsahující pole objektů, napiš pořádný JSON-aware extraktor — nebo si vyžádej fence formu a parsuj tělo fence-u.</li>
  <li><strong>Soukromí.</strong> Text a pattern zůstávají na stránce. Žádný upload, žádné API volání.</li>
</ul>
""",
    },
    "related": ["regex-tester", "regex-cheatsheet", "json-formatter"],
    "howto": {"flow": "transform", "action": "format", "noun": "text"},
}
