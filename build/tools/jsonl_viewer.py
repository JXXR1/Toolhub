TOOL = {
    "slug": "jsonl-viewer",
    "category": "data",
    "icon": "📃",
    "tags": ["jsonl", "ndjson", "json", "viewer", "llm", "training", "log", "stream"],
    "i18n": {
        "en": {
            "name": "JSONL Viewer",
            "tagline": "View JSONL (newline-delimited JSON) files in a paginated table. Paste up to thousands of lines, search, filter, expand individual records. Files never leave your browser.",
            "description": "Free online JSONL / NDJSON viewer. Paste content or upload a file, get a sortable / searchable table view with one row per JSON object. Useful for LLM training data, audit logs, event streams, OpenAI/Anthropic batch outputs. Schema auto-detected from first 100 rows; missing fields shown as blank cells. Pure JS in your browser — no upload, no server.",
        },
        "de": {
            "name": "JSONL-Viewer",
            "tagline": "JSONL-Dateien (zeilengetrenntes JSON) in einer paginierten Tabelle anzeigen. Bis zu tausende Zeilen einfügen, durchsuchen, filtern, einzelne Records aufklappen. Dateien verlassen den Browser nie.",
            "description": "Kostenloser Online-JSONL- / NDJSON-Viewer. Inhalt einfügen oder Datei hochladen, sofort eine sortier- / durchsuchbare Tabelle mit einer Zeile pro JSON-Objekt erhalten. Nützlich für LLM-Trainingsdaten, Audit-Logs, Event-Streams, OpenAI-/Anthropic-Batch-Outputs. Schema wird aus den ersten 100 Zeilen automatisch erkannt; fehlende Felder bleiben leer. Reines JS im Browser — kein Upload, kein Server.",
        },
        "es": {
            "name": "Visor JSONL",
            "tagline": "Visualiza archivos JSONL (JSON delimitado por líneas) en una tabla paginada. Pega hasta miles de líneas, busca, filtra, expande registros individuales. Los archivos nunca salen de tu navegador.",
            "description": "Visor JSONL / NDJSON gratuito en línea. Pega el contenido o sube un archivo, obtén una tabla ordenable / buscable con una fila por objeto JSON. Útil para datos de entrenamiento de LLM, logs de auditoría, streams de eventos, salidas batch de OpenAI/Anthropic. El esquema se detecta automáticamente de las primeras 100 filas; los campos faltantes se muestran como celdas vacías. JS puro en tu navegador — sin subida, sin servidor.",
        },
        "fr": {
            "name": "Visualiseur JSONL",
            "tagline": "Affichez des fichiers JSONL (JSON délimité par lignes) dans un tableau paginé. Collez jusqu'à des milliers de lignes, recherchez, filtrez, dépliez chaque enregistrement. Les fichiers ne quittent jamais votre navigateur.",
            "description": "Visualiseur JSONL / NDJSON gratuit en ligne. Collez le contenu ou chargez un fichier, obtenez une table triable / cherchable avec une ligne par objet JSON. Utile pour les données d'entraînement LLM, les logs d'audit, les flux d'événements, les sorties batch d'OpenAI/Anthropic. Le schéma est auto-détecté à partir des 100 premières lignes ; les champs manquants apparaissent en cellules vides. JS pur dans votre navigateur — pas d'upload, pas de serveur.",
        },
        "it": {
            "name": "Visualizzatore JSONL",
            "tagline": "Visualizza file JSONL (JSON delimitato da newline) in una tabella paginata. Incolla fino a migliaia di righe, cerca, filtra, espandi singoli record. I file non lasciano mai il browser.",
            "description": "Visualizzatore JSONL / NDJSON gratuito online. Incolla il contenuto o carica un file, ottieni una tabella ordinabile / ricercabile con una riga per oggetto JSON. Utile per dati di training LLM, log di audit, stream di eventi, output batch di OpenAI/Anthropic. Lo schema viene rilevato automaticamente dalle prime 100 righe; i campi mancanti appaiono come celle vuote. JS puro nel browser — niente upload, niente server.",
        },
        "pt": {
            "name": "Visualizador JSONL",
            "tagline": "Visualize arquivos JSONL (JSON delimitado por linha) numa tabela paginada. Cole até milhares de linhas, pesquise, filtre, expanda registros individuais. Arquivos nunca saem do seu browser.",
            "description": "Visualizador JSONL / NDJSON grátis online. Cole o conteúdo ou suba um arquivo, receba uma tabela ordenável / pesquisável com uma linha por objeto JSON. Útil pra dados de treinamento de LLM, audit logs, streams de eventos, saídas batch da OpenAI/Anthropic. Schema detectado automaticamente das primeiras 100 linhas; campos faltando aparecem como células vazias. JS puro no seu browser — sem upload, sem servidor.",
        },
        "pl": {
            "name": "Przeglądarka JSONL",
            "tagline": "Oglądaj pliki JSONL (JSON rozdzielony nowymi liniami) w paginowanej tabeli. Wklej do tysięcy linii, szukaj, filtruj, rozwijaj pojedyncze rekordy. Pliki nigdy nie opuszczają przeglądarki.",
            "description": "Darmowa online przeglądarka JSONL / NDJSON. Wklej zawartość albo wgraj plik, dostaniesz sortowalną / przeszukiwalną tabelę z jednym wierszem na obiekt JSON. Przydatne do danych treningowych LLM, audit logów, strumieni zdarzeń, batch outputów OpenAI/Anthropic. Schemat wykrywany automatycznie z pierwszych 100 wierszy; brakujące pola pokazywane jako puste komórki. Czysty JS w przeglądarce — żadnego uploadu, żadnego serwera.",
        },
        "ja": {
            "name": "JSONL ビューア",
            "tagline": "JSONL（改行区切り JSON）ファイルをページ送りテーブルで表示。最大数千行を貼り付け、検索・フィルタ・個別レコードの展開が可能。ファイルがブラウザを出ることはありません。",
            "description": "オンライン無料の JSONL / NDJSON ビューア。内容を貼り付けるかファイルをアップロードすると、1 オブジェクト 1 行のソート／検索可能なテーブルになります。LLM の学習データ、監査ログ、イベントストリーム、OpenAI／Anthropic のバッチ出力に便利です。スキーマは先頭 100 行から自動検出され、欠損フィールドは空セルになります。ブラウザ内の純粋な JS — アップロードなし、サーバーなし。",
        },
        "nl": {
            "name": "JSONL Viewer",
            "tagline": "Bekijk JSONL (newline-delimited JSON)-bestanden in een gepagineerde tabel. Plak duizenden regels, zoek, filter, expand individuele records. Bestanden verlaten je browser nooit.",
            "description": "Gratis online JSONL / NDJSON-viewer. Plak inhoud of upload een bestand, krijg een sorteerbare / doorzoekbare tabel met één rij per JSON-object. Handig voor LLM-trainingsdata, audit logs, event streams, OpenAI/Anthropic batch-outputs. Schema auto-gedetecteerd uit de eerste 100 rijen; ontbrekende velden als lege cellen. Pure JS in je browser — geen upload, geen server.",
        },
        "tr": {
            "name": "JSONL Görüntüleyici",
            "tagline": "JSONL (satır ayrılmış JSON) dosyalarını sayfalı tabloda görüntüle. Binlerce satıra kadar yapıştır, ara, filtrele, kayıtları ayrı ayrı genişlet. Dosyalar tarayıcından asla çıkmaz.",
            "description": "Ücretsiz online JSONL / NDJSON görüntüleyici. İçeriği yapıştır veya dosya yükle; her JSON nesnesi bir satır olan sıralanabilir / aranabilir bir tablo al. LLM eğitim verisi, audit log'ları, event stream'leri, OpenAI/Anthropic batch çıktıları için yararlı. Şema ilk 100 satırdan otomatik algılanır; eksik alanlar boş hücre olarak gösterilir. Tarayıcında saf JS — yükleme yok, sunucu yok.",
        },
        "id": {
            "name": "JSONL Viewer",
            "tagline": "Lihat file JSONL (JSON yang dipisah newline) dalam tabel berhalaman. Tempel hingga ribuan baris, cari, filter, expand record individu. File tidak pernah meninggalkan browser-mu.",
            "description": "Viewer JSONL / NDJSON gratis online. Tempel konten atau upload file, dapatkan tabel yang bisa diurutkan / dicari dengan satu baris per objek JSON. Berguna untuk data training LLM, log audit, stream event, output batch OpenAI/Anthropic. Schema dideteksi otomatis dari 100 baris pertama; field yang hilang ditampilkan sebagai sel kosong. JS murni di browser-mu — tanpa upload, tanpa server.",
        },
        "vi": {
            "name": "JSONL Viewer",
            "tagline": "Xem file JSONL (JSON phân tách bằng dòng mới) trong bảng phân trang. Dán đến hàng nghìn dòng, tìm kiếm, lọc, mở rộng từng bản ghi. File không bao giờ rời trình duyệt.",
            "description": "JSONL / NDJSON viewer miễn phí trực tuyến. Dán nội dung hoặc tải file, nhận bảng có thể sắp xếp / tìm kiếm với một hàng cho mỗi đối tượng JSON. Hữu ích cho dữ liệu training LLM, audit log, event stream, batch output của OpenAI/Anthropic. Schema được tự động phát hiện từ 100 hàng đầu tiên; trường thiếu hiển thị dưới dạng ô trống. JS thuần trong trình duyệt của bạn — không upload, không máy chủ.",
        },
        "hi": {
            "name": "JSONL Viewer",
            "tagline": "JSONL (newline-delimited JSON) files को paginated table में देखें। हज़ारों lines तक paste करें, search करें, filter करें, individual records expand करें। Files कभी आपके browser से बाहर नहीं जातीं।",
            "description": "मुफ़्त online JSONL / NDJSON viewer। Content paste करें या file upload करें, हर JSON object के लिए एक row के साथ sortable / searchable table पाएं। LLM training data, audit logs, event streams, OpenAI/Anthropic batch outputs के लिए उपयोगी। Schema पहली 100 rows से auto-detect किया जाता है; missing fields खाली cells के रूप में दिखाए जाते हैं। आपके browser में pure JS — कोई upload नहीं, कोई server नहीं।",
        },
        "sk": {
            "name": "JSONL prehliadač",
            "tagline": "Zobraz JSONL (JSON oddelený novými riadkami) súbory v stránkovanej tabuľke. Vlož tisíce riadkov, hľadaj, filtruj, rozbaľ jednotlivé záznamy. Súbory nikdy neopustia tvoj prehliadač.",
            "description": "Bezplatný online JSONL / NDJSON prehliadač. Vlož obsah alebo nahraj súbor, dostaneš tabuľku, ktorú možno triediť / prehľadávať, s jedným riadkom na JSON objekt. Užitočné pre tréningové dáta LLM, audit logy, eventové streamy, batch výstupy OpenAI/Anthropic. Schéma sa autodetekuje z prvých 100 riadkov; chýbajúce polia sa zobrazia ako prázdne bunky. Čisté JS v tvojom prehliadači — žiadny upload, žiadny server.",
        },
        "cs": {
            "name": "JSONL prohlížeč",
            "tagline": "Zobraz JSONL (JSON oddělený novými řádky) soubory ve stránkované tabulce. Vlož tisíce řádků, hledej, filtruj, rozbal jednotlivé záznamy. Soubory nikdy neopustí tvůj prohlížeč.",
            "description": "Zdarma online JSONL / NDJSON prohlížeč. Vlož obsah nebo nahraj soubor, dostaneš tabulku, kterou lze řadit / prohledávat, s jedním řádkem na JSON objekt. Užitečné pro trénovací data LLM, audit logy, eventové streamy, batch výstupy OpenAI/Anthropic. Schéma se autodetekuje z prvních 100 řádků; chybějící pole se zobrazí jako prázdné buňky. Čistý JS v tvém prohlížeči — žádný upload, žádný server.",
        },
    },
    "body": """
<div class="tool-card">
  <label for="jl-input">Paste JSONL (one JSON object per line) or upload a file</label>
  <textarea id="jl-input" oninput="jlParse()" spellcheck="false" placeholder='{"id": 1, "role": "user", "content": "Hello"}\n{"id": 2, "role": "assistant", "content": "Hi there"}'>{"id": 1, "role": "user", "content": "What is JSONL?", "tokens": 6}
{"id": 2, "role": "assistant", "content": "JSONL is newline-delimited JSON.", "tokens": 8}
{"id": 3, "role": "user", "content": "Where is it used?", "tokens": 5}
{"id": 4, "role": "assistant", "content": "LLM training data, audit logs, OpenAI/Anthropic batch APIs.", "tokens": 12, "meta": {"finish_reason": "stop"}}
{"id": 5, "role": "user", "content": "Nice."}</textarea>
  <div class="button-row" style="margin-top:0.5rem">
    <label class="secondary" style="display:inline-flex;align-items:center;gap:0.4rem;padding:0.5rem 0.9rem;border:1px solid var(--border);border-radius:6px;cursor:pointer">
      📂 Upload .jsonl / .ndjson
      <input type="file" id="jl-file" accept=".jsonl,.ndjson,application/x-ndjson,text/plain" onchange="jlLoadFile(event)" style="display:none">
    </label>
    <button class="secondary" onclick="document.getElementById('jl-input').value='';jlParse()">{LBL_CLEAR}</button>
  </div>
</div>
<div class="tool-card">
  <div class="jl-toolbar">
    <input type="text" id="jl-search" oninput="jlRender()" placeholder="🔍 Search any field (case-insensitive)…">
    <select id="jl-pagesize" onchange="jlRender()">
      <option value="50">50 / page</option>
      <option value="100" selected>100 / page</option>
      <option value="250">250 / page</option>
      <option value="500">500 / page</option>
    </select>
    <button class="secondary" onclick="jlExport('csv')">⬇ CSV</button>
    <button class="secondary" onclick="jlExport('json')">⬇ JSON array</button>
  </div>
  <div class="meta" id="jl-stats" style="margin-top:0.4rem"></div>
</div>
<div class="tool-card">
  <div id="jl-table-wrap" class="jl-tablewrap"></div>
  <div class="jl-pager" id="jl-pager"></div>
</div>
""",
    "script": """
<style>
.jl-toolbar{display:flex;gap:0.5rem;flex-wrap:wrap;align-items:center}
.jl-toolbar input[type=text]{flex:1;min-width:200px}
.jl-tablewrap{overflow:auto;max-height:600px;border:1px solid var(--border);border-radius:8px}
.jl-table{width:100%;border-collapse:collapse;font-size:0.85rem;font-family:ui-monospace,monospace}
.jl-table th,.jl-table td{padding:0.4rem 0.6rem;text-align:left;border-bottom:1px solid var(--border);vertical-align:top;white-space:nowrap}
.jl-table th{background:var(--bg-elev);position:sticky;top:0;z-index:1;cursor:pointer;user-select:none}
.jl-table th:hover{color:var(--accent)}
.jl-table td{max-width:320px;overflow:hidden;text-overflow:ellipsis}
.jl-table tr:hover{background:rgba(99,102,241,0.06)}
.jl-row-err{background:rgba(239,68,68,0.08)}
.jl-row-err td{color:#ef4444;white-space:normal}
.jl-cell-obj{color:var(--accent);cursor:pointer;text-decoration:underline dotted}
.jl-detail{background:var(--bg-elev);padding:0.6rem 0.8rem;white-space:pre-wrap;font-family:ui-monospace,monospace;font-size:0.82rem;border-top:1px solid var(--border);border-bottom:1px solid var(--border)}
.jl-pager{display:flex;gap:0.5rem;align-items:center;justify-content:center;margin-top:0.6rem;flex-wrap:wrap}
.jl-pager button{padding:0.3rem 0.7rem}
.jl-pager .jl-page-num{color:var(--text-muted);font-size:0.88rem;font-variant-numeric:tabular-nums}
.jl-null{color:var(--text-muted);font-style:italic}
</style>
<script>
let jlRows = [];
let jlCols = [];
let jlErrors = [];
let jlPage = 0;
let jlSort = {col: null, dir: 1};
let jlExpanded = new Set();
function jlEsc(s){ return String(s ?? '').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;'); }
function jlParse(){
  const raw = document.getElementById('jl-input').value;
  jlRows = []; jlErrors = []; jlExpanded = new Set();
  if (!raw.trim()){ jlCols = []; jlRender(); return; }
  const lines = raw.split('\\n');
  lines.forEach((line, i) => {
    const trimmed = line.trim();
    if (!trimmed) return;
    try {
      const obj = JSON.parse(trimmed);
      jlRows.push({_line: i+1, _data: obj, _err: null});
    } catch(e){
      jlRows.push({_line: i+1, _data: null, _err: e.message});
      jlErrors.push({line: i+1, msg: e.message});
    }
  });
  // Auto-detect columns from first 100 valid rows
  const keys = new Map();
  let scanned = 0;
  for (const r of jlRows){
    if (!r._data || typeof r._data !== 'object' || Array.isArray(r._data)) continue;
    Object.keys(r._data).forEach(k => keys.set(k, (keys.get(k) || 0) + 1));
    scanned++;
    if (scanned >= 100) break;
  }
  jlCols = Array.from(keys.entries()).sort((a,b) => b[1] - a[1]).map(e => e[0]);
  jlPage = 0;
  jlRender();
}
function jlLoadFile(ev){
  const f = ev.target.files[0];
  if (!f) return;
  const reader = new FileReader();
  reader.onload = e => {
    document.getElementById('jl-input').value = e.target.result;
    jlParse();
  };
  reader.readAsText(f);
}
function jlFiltered(){
  const q = document.getElementById('jl-search').value.toLowerCase();
  if (!q) return jlRows;
  return jlRows.filter(r => {
    if (r._err) return r._err.toLowerCase().includes(q);
    try { return JSON.stringify(r._data).toLowerCase().includes(q); }
    catch(_) { return false; }
  });
}
function jlCellRender(val){
  if (val === undefined) return '<span class="jl-null">—</span>';
  if (val === null) return '<span class="jl-null">null</span>';
  if (typeof val === 'object'){
    const s = JSON.stringify(val);
    const short = s.length > 60 ? s.slice(0, 60) + '…' : s;
    return '<span class="jl-cell-obj" title="click row to expand">' + jlEsc(short) + '</span>';
  }
  if (typeof val === 'boolean') return '<span style="color:var(--accent)">' + val + '</span>';
  if (typeof val === 'number') return '<span style="font-variant-numeric:tabular-nums">' + val + '</span>';
  const s = String(val);
  return jlEsc(s.length > 200 ? s.slice(0, 200) + '…' : s);
}
function jlRender(){
  const filtered = jlFiltered();
  const sortCol = jlSort.col;
  if (sortCol){
    filtered.sort((a, b) => {
      const va = a._data ? a._data[sortCol] : null;
      const vb = b._data ? b._data[sortCol] : null;
      if (va === vb) return 0;
      if (va == null) return 1;
      if (vb == null) return -1;
      if (typeof va === 'number' && typeof vb === 'number') return (va - vb) * jlSort.dir;
      return String(va).localeCompare(String(vb)) * jlSort.dir;
    });
  }
  const size = parseInt(document.getElementById('jl-pagesize').value, 10);
  const total = filtered.length;
  const pages = Math.max(1, Math.ceil(total / size));
  if (jlPage >= pages) jlPage = pages - 1;
  if (jlPage < 0) jlPage = 0;
  const start = jlPage * size;
  const slice = filtered.slice(start, start + size);
  // Stats
  let stats = total + ' row' + (total === 1 ? '' : 's');
  if (jlRows.length !== total) stats += ' (of ' + jlRows.length + ')';
  stats += ' · ' + jlCols.length + ' column' + (jlCols.length === 1 ? '' : 's');
  if (jlErrors.length) stats += ' · <span style="color:#ef4444">' + jlErrors.length + ' parse error' + (jlErrors.length === 1 ? '' : 's') + '</span>';
  document.getElementById('jl-stats').innerHTML = stats;
  // Table
  if (jlRows.length === 0){
    document.getElementById('jl-table-wrap').innerHTML = '<div style="padding:1rem;color:var(--text-muted)">No data. Paste JSONL above or upload a file.</div>';
    document.getElementById('jl-pager').innerHTML = '';
    return;
  }
  const headCells = ['<th style="width:60px">#</th>'].concat(
    jlCols.map(c => {
      const arrow = jlSort.col === c ? (jlSort.dir > 0 ? ' ▲' : ' ▼') : '';
      return '<th onclick="jlSortBy(\\'' + c.replace(/'/g, "\\\\'") + '\\')">' + jlEsc(c) + arrow + '</th>';
    })
  ).join('');
  const bodyRows = slice.map((r, idx) => {
    const realIdx = start + idx;
    if (r._err){
      return '<tr class="jl-row-err"><td>' + r._line + '</td><td colspan="' + (jlCols.length || 1) + '">✗ parse error: ' + jlEsc(r._err) + '</td></tr>';
    }
    const cells = jlCols.map(c => '<td>' + jlCellRender(r._data ? r._data[c] : undefined) + '</td>').join('');
    const main = '<tr onclick="jlToggle(' + realIdx + ')" style="cursor:pointer"><td>' + r._line + '</td>' + cells + '</tr>';
    if (jlExpanded.has(realIdx)){
      const detail = '<tr><td colspan="' + (jlCols.length + 1) + '" style="padding:0"><div class="jl-detail">' + jlEsc(JSON.stringify(r._data, null, 2)) + '</div></td></tr>';
      return main + detail;
    }
    return main;
  }).join('');
  document.getElementById('jl-table-wrap').innerHTML = '<table class="jl-table"><thead><tr>' + headCells + '</tr></thead><tbody>' + bodyRows + '</tbody></table>';
  // Pager
  if (pages <= 1){
    document.getElementById('jl-pager').innerHTML = '';
  } else {
    document.getElementById('jl-pager').innerHTML =
      '<button class="secondary" onclick="jlGoto(0)" ' + (jlPage === 0 ? 'disabled' : '') + '>« First</button>' +
      '<button class="secondary" onclick="jlGoto(' + (jlPage - 1) + ')" ' + (jlPage === 0 ? 'disabled' : '') + '>‹ Prev</button>' +
      '<span class="jl-page-num">Page ' + (jlPage + 1) + ' of ' + pages + '</span>' +
      '<button class="secondary" onclick="jlGoto(' + (jlPage + 1) + ')" ' + (jlPage >= pages - 1 ? 'disabled' : '') + '>Next ›</button>' +
      '<button class="secondary" onclick="jlGoto(' + (pages - 1) + ')" ' + (jlPage >= pages - 1 ? 'disabled' : '') + '>Last »</button>';
  }
}
function jlSortBy(col){
  if (jlSort.col === col) jlSort.dir = -jlSort.dir;
  else { jlSort.col = col; jlSort.dir = 1; }
  jlRender();
}
function jlToggle(idx){
  if (jlExpanded.has(idx)) jlExpanded.delete(idx);
  else jlExpanded.add(idx);
  jlRender();
}
function jlGoto(p){ jlPage = p; jlRender(); }
function jlExport(kind){
  const rows = jlFiltered().filter(r => !r._err).map(r => r._data);
  if (rows.length === 0){ alert('Nothing to export.'); return; }
  let blob, ext;
  if (kind === 'json'){
    blob = new Blob([JSON.stringify(rows, null, 2)], {type:'application/json'});
    ext = 'json';
  } else {
    // CSV — use union of jlCols
    const escCsv = v => {
      if (v === null || v === undefined) return '';
      const s = typeof v === 'object' ? JSON.stringify(v) : String(v);
      return /[",\\n\\r]/.test(s) ? '"' + s.replace(/"/g, '""') + '"' : s;
    };
    const lines = [jlCols.map(escCsv).join(',')];
    rows.forEach(r => lines.push(jlCols.map(c => escCsv(r[c])).join(',')));
    blob = new Blob([lines.join('\\n')], {type:'text/csv'});
    ext = 'csv';
  }
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = 'jsonl-export.' + ext;
  a.click();
  setTimeout(() => URL.revokeObjectURL(url), 1000);
}
document.addEventListener('DOMContentLoaded', jlParse);
</script>
""",
    "help": {
        "en": """
<h2>What is this for?</h2>
<p>JSONL (also called NDJSON — newline-delimited JSON) is the de facto format for LLM training data, audit logs, event streams, and batch API outputs. One JSON object per line, no enclosing array. It's append-friendly, parses line by line, and survives partial corruption. Reading it as a human is painful though — you want a table, not a wall of curly braces. This tool gives you that table in your browser. Nothing uploaded; the file gets read with the browser's FileReader and parsed on the spot.</p>

<h3>When to use it</h3>
<ul>
  <li><strong>Inspecting LLM training data.</strong> A fine-tuning dataset is typically <code>{"messages": [...]}</code> per line. Skim the corpus, spot-check class balance, find the one outlier row that's failing your trainer.</li>
  <li><strong>Reading OpenAI / Anthropic batch outputs.</strong> Both providers return batch results as JSONL — one line per request, with status and content. View at a glance which requests succeeded.</li>
  <li><strong>Reviewing audit logs.</strong> Application logs in JSONL are common; this tool surfaces them in a table for ad-hoc analysis without leaving the browser.</li>
  <li><strong>Sanity-checking event streams.</strong> Kafka / Kinesis dumps often land as JSONL. Quick paginated view, no jq required.</li>
  <li><strong>Converting JSONL → CSV.</strong> Use the export button when you want to drop the data into a spreadsheet.</li>
</ul>

<h3>How the parsing works</h3>
<ul>
  <li>Each non-empty line is parsed independently as JSON. Empty lines are ignored.</li>
  <li>If a line fails to parse, it appears as a red row with the parse error — the rest of the file continues. This is intentional: one bad line shouldn't lose you the other thousand.</li>
  <li>Columns are auto-detected from the union of keys in the first 100 valid rows, sorted by how often they appear. Rare keys past row 100 still appear in the data but won't get their own column.</li>
  <li>Nested objects and arrays show as a clipped JSON snippet in their cell. Click the row to see the full pretty-printed JSON.</li>
</ul>

<h3>Common gotchas</h3>
<ul>
  <li><strong>JSON array ≠ JSONL.</strong> If your file looks like <code>[{...}, {...}, {...}]</code> with commas between objects, that's a JSON array, not JSONL. Strip the outer brackets and replace commas with newlines, or just use a JSON viewer.</li>
  <li><strong>Pretty-printed JSON is not JSONL.</strong> If each "object" spans multiple lines, the parser will see broken fragments. JSONL is strictly one object per line.</li>
  <li><strong>Mixed-shape rows are fine but messy.</strong> If half your rows have <code>content</code> and the other half have <code>text</code>, you'll get two columns and lots of blanks. Often this is what you want; sometimes it points to a data-cleanup bug.</li>
  <li><strong>Very large files.</strong> The whole file gets parsed in memory; ~50–100 MB is fine on modern laptops, beyond that things slow down. For multi-gigabyte logs, use a streaming CLI tool.</li>
  <li><strong>Privacy.</strong> Files never leave the page. The FileReader reads bytes into JS memory; no network request is made.</li>
</ul>
""",
        "de": """
<h2>Wozu ist das gut?</h2>
<p>JSONL (auch NDJSON — newline-delimited JSON) ist das De-facto-Format für LLM-Trainingsdaten, Audit-Logs, Event-Streams und Batch-API-Outputs. Ein JSON-Objekt pro Zeile, kein umschließendes Array. Append-freundlich, zeilenweise parsbar und übersteht teilweise Korruption. Es als Mensch zu lesen ist allerdings schmerzhaft — du willst eine Tabelle, kein Mauer aus geschweiften Klammern. Dieses Tool liefert dir diese Tabelle im Browser. Nichts wird hochgeladen; die Datei wird mit FileReader des Browsers gelesen und an Ort und Stelle geparst.</p>

<h3>Wann nützlich</h3>
<ul>
  <li><strong>LLM-Trainingsdaten inspizieren.</strong> Ein Fine-Tuning-Dataset ist typisch <code>{"messages": [...]}</code> pro Zeile. Über das Korpus überfliegen, Klassenbalance prüfen, die eine Ausreißer-Zeile finden, die den Trainer scheitern lässt.</li>
  <li><strong>OpenAI- / Anthropic-Batch-Outputs lesen.</strong> Beide liefern Batch-Ergebnisse als JSONL — eine Zeile pro Request, mit Status und Inhalt. Auf einen Blick sehen, welche Requests durchkamen.</li>
  <li><strong>Audit-Logs reviewen.</strong> Application Logs als JSONL sind verbreitet; dieses Tool zeigt sie als Tabelle für Ad-hoc-Analyse.</li>
  <li><strong>Event-Streams sanity-checken.</strong> Kafka- / Kinesis-Dumps landen oft als JSONL. Schnelle paginierte Ansicht, ohne jq.</li>
  <li><strong>JSONL → CSV konvertieren.</strong> Der Export-Button hilft, die Daten in eine Tabellenkalkulation zu kippen.</li>
</ul>

<h3>Wie das Parsing funktioniert</h3>
<ul>
  <li>Jede nicht-leere Zeile wird unabhängig als JSON geparst. Leere Zeilen werden ignoriert.</li>
  <li>Schlägt eine Zeile fehl, erscheint sie als rote Zeile mit dem Parse-Fehler — der Rest der Datei läuft weiter. Absicht: eine kaputte Zeile soll nicht die anderen tausend kosten.</li>
  <li>Spalten werden aus der Vereinigung der Keys in den ersten 100 gültigen Zeilen erkannt, sortiert nach Häufigkeit. Seltene Keys nach Zeile 100 sind weiter in den Daten, bekommen aber keine eigene Spalte.</li>
  <li>Verschachtelte Objekte und Arrays erscheinen als gekürzter JSON-Schnipsel in der Zelle. Auf die Zeile klicken zeigt die vollständige hübsch gedruckte Version.</li>
</ul>

<h3>Typische Stolperfallen</h3>
<ul>
  <li><strong>JSON-Array ≠ JSONL.</strong> Sieht deine Datei aus wie <code>[{...}, {...}, {...}]</code> mit Kommas, ist das ein JSON-Array, kein JSONL. Klammern raus, Kommas durch Zeilenumbrüche ersetzen — oder einen JSON-Viewer nehmen.</li>
  <li><strong>Pretty-printed JSON ist kein JSONL.</strong> Streckt sich jedes "Objekt" über mehrere Zeilen, sieht der Parser nur Fragmente. JSONL ist strikt ein Objekt pro Zeile.</li>
  <li><strong>Gemischte Zeilen-Formen sind okay, aber unübersichtlich.</strong> Wenn die Hälfte deiner Zeilen <code>content</code> hat und die andere <code>text</code>, gibt's zwei Spalten und viele Leerstellen. Oft gewollt; manchmal Zeichen für einen Daten-Bug.</li>
  <li><strong>Sehr große Dateien.</strong> Alles wird im Speicher geparst; ~50–100 MB sind auf modernen Laptops in Ordnung, darüber wird's träge. Für Multi-GB-Logs lieber ein Streaming-CLI-Tool.</li>
  <li><strong>Privacy.</strong> Dateien verlassen die Seite nie. FileReader liest Bytes ins JS-RAM; kein Netzwerk-Request.</li>
</ul>
""",
        "es": """
<h2>¿Para qué sirve?</h2>
<p>JSONL (también NDJSON — JSON delimitado por línea) es el formato de facto para datos de entrenamiento de LLM, logs de auditoría, streams de eventos y outputs de APIs batch. Un objeto JSON por línea, sin array contenedor. Es amigable para append, se parsea línea a línea y sobrevive a corrupción parcial. Leerlo como humano duele — quieres una tabla, no un muro de llaves. Esta herramienta te da esa tabla en el navegador. Nada se sube; el archivo se lee con FileReader y se parsea en el sitio.</p>

<h3>Cuándo usarla</h3>
<ul>
  <li><strong>Inspeccionar datos de training de LLM.</strong> Un dataset de fine-tuning suele ser <code>{"messages": [...]}</code> por línea. Hojear el corpus, comprobar balance de clases, encontrar la fila outlier que está rompiendo tu trainer.</li>
  <li><strong>Leer outputs batch de OpenAI / Anthropic.</strong> Ambos devuelven resultados batch como JSONL — una línea por request, con status y contenido. Ver de un vistazo qué requests salieron bien.</li>
  <li><strong>Revisar logs de auditoría.</strong> Logs de aplicación en JSONL son comunes; esta herramienta los presenta como tabla para análisis ad-hoc sin salir del navegador.</li>
  <li><strong>Sanity-check de event streams.</strong> Dumps de Kafka / Kinesis suelen llegar como JSONL. Vista paginada rápida, sin jq.</li>
  <li><strong>Convertir JSONL → CSV.</strong> Usa el botón de export cuando quieras meter los datos en una hoja de cálculo.</li>
</ul>

<h3>Cómo funciona el parsing</h3>
<ul>
  <li>Cada línea no vacía se parsea independientemente como JSON. Líneas vacías se ignoran.</li>
  <li>Si una línea falla, aparece como fila roja con el error — el resto del archivo continúa. Intencional: una línea mala no debería perder las otras mil.</li>
  <li>Las columnas se auto-detectan de la unión de keys en las primeras 100 filas válidas, ordenadas por frecuencia. Keys raras pasadas la fila 100 siguen en los datos pero no tendrán columna propia.</li>
  <li>Objetos y arrays anidados se muestran como snippet JSON recortado en su celda. Haz click en la fila para ver el JSON pretty-printed completo.</li>
</ul>

<h3>Errores comunes</h3>
<ul>
  <li><strong>Array JSON ≠ JSONL.</strong> Si tu archivo se ve como <code>[{...}, {...}, {...}]</code> con comas, eso es un array JSON, no JSONL. Quita los corchetes externos y cambia comas por saltos de línea — o usa un visor JSON.</li>
  <li><strong>JSON pretty-printed no es JSONL.</strong> Si cada "objeto" abarca varias líneas, el parser verá fragmentos rotos. JSONL es estrictamente un objeto por línea.</li>
  <li><strong>Filas con shape mixto van bien pero quedan desordenadas.</strong> Si la mitad de tus filas tienen <code>content</code> y la otra mitad <code>text</code>, vas a tener dos columnas y muchos huecos. A menudo eso es lo que quieres; a veces señala un bug de limpieza de datos.</li>
  <li><strong>Archivos muy grandes.</strong> Todo se parsea en memoria; ~50–100 MB va bien en laptops modernos, más allá se ralentiza. Para logs de varios GB, usa una herramienta CLI de streaming.</li>
  <li><strong>Privacidad.</strong> Los archivos nunca salen de la página. FileReader lee bytes a memoria JS; no se hace request de red.</li>
</ul>
""",
        "fr": """
<h2>À quoi ça sert ?</h2>
<p>JSONL (aussi NDJSON — JSON délimité par lignes) est le format de facto pour les données d'entraînement LLM, les logs d'audit, les flux d'événements et les sorties d'API batch. Un objet JSON par ligne, pas de tableau englobant. Pratique pour l'append, se parse ligne par ligne, survit à la corruption partielle. Le lire en tant qu'humain est pénible — vous voulez un tableau, pas un mur d'accolades. Cet outil vous donne ce tableau dans le navigateur. Rien n'est uploadé ; le fichier est lu avec FileReader et parsé sur place.</p>

<h3>Quand l'utiliser</h3>
<ul>
  <li><strong>Inspecter des données de training LLM.</strong> Un dataset de fine-tuning, c'est typiquement <code>{"messages": [...]}</code> par ligne. Survoler le corpus, vérifier l'équilibre des classes, trouver la ligne aberrante qui fait planter votre entraîneur.</li>
  <li><strong>Lire les sorties batch d'OpenAI / Anthropic.</strong> Les deux retournent les résultats batch en JSONL — une ligne par requête, avec statut et contenu. Voir d'un coup quelles requêtes ont réussi.</li>
  <li><strong>Reviewer des logs d'audit.</strong> Les logs applicatifs en JSONL sont courants ; cet outil les présente en tableau pour analyse ad hoc sans quitter le navigateur.</li>
  <li><strong>Sanity-check de flux d'événements.</strong> Les dumps Kafka / Kinesis arrivent souvent en JSONL. Vue paginée rapide, sans jq.</li>
  <li><strong>Convertir JSONL → CSV.</strong> Utilisez le bouton d'export quand vous voulez déposer les données dans un tableur.</li>
</ul>

<h3>Comment marche le parsing</h3>
<ul>
  <li>Chaque ligne non vide est parsée indépendamment en JSON. Les lignes vides sont ignorées.</li>
  <li>Si une ligne échoue, elle apparaît en rouge avec l'erreur — le reste du fichier continue. Intentionnel : une mauvaise ligne ne doit pas vous coûter les autres milliers.</li>
  <li>Les colonnes sont auto-détectées à partir de l'union des clés dans les 100 premières lignes valides, triées par fréquence. Les clés rares au-delà de la ligne 100 restent dans les données mais n'auront pas leur propre colonne.</li>
  <li>Objets et tableaux imbriqués s'affichent en snippet JSON tronqué dans leur cellule. Cliquez la ligne pour voir le JSON pretty-printed complet.</li>
</ul>

<h3>Pièges courants</h3>
<ul>
  <li><strong>Tableau JSON ≠ JSONL.</strong> Si votre fichier ressemble à <code>[{...}, {...}, {...}]</code> avec des virgules, c'est un tableau JSON, pas du JSONL. Retirez les crochets externes et remplacez les virgules par des sauts de ligne — ou utilisez un visualiseur JSON.</li>
  <li><strong>JSON pretty-printed n'est pas du JSONL.</strong> Si chaque « objet » s'étale sur plusieurs lignes, le parser ne voit que des fragments cassés. JSONL = strictement un objet par ligne.</li>
  <li><strong>Lignes de formes mélangées : ça marche mais c'est moche.</strong> Si la moitié de vos lignes ont <code>content</code> et l'autre <code>text</code>, vous obtenez deux colonnes et plein de cases vides. Souvent c'est ce que vous voulez ; parfois c'est le signe d'un bug de nettoyage de données.</li>
  <li><strong>Très gros fichiers.</strong> Tout est parsé en mémoire ; ~50–100 Mo passe sur un laptop moderne, au-delà ça ralentit. Pour des logs de plusieurs Go, prenez un outil CLI en streaming.</li>
  <li><strong>Confidentialité.</strong> Les fichiers ne quittent jamais la page. FileReader lit les octets en mémoire JS ; aucune requête réseau.</li>
</ul>
""",
        "it": """
<h2>A cosa serve?</h2>
<p>JSONL (anche NDJSON — JSON delimitato da newline) è il formato de facto per dati di training LLM, audit log, stream di eventi e output di API batch. Un oggetto JSON per riga, senza array contenitore. Append-friendly, si parsa riga per riga, sopravvive a corruzione parziale. Leggerlo da umano è doloroso però — vuoi una tabella, non un muro di parentesi. Questo strumento ti dà quella tabella nel browser. Nulla viene caricato; il file viene letto col FileReader del browser e parsato sul posto.</p>

<h3>Quando usarlo</h3>
<ul>
  <li><strong>Ispezionare dati di training LLM.</strong> Un dataset di fine-tuning è tipicamente <code>{"messages": [...]}</code> per riga. Sfogliare il corpus, controllare il bilancio delle classi, trovare la riga outlier che sta facendo fallire il tuo trainer.</li>
  <li><strong>Leggere output batch di OpenAI / Anthropic.</strong> Entrambi tornano risultati batch come JSONL — una riga per richiesta, con status e contenuto. Vedere a colpo d'occhio quali richieste sono andate.</li>
  <li><strong>Revisionare audit log.</strong> Log applicativi in JSONL sono comuni; questo strumento li mostra come tabella per analisi ad hoc senza lasciare il browser.</li>
  <li><strong>Sanity-check di stream di eventi.</strong> Dump Kafka / Kinesis arrivano spesso come JSONL. Vista paginata veloce, senza jq.</li>
  <li><strong>Convertire JSONL → CSV.</strong> Usa il pulsante export quando vuoi versare i dati in un foglio di calcolo.</li>
</ul>

<h3>Come funziona il parsing</h3>
<ul>
  <li>Ogni riga non vuota viene parsata indipendentemente come JSON. Le righe vuote sono ignorate.</li>
  <li>Se una riga fallisce, appare come riga rossa con l'errore — il resto del file continua. Intenzionale: una riga rotta non deve costarti le altre mille.</li>
  <li>Le colonne sono auto-rilevate dall'unione delle chiavi nelle prime 100 righe valide, ordinate per frequenza. Chiavi rare oltre la riga 100 restano nei dati ma non avranno colonna propria.</li>
  <li>Oggetti e array annidati appaiono come snippet JSON troncato nella cella. Clicca la riga per vedere il JSON pretty-printed completo.</li>
</ul>

<h3>Trappole comuni</h3>
<ul>
  <li><strong>Array JSON ≠ JSONL.</strong> Se il tuo file sembra <code>[{...}, {...}, {...}]</code> con virgole, è un array JSON, non JSONL. Togli le parentesi esterne e sostituisci le virgole con newline — o usa un visualizzatore JSON.</li>
  <li><strong>JSON pretty-printed non è JSONL.</strong> Se ogni "oggetto" si stende su più righe, il parser vedrà frammenti rotti. JSONL è strettamente un oggetto per riga.</li>
  <li><strong>Righe a forma mista vanno bene ma sporcano.</strong> Se metà delle tue righe hanno <code>content</code> e l'altra metà <code>text</code>, avrai due colonne e tanti vuoti. Spesso è quello che vuoi; a volte segnala un bug di pulizia dati.</li>
  <li><strong>File molto grandi.</strong> Tutto viene parsato in memoria; ~50–100 MB va bene su laptop moderni, oltre rallenta. Per log multi-gigabyte, usa uno strumento CLI in streaming.</li>
  <li><strong>Privacy.</strong> I file non lasciano mai la pagina. FileReader legge byte in memoria JS; nessuna richiesta di rete.</li>
</ul>
""",
        "pt": """
<h2>Para que serve?</h2>
<p>JSONL (também NDJSON — JSON delimitado por linha) é o formato de facto pra dados de treinamento de LLM, audit logs, streams de eventos e outputs de APIs batch. Um objeto JSON por linha, sem array englobante. É append-friendly, parseia linha a linha e sobrevive a corrupção parcial. Mas ler como humano dói — você quer uma tabela, não um muro de chaves. Esta ferramenta te dá essa tabela no browser. Nada é enviado; o arquivo é lido com o FileReader do browser e parseado no lugar.</p>

<h3>Quando usar</h3>
<ul>
  <li><strong>Inspecionar dados de training de LLM.</strong> Um dataset de fine-tuning normalmente é <code>{"messages": [...]}</code> por linha. Olhar o corpus, conferir balanço de classes, achar aquela linha outlier que tá fazendo o trainer falhar.</li>
  <li><strong>Ler outputs batch da OpenAI / Anthropic.</strong> Ambos voltam resultados batch como JSONL — uma linha por request, com status e conteúdo. Ver de relance quais requests deram certo.</li>
  <li><strong>Revisar audit logs.</strong> Logs de aplicação em JSONL são comuns; esta ferramenta mostra eles como tabela pra análise ad hoc sem sair do browser.</li>
  <li><strong>Sanity-check de event streams.</strong> Dumps de Kafka / Kinesis costumam vir como JSONL. Vista paginada rápida, sem jq.</li>
  <li><strong>Converter JSONL → CSV.</strong> Use o botão de export quando quiser jogar os dados numa planilha.</li>
</ul>

<h3>Como funciona o parsing</h3>
<ul>
  <li>Cada linha não vazia é parseada independentemente como JSON. Linhas vazias são ignoradas.</li>
  <li>Se uma linha falha, aparece como linha vermelha com o erro — o resto do arquivo segue. Intencional: uma linha ruim não deve te custar as outras mil.</li>
  <li>Colunas são auto-detectadas pela união de keys nas primeiras 100 linhas válidas, ordenadas por frequência. Keys raras depois da linha 100 ficam nos dados mas não ganham coluna própria.</li>
  <li>Objetos e arrays aninhados aparecem como snippet JSON cortado na célula. Clica na linha pra ver o JSON pretty-printed completo.</li>
</ul>

<h3>Pegadinhas comuns</h3>
<ul>
  <li><strong>Array JSON ≠ JSONL.</strong> Se seu arquivo parece <code>[{...}, {...}, {...}]</code> com vírgulas, isso é array JSON, não JSONL. Tira os colchetes de fora e troca vírgulas por quebras de linha — ou usa um visualizador JSON.</li>
  <li><strong>JSON pretty-printed não é JSONL.</strong> Se cada "objeto" se estende por várias linhas, o parser vai ver fragmentos quebrados. JSONL é estritamente um objeto por linha.</li>
  <li><strong>Linhas com shape misto funcionam mas ficam bagunçadas.</strong> Se metade das linhas tem <code>content</code> e a outra metade <code>text</code>, vai dar duas colunas e muitos vazios. Geralmente é o que você quer; às vezes indica bug de limpeza de dados.</li>
  <li><strong>Arquivos muito grandes.</strong> Tudo é parseado em memória; ~50–100 MB vai bem em laptops modernos, acima disso fica lento. Pra logs de vários GB, usa uma ferramenta CLI de streaming.</li>
  <li><strong>Privacidade.</strong> Arquivos nunca saem da página. FileReader lê bytes na memória JS; nenhum request de rede.</li>
</ul>
""",
        "pl": """
<h2>Do czego to służy?</h2>
<p>JSONL (zwane też NDJSON — JSON rozdzielony nowymi liniami) to de facto format danych treningowych LLM, audit logów, strumieni zdarzeń i wyjść batchowych API. Jeden obiekt JSON na linię, bez obejmującej tablicy. Przyjazny dla append, parsuje się linia po linii, przeżywa częściową korupcję. Czytanie tego jako człowiek boli — chcesz tabeli, nie ściany klamr. To narzędzie daje ci taką tabelę w przeglądarce. Nic nie jest wysyłane; plik czytany jest FileReaderem przeglądarki i parsowany na miejscu.</p>

<h3>Kiedy tego użyć</h3>
<ul>
  <li><strong>Przegląd danych treningowych LLM.</strong> Dataset do fine-tuningu to zwykle <code>{"messages": [...]}</code> na linię. Przekartkowanie korpusu, sprawdzenie balansu klas, znalezienie jednego outliera, który wywala twojemu trainerowi parser.</li>
  <li><strong>Czytanie wyjść batchowych OpenAI / Anthropic.</strong> Obaj zwracają wyniki batch jako JSONL — jedna linia na request, ze statusem i treścią. Zobaczenie od ręki, które requesty się powiodły.</li>
  <li><strong>Review audit logów.</strong> Logi aplikacyjne w JSONL są popularne; to narzędzie pokazuje je jako tabelę do ad-hocowej analizy bez wychodzenia z przeglądarki.</li>
  <li><strong>Sanity-check strumieni zdarzeń.</strong> Dumpy z Kafki / Kinesis często lądują jako JSONL. Szybki paginowany widok, bez jq.</li>
  <li><strong>Konwersja JSONL → CSV.</strong> Użyj przycisku eksportu, gdy chcesz wrzucić dane do arkusza.</li>
</ul>

<h3>Jak działa parsowanie</h3>
<ul>
  <li>Każda niepusta linia jest parsowana niezależnie jako JSON. Puste linie są ignorowane.</li>
  <li>Jeśli linia padnie, pojawia się jako czerwony wiersz z błędem parsera — reszta pliku leci dalej. Świadomie: jedna zła linia nie powinna kosztować cię tysiąca pozostałych.</li>
  <li>Kolumny są auto-wykrywane z unii kluczy w pierwszych 100 poprawnych wierszach, sortowane po częstości. Rzadkie klucze za wierszem 100 zostają w danych, ale nie dostaną własnej kolumny.</li>
  <li>Zagnieżdżone obiekty i tablice pokazują się jako skrócony snippet JSON w komórce. Kliknij wiersz, żeby zobaczyć pełny pretty-printed JSON.</li>
</ul>

<h3>Częste pułapki</h3>
<ul>
  <li><strong>Tablica JSON ≠ JSONL.</strong> Jeśli twój plik wygląda jak <code>[{...}, {...}, {...}]</code> z przecinkami między obiektami, to tablica JSON, nie JSONL. Zdejmij zewnętrzne nawiasy i zamień przecinki na nowe linie — albo użyj wizualizatora JSON.</li>
  <li><strong>Pretty-printed JSON to nie JSONL.</strong> Jeśli każdy „obiekt" rozciąga się na wiele linii, parser zobaczy popsute fragmenty. JSONL to ściśle jeden obiekt na linię.</li>
  <li><strong>Linie o mieszanych kształtach są okay, ale chaotyczne.</strong> Jeśli połowa linii ma <code>content</code>, a druga <code>text</code>, dostaniesz dwie kolumny i mnóstwo pustych. Często tego chcesz; czasem to znak buga w czyszczeniu danych.</li>
  <li><strong>Bardzo duże pliki.</strong> Wszystko parsowane w pamięci; ~50–100 MB to spokojnie na nowoczesnym laptopie, dalej zwalnia. Do wielogigabajtowych logów weź streamingowe narzędzie CLI.</li>
  <li><strong>Prywatność.</strong> Pliki nigdy nie opuszczają strony. FileReader czyta bajty do pamięci JS; żadnego requestu sieciowego.</li>
</ul>
""",
        "ja": """
<h2>用途</h2>
<p>JSONL（NDJSON とも呼ばれます — 改行区切り JSON）は、LLM の学習データ、監査ログ、イベントストリーム、バッチ API 出力の事実上の標準フォーマットです。1 行に 1 オブジェクト、外側の配列はありません。追記しやすく、行単位で解析でき、一部破損にも強い。ただし人が読むのは辛い — 中括弧の壁ではなくテーブルが欲しいわけです。本ツールはそのテーブルをブラウザ内で提供します。アップロードはなく、FileReader でその場で読み込みます。</p>

<h3>使うべきタイミング</h3>
<ul>
  <li><strong>LLM の学習データ点検。</strong> ファインチューニング用のデータセットは典型的には 1 行に <code>{"messages": [...]}</code>。コーパスを流し読みし、クラスバランスを確認し、トレーナーを落としている 1 件の外れ値を発見します。</li>
  <li><strong>OpenAI / Anthropic のバッチ出力の閲覧。</strong> 両社ともバッチ結果は JSONL で返ります（1 リクエスト 1 行、ステータスと内容付き）。どのリクエストが通ったか一目で。</li>
  <li><strong>監査ログのレビュー。</strong> JSONL のアプリログは一般的。ブラウザを離れずアドホックに分析できます。</li>
  <li><strong>イベントストリームの確認。</strong> Kafka / Kinesis のダンプはしばしば JSONL。jq なしで素早く分割表示。</li>
  <li><strong>JSONL → CSV 変換。</strong> 表計算ソフトに入れたいときは Export ボタンを使います。</li>
</ul>

<h3>解析の挙動</h3>
<ul>
  <li>空でない各行を独立に JSON としてパースします。空行は無視されます。</li>
  <li>パース失敗の行は赤い行とエラーメッセージで表示され、残りの行は処理を続けます。意図的な仕様 — 一行壊れただけで残り千行を諦めるべきではないため。</li>
  <li>カラムは先頭 100 件の有効行に出現するキーの和集合を、登場頻度順で自動検出します。100 行以降に初登場する希少キーはデータ内には残りますが、独自カラムは作られません。</li>
  <li>ネストされたオブジェクトや配列はセル内で JSON スニペット（省略付き）として表示。行をクリックすると整形済み JSON が展開されます。</li>
</ul>

<h3>よくある注意点</h3>
<ul>
  <li><strong>JSON 配列 ≠ JSONL。</strong> ファイルが <code>[{...}, {...}, {...}]</code> のようにカンマ区切りで囲まれているなら、それは JSON 配列であって JSONL ではありません。外側の括弧を外しカンマを改行に置き換えるか、JSON ビューアを使ってください。</li>
  <li><strong>整形済み JSON は JSONL ではありません。</strong> 1 つの「オブジェクト」が複数行にまたがると、パーサは断片を見ることになります。JSONL は厳密に 1 行 1 オブジェクトです。</li>
  <li><strong>形がバラバラな行も受け付けるが汚くなります。</strong> 半分が <code>content</code>、半分が <code>text</code> なら、2 カラムと多数の空白セルになります。意図通りの場合も多いですが、データクレンジングのバグのサインのこともあります。</li>
  <li><strong>非常に大きなファイル。</strong> 全体をメモリ上で解析します。50〜100 MB 程度なら最近のラップトップで問題なく動きますが、それを超えると遅くなります。数 GB のログにはストリーミング CLI ツールを。</li>
  <li><strong>プライバシー。</strong> ファイルはページから出ません。FileReader が JS メモリへバイトを読み込むだけで、ネットワーク通信は発生しません。</li>
</ul>
""",
        "nl": """
<h2>Waarvoor is dit?</h2>
<p>JSONL (ook NDJSON — newline-delimited JSON) is het de facto formaat voor LLM-trainingsdata, audit logs, event streams en batch API-outputs. Eén JSON-object per regel, geen omhullende array. Append-vriendelijk, regel-voor-regel te parsen, overleeft gedeeltelijke corruptie. Het als mens lezen is alleen pijnlijk — je wilt een tabel, geen muur van krulhaken. Deze tool geeft je die tabel in je browser. Niks geüpload; het bestand wordt gelezen met de FileReader van de browser en ter plekke geparseerd.</p>

<h3>Wanneer gebruiken</h3>
<ul>
  <li><strong>LLM-trainingsdata inspecteren.</strong> Een fine-tuning dataset is typisch <code>{"messages": [...]}</code> per regel. Door het corpus skimmen, klassen-balans checken, die ene outlier-rij vinden die je trainer laat falen.</li>
  <li><strong>OpenAI / Anthropic batch-outputs lezen.</strong> Beide leveren batchresultaten als JSONL — één regel per request, met status en content. In één oogopslag zien welke requests slaagden.</li>
  <li><strong>Audit logs reviewen.</strong> Applicatielogs in JSONL zijn gangbaar; deze tool toont ze als tabel voor ad-hoc analyse zonder de browser te verlaten.</li>
  <li><strong>Sanity-check van event streams.</strong> Kafka / Kinesis-dumps komen vaak als JSONL. Snelle gepagineerde view, zonder jq.</li>
  <li><strong>JSONL → CSV converteren.</strong> Gebruik de export-knop als je de data in een spreadsheet wil droppen.</li>
</ul>

<h3>Hoe het parsen werkt</h3>
<ul>
  <li>Elke niet-lege regel wordt onafhankelijk als JSON geparseerd. Lege regels worden genegeerd.</li>
  <li>Als een regel faalt, verschijnt 'ie als rode rij met de parse-error — de rest van het bestand gaat door. Bewust: één slechte regel mag je de andere duizend niet kosten.</li>
  <li>Kolommen worden auto-gedetecteerd uit de unie van keys in de eerste 100 geldige rijen, gesorteerd op frequentie. Zeldzame keys voorbij rij 100 blijven in de data, krijgen geen eigen kolom.</li>
  <li>Geneste objecten en arrays tonen als ingekorte JSON-snippet in hun cel. Klik op de rij voor de volledige pretty-printed JSON.</li>
</ul>

<h3>Veelvoorkomende valkuilen</h3>
<ul>
  <li><strong>JSON-array ≠ JSONL.</strong> Als je bestand eruitziet als <code>[{...}, {...}, {...}]</code> met komma's tussen objecten, is dat een JSON-array, geen JSONL. Strip de buitenste brackets en vervang komma's door newlines — of pak een JSON-viewer.</li>
  <li><strong>Pretty-printed JSON is geen JSONL.</strong> Als elk "object" meerdere regels beslaat, ziet de parser kapotte fragmenten. JSONL is strikt één object per regel.</li>
  <li><strong>Rijen met gemixte vorm gaan goed maar rommelig.</strong> Heeft de helft van je rijen <code>content</code> en de andere helft <code>text</code>, dan krijg je twee kolommen en veel lege cellen. Vaak is dat wat je wilt; soms wijst het op een data-cleanup-bug.</li>
  <li><strong>Heel grote bestanden.</strong> Alles wordt in geheugen geparseerd; ~50–100 MB gaat prima op moderne laptops, daarboven wordt het traag. Voor logs van meerdere GB: gebruik een streaming CLI-tool.</li>
  <li><strong>Privacy.</strong> Bestanden verlaten de pagina nooit. FileReader leest bytes naar JS-geheugen; geen netwerk-request.</li>
</ul>
""",
        "tr": """
<h2>Bu ne işe yarar?</h2>
<p>JSONL (NDJSON da denir — yeni satırlarla ayrılmış JSON), LLM eğitim verisi, audit log'ları, event stream'leri ve batch API çıktıları için fiili formattır. Satır başına bir JSON nesnesi, dış sarmalayıcı array yok. Append-dostu, satır satır parse edilir, kısmi bozulmaya dayanır. Ama insan olarak okumak acı verici — süslü parantez duvarı yerine tablo istiyorsun. Bu araç o tabloyu tarayıcında verir. Hiçbir şey yüklenmez; dosya tarayıcının FileReader'ı ile okunur ve yerinde parse edilir.</p>

<h3>Ne zaman kullanılır</h3>
<ul>
  <li><strong>LLM eğitim verisini inceleme.</strong> Bir fine-tuning dataset'i tipik olarak satır başına <code>{"messages": [...]}</code>'dur. Külliyatı tarayıp sınıf dengesini kontrol et, trainer'ını çökerten o tek aykırı satırı bul.</li>
  <li><strong>OpenAI / Anthropic batch çıktılarını okuma.</strong> İkisi de batch sonuçlarını JSONL olarak döner — istek başına bir satır, statü ve içerik. Hangi isteklerin başardığını bir bakışta gör.</li>
  <li><strong>Audit log'ları gözden geçirme.</strong> JSONL'daki uygulama log'ları yaygındır; bu araç onları tarayıcıdan çıkmadan, ad hoc analiz için tabloda gösterir.</li>
  <li><strong>Event stream sanity check.</strong> Kafka / Kinesis dump'ları sık sık JSONL gelir. Hızlı sayfalı görüntü, jq olmadan.</li>
  <li><strong>JSONL → CSV dönüşümü.</strong> Veriyi bir hesap tablosuna atmak istediğinde export düğmesini kullan.</li>
</ul>

<h3>Parsing nasıl çalışır</h3>
<ul>
  <li>Boş olmayan her satır JSON olarak bağımsız parse edilir. Boş satırlar yok sayılır.</li>
  <li>Bir satır başarısız olursa parse hatasıyla kırmızı satır olarak görünür — dosyanın geri kalanı devam eder. Bilinçli: bir bozuk satır seni diğer binini kaybetmemeli.</li>
  <li>Sütunlar, ilk 100 geçerli satırdaki anahtarların birleşiminden, görülme sıklığına göre sıralanarak otomatik tespit edilir. 100. satırdan sonraki nadir anahtarlar veride kalır ama kendi sütununu almaz.</li>
  <li>İç içe nesneler ve diziler hücrelerinde kırpılmış JSON parçası olarak gösterilir. Satıra tıkla, tam pretty-printed JSON'u gör.</li>
</ul>

<h3>Yaygın tuzaklar</h3>
<ul>
  <li><strong>JSON dizisi ≠ JSONL.</strong> Dosyan <code>[{...}, {...}, {...}]</code> gibi virgüllü görünüyorsa, bu bir JSON dizisidir, JSONL değil. Dış parantezleri kaldır ve virgülleri yeni satırla değiştir — veya bir JSON görüntüleyici kullan.</li>
  <li><strong>Pretty-printed JSON, JSONL değildir.</strong> Her "nesne" birden fazla satıra yayılırsa, parser kırık parçalar görür. JSONL kesinlikle satır başına bir nesnedir.</li>
  <li><strong>Karışık şekilli satırlar olur ama dağınık görünür.</strong> Satırlarının yarısı <code>content</code>, diğer yarısı <code>text</code> içeriyorsa, iki sütun ve çok boşluk alacaksın. Çoğu zaman istediğin budur; bazen veri temizleme bug'ının işaretidir.</li>
  <li><strong>Çok büyük dosyalar.</strong> Her şey bellekte parse edilir; modern dizüstülerde ~50–100 MB sorun değil, ötesinde yavaşlar. Çoklu GB log'lar için bir streaming CLI aracı kullan.</li>
  <li><strong>Gizlilik.</strong> Dosyalar sayfayı asla terk etmez. FileReader byte'ları JS belleğine okur; ağ isteği yapılmaz.</li>
</ul>
""",
        "id": """
<h2>Untuk apa ini?</h2>
<p>JSONL (juga NDJSON — JSON yang dipisah newline) adalah format de facto untuk data training LLM, audit log, event stream, dan output API batch. Satu objek JSON per baris, tanpa array pembungkus. Mudah di-append, di-parse baris demi baris, dan tahan korupsi parsial. Tapi membacanya sebagai manusia menyakitkan — kamu mau tabel, bukan tembok kurawal. Tool ini memberi tabel itu di browser-mu. Tidak ada yang di-upload; file dibaca dengan FileReader browser dan di-parse di tempat.</p>

<h3>Kapan digunakan</h3>
<ul>
  <li><strong>Memeriksa data training LLM.</strong> Dataset fine-tuning biasanya <code>{"messages": [...]}</code> per baris. Menyisir korpus, mengecek balance kelas, menemukan satu baris outlier yang membuat trainer-mu gagal.</li>
  <li><strong>Membaca output batch OpenAI / Anthropic.</strong> Keduanya mengembalikan hasil batch sebagai JSONL — satu baris per request, dengan status dan konten. Lihat sekilas request mana yang berhasil.</li>
  <li><strong>Review audit log.</strong> Log aplikasi dalam JSONL umum; tool ini menampilkannya sebagai tabel untuk analisis ad hoc tanpa keluar dari browser.</li>
  <li><strong>Sanity-check event stream.</strong> Dump Kafka / Kinesis sering datang sebagai JSONL. Tampilan paginated cepat, tanpa jq.</li>
  <li><strong>Konversi JSONL → CSV.</strong> Pakai tombol export saat ingin masukin data ke spreadsheet.</li>
</ul>

<h3>Cara parsing bekerja</h3>
<ul>
  <li>Setiap baris non-kosong di-parse secara independen sebagai JSON. Baris kosong diabaikan.</li>
  <li>Jika sebuah baris gagal, muncul sebagai baris merah dengan error parser — sisa file lanjut. Disengaja: satu baris rusak tidak boleh membuatmu kehilangan seribu lainnya.</li>
  <li>Kolom dideteksi otomatis dari union key di 100 baris valid pertama, diurutkan berdasarkan frekuensi. Key langka setelah baris 100 tetap di data tapi tidak mendapat kolom sendiri.</li>
  <li>Objek dan array bersarang ditampilkan sebagai snippet JSON terpotong di sel mereka. Klik baris untuk melihat JSON pretty-printed lengkap.</li>
</ul>

<h3>Kesalahan umum</h3>
<ul>
  <li><strong>Array JSON ≠ JSONL.</strong> Kalau file-mu terlihat seperti <code>[{...}, {...}, {...}]</code> dengan koma antar objek, itu array JSON, bukan JSONL. Buang bracket luarnya dan ganti koma dengan newline — atau pakai viewer JSON.</li>
  <li><strong>JSON pretty-printed bukan JSONL.</strong> Kalau setiap "objek" memakan beberapa baris, parser akan melihat fragmen rusak. JSONL ketat satu objek per baris.</li>
  <li><strong>Baris dengan shape campur boleh tapi berantakan.</strong> Kalau separuh baris punya <code>content</code> dan separuh lainnya punya <code>text</code>, kamu akan dapat dua kolom dan banyak kosong. Sering memang itu yang kamu mau; kadang menandakan bug cleanup data.</li>
  <li><strong>File sangat besar.</strong> Semuanya di-parse di memori; ~50–100 MB oke di laptop modern, di atasnya melambat. Untuk log multi-gigabyte, pakai tool CLI streaming.</li>
  <li><strong>Privasi.</strong> File tidak pernah meninggalkan halaman. FileReader membaca byte ke memori JS; tidak ada request jaringan.</li>
</ul>
""",
        "vi": """
<h2>Công cụ này để làm gì?</h2>
<p>JSONL (còn gọi là NDJSON — JSON phân tách bằng dòng mới) là định dạng tiêu chuẩn cho dữ liệu training LLM, audit log, event stream, và đầu ra batch API. Một đối tượng JSON mỗi dòng, không có mảng bao quanh. Thân thiện với append, parse từng dòng một, sống sót qua tham nhũng cục bộ. Nhưng đọc như một con người thì đau đớn — bạn muốn một bảng, không phải bức tường ngoặc nhọn. Công cụ này cho bạn bảng đó trong trình duyệt. Không có gì được upload; file được đọc bằng FileReader của trình duyệt và parse tại chỗ.</p>

<h3>Khi nào nên dùng</h3>
<ul>
  <li><strong>Kiểm tra dữ liệu training LLM.</strong> Dataset fine-tuning thường là <code>{"messages": [...]}</code> mỗi dòng. Lướt qua corpus, kiểm tra cân bằng lớp, tìm dòng outlier khiến trainer của bạn fail.</li>
  <li><strong>Đọc đầu ra batch của OpenAI / Anthropic.</strong> Cả hai trả về kết quả batch dưới dạng JSONL — một dòng cho mỗi request, kèm status và content. Xem ngay request nào đã thành công.</li>
  <li><strong>Review audit log.</strong> Log ứng dụng dạng JSONL phổ biến; công cụ này hiển thị chúng dưới dạng bảng để phân tích ad hoc mà không rời trình duyệt.</li>
  <li><strong>Sanity-check event stream.</strong> Dump Kafka / Kinesis thường ở dạng JSONL. Xem phân trang nhanh, không cần jq.</li>
  <li><strong>Chuyển JSONL → CSV.</strong> Dùng nút export khi muốn đổ dữ liệu vào bảng tính.</li>
</ul>

<h3>Cách parsing hoạt động</h3>
<ul>
  <li>Mỗi dòng không trống được parse độc lập như JSON. Dòng trống bị bỏ qua.</li>
  <li>Nếu một dòng fail, nó xuất hiện dưới dạng dòng đỏ với lỗi parser — phần còn lại tiếp tục. Cố ý: một dòng hỏng không nên khiến bạn mất một nghìn dòng khác.</li>
  <li>Cột được tự động phát hiện từ hợp của các key trong 100 dòng hợp lệ đầu tiên, sắp xếp theo tần suất. Key hiếm sau dòng 100 vẫn còn trong dữ liệu nhưng không có cột riêng.</li>
  <li>Object và mảng lồng nhau hiển thị dưới dạng snippet JSON bị cắt trong ô. Click vào dòng để xem JSON đầy đủ đã làm đẹp.</li>
</ul>

<h3>Lưu ý thường gặp</h3>
<ul>
  <li><strong>Mảng JSON ≠ JSONL.</strong> Nếu file trông như <code>[{...}, {...}, {...}]</code> với dấu phẩy giữa các đối tượng, đó là mảng JSON, không phải JSONL. Bỏ ngoặc ngoài và thay dấu phẩy bằng dòng mới — hoặc dùng viewer JSON.</li>
  <li><strong>JSON pretty-printed không phải JSONL.</strong> Nếu mỗi "đối tượng" trải dài nhiều dòng, parser sẽ thấy mảnh vỡ. JSONL nghiêm ngặt một đối tượng mỗi dòng.</li>
  <li><strong>Dòng có shape hỗn hợp ổn nhưng lộn xộn.</strong> Nếu một nửa dòng có <code>content</code> và nửa kia có <code>text</code>, bạn sẽ có hai cột và nhiều ô trống. Thường là điều bạn muốn; đôi khi chỉ ra bug cleanup dữ liệu.</li>
  <li><strong>File rất lớn.</strong> Tất cả được parse trong memory; ~50–100 MB ổn trên laptop hiện đại, vượt qua thì chậm. Cho log nhiều GB, dùng công cụ CLI streaming.</li>
  <li><strong>Riêng tư.</strong> File không bao giờ rời trang. FileReader đọc byte vào memory JS; không có request mạng.</li>
</ul>
""",
        "hi": """
<h2>यह किसके लिए है?</h2>
<p>JSONL (जिसे NDJSON भी कहते हैं — newline-delimited JSON) LLM training data, audit logs, event streams, और batch API outputs के लिए de facto format है। प्रति line एक JSON object, कोई enclosing array नहीं। Append-friendly, line-by-line parse होता है, और partial corruption को झेल लेता है। पर इसे human के तौर पर पढ़ना दर्दनाक है — आप table चाहते हैं, curly braces की दीवार नहीं। यह tool वह table आपके browser में देता है। कुछ upload नहीं होता; file को browser के FileReader से पढ़ा और तुरंत parse किया जाता है।</p>

<h3>कब इस्तेमाल करें</h3>
<ul>
  <li><strong>LLM training data inspect करने के लिए।</strong> Fine-tuning dataset आम तौर पर per line <code>{"messages": [...]}</code> होता है। Corpus skim करें, class balance देखें, वह एक outlier row ढूँढें जो आपके trainer को fail कर रही है।</li>
  <li><strong>OpenAI / Anthropic batch outputs पढ़ने के लिए।</strong> दोनों provider batch results JSONL में लौटाते हैं — per request एक line, status और content सहित। एक नज़र में देखें कौन से requests सफल हुए।</li>
  <li><strong>Audit logs review करने के लिए।</strong> JSONL में application logs आम हैं; यह tool उन्हें ad-hoc analysis के लिए table में दिखाता है, browser छोड़े बिना।</li>
  <li><strong>Event streams की sanity-check।</strong> Kafka / Kinesis dumps अक्सर JSONL में आते हैं। Quick paginated view, jq की ज़रूरत नहीं।</li>
  <li><strong>JSONL → CSV में convert करना।</strong> Export button का use करें जब डेटा को spreadsheet में डालना हो।</li>
</ul>

<h3>Parsing कैसे काम करता है</h3>
<ul>
  <li>हर non-empty line को independent JSON के रूप में parse किया जाता है। Empty lines ignore होती हैं।</li>
  <li>अगर एक line parse में fail हो, तो वह red row के रूप में parse error के साथ दिखती है — बाकी file चलती रहती है। यह जान-बूझकर है: एक खराब line से बाकी हज़ार नहीं खोनी चाहिए।</li>
  <li>Columns पहले 100 valid rows की keys के union से auto-detect होती हैं, frequency के आधार पर sorted। Row 100 के बाद की rare keys data में रहती हैं पर अपना column नहीं मिलता।</li>
  <li>Nested objects और arrays अपने cell में clipped JSON snippet के रूप में दिखते हैं। Row पर click करें full pretty-printed JSON देखने के लिए।</li>
</ul>

<h3>आम गलतियाँ</h3>
<ul>
  <li><strong>JSON array ≠ JSONL।</strong> अगर आपकी file <code>[{...}, {...}, {...}]</code> जैसी commas के साथ दिखती है, वह JSON array है, JSONL नहीं। बाहरी brackets हटाएँ और commas को newlines से बदलें — या JSON viewer use करें।</li>
  <li><strong>Pretty-printed JSON JSONL नहीं है।</strong> अगर हर "object" कई lines में फैला है, parser टूटे fragments देखेगा। JSONL सख्ती से per line एक object है।</li>
  <li><strong>Mixed-shape rows चलती हैं पर गंदगी होती है।</strong> अगर आधी rows में <code>content</code> है और दूसरी आधी में <code>text</code>, आपको दो columns और बहुत खाली cells मिलेंगी। अक्सर यही आप चाहते हैं; कभी-कभी यह data-cleanup bug का संकेत होता है।</li>
  <li><strong>बहुत बड़ी files।</strong> सब memory में parse होता है; ~50–100 MB modern laptops पर ठीक है, उससे ज़्यादा पर slow। Multi-gigabyte logs के लिए streaming CLI tool इस्तेमाल करें।</li>
  <li><strong>Privacy।</strong> Files page कभी नहीं छोड़तीं। FileReader bytes JS memory में पढ़ता है; कोई network request नहीं।</li>
</ul>
""",
        "sk": """
<h2>Načo to slúži?</h2>
<p>JSONL (tiež NDJSON — JSON oddelený novými riadkami) je de facto formát pre tréningové dáta LLM, audit logy, eventové streamy a výstupy batch API. Jeden JSON objekt na riadok, žiadne obklopujúce pole. Príjemný na append, parsuje sa riadok po riadku, prežije čiastočnú korupciu. Čítať ho ako človek však bolí — chceš tabuľku, nie stenu zložených zátvoriek. Tento nástroj ti dá tú tabuľku v prehliadači. Nič sa nenahráva; súbor sa číta cez FileReader prehliadača a parsuje na mieste.</p>

<h3>Kedy to použiť</h3>
<ul>
  <li><strong>Kontrola tréningových dát LLM.</strong> Dataset na fine-tuning je typicky <code>{"messages": [...]}</code> na riadok. Prekrolovať korpus, skontrolovať vyváženosť tried, nájsť tú jednu outlier riadku, ktorá ti rúca trainer.</li>
  <li><strong>Čítanie batch výstupov OpenAI / Anthropic.</strong> Obaja vracajú batch výsledky ako JSONL — jeden riadok na request, so statusom a obsahom. Na prvý pohľad uvidíš, ktoré requesty prešli.</li>
  <li><strong>Review audit logov.</strong> Aplikačné logy v JSONL sú bežné; tento nástroj ich zobrazí ako tabuľku na ad-hoc analýzu bez opustenia prehliadača.</li>
  <li><strong>Sanity-check eventových streamov.</strong> Kafka / Kinesis dumpy často chodia ako JSONL. Rýchly stránkovaný pohľad, bez jq.</li>
  <li><strong>Konverzia JSONL → CSV.</strong> Použi export tlačidlo, keď chceš hodiť dáta do tabuľky.</li>
</ul>

<h3>Ako funguje parsovanie</h3>
<ul>
  <li>Každý neprázdny riadok sa parsuje nezávisle ako JSON. Prázdne riadky sa ignorujú.</li>
  <li>Ak riadok zlyhá, objaví sa ako červený riadok s chybou parsera — zvyšok súboru pokračuje. Zámerne: jeden zlý riadok nemá stáť ďalšiu tisícku.</li>
  <li>Stĺpce sa autodetekujú z únie kľúčov v prvých 100 platných riadkoch, zoradené podľa frekvencie. Vzácne kľúče po 100. riadku zostanú v dátach, ale nedostanú vlastný stĺpec.</li>
  <li>Vnorené objekty a polia sa v bunke zobrazia ako orezaný JSON snippet. Klikni na riadok, aby si videl plný pretty-printed JSON.</li>
</ul>

<h3>Časté pasce</h3>
<ul>
  <li><strong>JSON pole ≠ JSONL.</strong> Ak tvoj súbor vyzerá ako <code>[{...}, {...}, {...}]</code> s čiarkami medzi objektmi, je to JSON pole, nie JSONL. Odstráň vonkajšie zátvorky a nahraď čiarky novými riadkami — alebo použi JSON prehliadač.</li>
  <li><strong>Pretty-printed JSON nie je JSONL.</strong> Ak sa každý „objekt" tiahne cez viac riadkov, parser uvidí pokazené fragmenty. JSONL je striktne jeden objekt na riadok.</li>
  <li><strong>Zmiešané tvary riadkov sú v poriadku, ale neporiadne.</strong> Ak má polovica riadkov <code>content</code> a druhá <code>text</code>, dostaneš dva stĺpce a veľa prázdnych miest. Často to chceš; niekedy to ukazuje na bug v čistení dát.</li>
  <li><strong>Veľmi veľké súbory.</strong> Všetko sa parsuje v pamäti; ~50–100 MB je v pohode na moderných laptopoch, viac začne spomaľovať. Pre logy v gigabajtoch použi streamovací CLI nástroj.</li>
  <li><strong>Súkromie.</strong> Súbory nikdy neopustia stránku. FileReader číta bajty do JS pamäte; žiadny sieťový request.</li>
</ul>
""",
        "cs": """
<h2>K čemu to slouží?</h2>
<p>JSONL (také NDJSON — JSON oddělený novými řádky) je de facto formát pro trénovací data LLM, audit logy, eventové streamy a výstupy batch API. Jeden JSON objekt na řádek, žádné obklopující pole. Append-friendly, parsuje se řádek po řádku, přežije částečnou korupci. Číst ho jako člověk ale bolí — chceš tabulku, ne zeď složených závorek. Tenhle nástroj ti dá tu tabulku v prohlížeči. Nic se nenahrává; soubor se čte přes FileReader prohlížeče a parsuje na místě.</p>

<h3>Kdy to použít</h3>
<ul>
  <li><strong>Kontrola trénovacích dat LLM.</strong> Dataset na fine-tuning je typicky <code>{"messages": [...]}</code> na řádek. Projít korpus, ověřit vyváženost tříd, najít tu jednu outlier řádku, která ti shazuje trainer.</li>
  <li><strong>Čtení batch výstupů OpenAI / Anthropic.</strong> Oba vracejí batch výsledky jako JSONL — jeden řádek na request, se statusem a obsahem. Na první pohled uvidíš, které requesty prošly.</li>
  <li><strong>Review audit logů.</strong> Aplikační logy v JSONL jsou běžné; tento nástroj je zobrazí jako tabulku na ad-hoc analýzu bez opuštění prohlížeče.</li>
  <li><strong>Sanity-check eventových streamů.</strong> Kafka / Kinesis dumpy často chodí jako JSONL. Rychlý stránkovaný pohled, bez jq.</li>
  <li><strong>Konverze JSONL → CSV.</strong> Použij export tlačítko, když chceš nasypat data do tabulky.</li>
</ul>

<h3>Jak parsování funguje</h3>
<ul>
  <li>Každý neprázdný řádek se parsuje nezávisle jako JSON. Prázdné řádky se ignorují.</li>
  <li>Pokud řádek selže, objeví se jako červený řádek s chybou parseru — zbytek souboru pokračuje. Záměrně: jeden špatný řádek nemá stát další tisícovku.</li>
  <li>Sloupce se autodetekují z unie klíčů v prvních 100 platných řádcích, seřazené podle frekvence. Vzácné klíče po 100. řádku zůstanou v datech, ale nedostanou vlastní sloupec.</li>
  <li>Vnořené objekty a pole se v buňce zobrazí jako oříznutý JSON snippet. Klikni na řádek, abys viděl plný pretty-printed JSON.</li>
</ul>

<h3>Časté pasti</h3>
<ul>
  <li><strong>JSON pole ≠ JSONL.</strong> Pokud tvůj soubor vypadá jako <code>[{...}, {...}, {...}]</code> s čárkami mezi objekty, je to JSON pole, ne JSONL. Odstraň vnější závorky a nahraď čárky novými řádky — nebo použij JSON prohlížeč.</li>
  <li><strong>Pretty-printed JSON není JSONL.</strong> Pokud se každý „objekt" táhne přes víc řádků, parser uvidí rozbité fragmenty. JSONL je striktně jeden objekt na řádek.</li>
  <li><strong>Smíšené tvary řádků jsou v pořádku, ale nepořádné.</strong> Pokud má polovina řádků <code>content</code> a druhá <code>text</code>, dostaneš dva sloupce a hodně prázdných míst. Často to chceš; někdy to ukazuje na bug v čištění dat.</li>
  <li><strong>Velmi velké soubory.</strong> Vše se parsuje v paměti; ~50–100 MB je v pohodě na moderních laptopech, víc začne zpomalovat. Pro logy v gigabajtech použij streamovací CLI nástroj.</li>
  <li><strong>Soukromí.</strong> Soubory nikdy neopustí stránku. FileReader čte bajty do JS paměti; žádný síťový request.</li>
</ul>
""",
    },
    "related": ["json-formatter", "csv-to-json", "json-to-csv"],
    "howto": {"flow": "transform", "action": "format", "noun": "text"},
}
