TOOL = {
    "slug": "base64-to-image",
    "category": "encoding",
    "icon": "🔓",
    "tags": ["base64", "data-uri", "decode", "image", "convert"],
    "i18n": {
        "en": {
            "name": "Base64 to Image",
            "tagline": "Decode a Base64 image or data URI back into a file. Paste the string, preview, download. The inverse of our image-to-Base64 tool.",
            "description": "Free in-browser Base64 → image decoder. Paste a data: URI (or raw Base64 payload), see the rendered image, and download. Handles PNG, JPG, WebP, GIF, SVG. Useful when you've copied a data URI out of CSS or HTML and want the original file back.",
        },
        "de": {
            "name": "Base64 zu Bild",
            "tagline": "Ein Base64-Bild oder Data URI zurück in eine Datei dekodieren. String einfügen, Vorschau, herunterladen. Die Umkehr unseres Bild-zu-Base64-Tools.",
            "description": "Kostenloser In-Browser-Base64-→-Bild-Decoder. Füge eine data:-URI (oder Roh-Base64) ein, sieh das gerenderte Bild und lade es herunter. Unterstützt PNG, JPG, WebP, GIF, SVG. Praktisch, wenn du ein Data URI aus CSS oder HTML kopiert hast und die Originaldatei zurück möchtest.",
        },
        "es": {
            "name": "Base64 a imagen",
            "tagline": "Decodifica una imagen Base64 o data URI de vuelta a archivo. Pega la cadena, previsualiza, descarga. La inversa de nuestra herramienta imagen-a-Base64.",
            "description": "Decodificador gratuito Base64 → imagen en el navegador. Pega un data: URI (o el payload Base64 crudo), mira la imagen renderizada y descárgala. Soporta PNG, JPG, WebP, GIF, SVG. Útil cuando has copiado un data URI de CSS o HTML y quieres el archivo original de vuelta.",
        },
        "fr": {
            "name": "Base64 vers image",
            "tagline": "Décodez une image Base64 ou un data URI en fichier. Collez la chaîne, prévisualisez, téléchargez. L'inverse de notre outil image-vers-Base64.",
            "description": "Décodeur Base64 → image gratuit, dans le navigateur. Collez une data: URI (ou un payload Base64 brut), voyez l'image rendue et téléchargez-la. Gère PNG, JPG, WebP, GIF, SVG. Utile quand vous avez copié un data URI depuis du CSS ou du HTML et que vous voulez retrouver le fichier d'origine.",
        },
        "it": {
            "name": "Base64 a immagine",
            "tagline": "Decodifica un'immagine Base64 o data URI di nuovo in file. Incolla la stringa, anteprima, scarica. L'inverso del nostro tool immagine-a-Base64.",
            "description": "Decodificatore gratuito Base64 → immagine nel browser. Incolla un data: URI (o payload Base64 grezzo), vedi l'immagine renderizzata e scaricala. Gestisce PNG, JPG, WebP, GIF, SVG. Utile quando hai copiato un data URI da CSS o HTML e rivuoi il file originale.",
        },
        "pt": {
            "name": "Base64 para imagem",
            "tagline": "Decodifica uma imagem Base64 ou data URI de volta pra arquivo. Cola a string, prévia, baixa. O inverso da nossa ferramenta imagem-para-Base64.",
            "description": "Decoder grátis Base64 → imagem no browser. Cole uma data: URI (ou o payload Base64 cru), veja a imagem renderizada e baixe. Funciona com PNG, JPG, WebP, GIF, SVG. Útil quando você copiou um data URI de CSS ou HTML e quer o arquivo original de volta.",
        },
        "pl": {
            "name": "Base64 do obrazka",
            "tagline": "Zdekoduj obrazek Base64 albo data URI z powrotem do pliku. Wklej string, podgląd, pobierz. Odwrotność naszego narzędzia obrazek-do-Base64.",
            "description": "Darmowy in-browser dekoder Base64 → obrazek. Wklej data: URI (albo surowy Base64 payload), zobacz wyrenderowany obrazek i pobierz. Obsługuje PNG, JPG, WebP, GIF, SVG. Przydatne, gdy skopiowałeś data URI z CSS albo HTML i chcesz odzyskać oryginalny plik.",
        },
        "ja": {
            "name": "Base64 から画像へ",
            "tagline": "Base64 画像や data URI をファイルに戻すデコーダー。文字列を貼り付け、プレビューしてダウンロード。image-to-Base64 ツールの逆方向。",
            "description": "ブラウザ内で動く無料 Base64 → 画像デコーダー。data: URI（または生の Base64 ペイロード）を貼り付けると、レンダリングされた画像が見え、ダウンロードできます。PNG・JPG・WebP・GIF・SVG に対応。CSS や HTML からコピーした data URI を元のファイルに戻したい時に便利です。",
        },
        "nl": {
            "name": "Base64 naar afbeelding",
            "tagline": "Decodeer een Base64-afbeelding of data URI terug naar een bestand. Plak de string, bekijk preview, download. De inverse van onze afbeelding-naar-Base64 tool.",
            "description": "Gratis in-browser Base64 → afbeelding decoder. Plak een data: URI (of ruwe Base64-payload), zie de gerenderde afbeelding en download. Werkt voor PNG, JPG, WebP, GIF, SVG. Handig als je een data URI uit CSS of HTML hebt gekopieerd en het oorspronkelijke bestand terug wilt.",
        },
        "tr": {
            "name": "Base64'ten görsele",
            "tagline": "Bir Base64 görseli veya data URI'ı tekrar dosyaya çevir. Stringi yapıştır, önizle, indir. Görsel'den Base64'e aracımızın tersi.",
            "description": "Tamamen tarayıcı içi ücretsiz Base64 → görsel decoder. Bir data: URI (veya ham Base64 payload) yapıştır, render edilmiş görseli gör ve indir. PNG, JPG, WebP, GIF, SVG'yi destekler. CSS veya HTML'den bir data URI kopyaladıysan ve orijinal dosyayı geri istiyorsan kullanışlıdır.",
        },
        "id": {
            "name": "Base64 ke gambar",
            "tagline": "Dekode gambar Base64 atau data URI kembali menjadi file. Tempel string-nya, lihat preview, unduh. Kebalikan dari tool gambar-ke-Base64 kami.",
            "description": "Decoder Base64 → gambar gratis di-browser. Tempel sebuah data: URI (atau payload Base64 mentah), lihat gambar yang di-render, dan unduh. Mendukung PNG, JPG, WebP, GIF, SVG. Berguna saat kamu menyalin data URI dari CSS atau HTML dan ingin file aslinya kembali.",
        },
        "vi": {
            "name": "Base64 sang ảnh",
            "tagline": "Giải mã ảnh Base64 hoặc data URI thành file. Dán chuỗi, xem trước, tải về. Ngược lại với công cụ ảnh-sang-Base64 của chúng tôi.",
            "description": "Trình giải mã Base64 → ảnh miễn phí, hoàn toàn trong trình duyệt. Dán data: URI (hoặc payload Base64 thô), xem ảnh được render và tải về. Hỗ trợ PNG, JPG, WebP, GIF, SVG. Hữu ích khi bạn đã sao chép một data URI từ CSS hoặc HTML và muốn lấy lại file gốc.",
        },
        "hi": {
            "name": "Base64 to Image",
            "tagline": "एक Base64 image या data URI को वापस file में decode करें। String paste करें, preview देखें, download करें। हमारे image-to-Base64 tool का उल्टा।",
            "description": "मुफ़्त in-browser Base64 → image decoder। एक data: URI (या raw Base64 payload) paste करें, rendered image देखें, और download करें। PNG, JPG, WebP, GIF, SVG को handle करता है। उपयोगी जब आपने CSS या HTML से एक data URI copy किया है और original file वापस चाहते हैं।",
        },
        "sk": {
            "name": "Base64 na obrázok",
            "tagline": "Dekóduj Base64 obrázok alebo data URI späť do súboru. Vlož reťazec, pozri náhľad, stiahni. Opak nášho nástroja obrázok-na-Base64.",
            "description": "Bezplatný in-browser Base64 → obrázok dekodér. Vlož data: URI (alebo surový Base64 payload), pozri vykreslený obrázok a stiahni si ho. Zvláda PNG, JPG, WebP, GIF, SVG. Hodí sa, keď si skopíroval data URI z CSS alebo HTML a chceš späť originálny súbor.",
        },
        "cs": {
            "name": "Base64 na obrázek",
            "tagline": "Dekóduj Base64 obrázek nebo data URI zpět do souboru. Vlož řetězec, podívej se na náhled, stáhni. Opak našeho nástroje obrázek-na-Base64.",
            "description": "Zdarma in-browser Base64 → obrázek dekodér. Vlož data: URI (nebo surový Base64 payload), podívej se na vykreslený obrázek a stáhni si ho. Zvládá PNG, JPG, WebP, GIF, SVG. Hodí se, když jsi zkopíroval data URI z CSS nebo HTML a chceš zpět originální soubor.",
        },
    },
    "body": """
<div class="tool-card">
  <label>Paste data: URI or raw Base64</label>
  <textarea id="b2i-in" spellcheck="false" oninput="b2iRun()" placeholder="data:image/png;base64,iVBORw0KGgo… or just the Base64 payload" style="width:100%;min-height:140px;font-family:var(--font-mono);font-size:0.82rem"></textarea>
  <div class="meta" id="b2i-err" style="margin-top:0.5rem;color:var(--err)"></div>
  <div style="font-size:0.78rem;color:var(--text-muted);margin-top:0.4rem">🔒 Everything happens in your browser — no upload, no server, no tracking.</div>
</div>
<div class="tool-card">
  <label>Preview</label>
  <div id="b2i-prev" style="text-align:center;padding:1rem;background:var(--code-bg);border:1px solid var(--border);border-radius:6px;min-height:80px"><span style="color:var(--text-muted)">No image yet.</span></div>
  <div class="meta" id="b2i-meta" style="margin-top:0.5rem"></div>
  <div class="button-row" style="margin-top:0.6rem"><a id="b2i-dl" class="" download style="display:none">{LBL_DOWNLOAD}</a><button id="b2i-dl-btn" onclick="b2iDownload()" disabled>{LBL_DOWNLOAD}</button></div>
</div>
""",
    "script": """
<script>
let b2iDataUrl = null;
let b2iExt = 'bin';

function b2iShowError(msg){
  document.getElementById('b2i-err').textContent = msg;
  document.getElementById('b2i-prev').innerHTML = '<span style="color:var(--text-muted)">No image yet.</span>';
  document.getElementById('b2i-meta').textContent = '';
  document.getElementById('b2i-dl-btn').disabled = true;
}

function b2iRun(){
  let s = document.getElementById('b2i-in').value.trim();
  document.getElementById('b2i-err').textContent = '';
  if (!s){
    document.getElementById('b2i-prev').innerHTML = '<span style="color:var(--text-muted)">No image yet.</span>';
    document.getElementById('b2i-meta').textContent = '';
    document.getElementById('b2i-dl-btn').disabled = true;
    return;
  }
  let mime;
  if (s.startsWith('data:')){
    const m = s.match(/^data:([^;,]+)(?:;base64)?,(.+)$/);
    if (!m){ b2iShowError('✗ Invalid data URI — expected data:<mime>;base64,<payload>'); return; }
    mime = m[1];
    s = m[2];
  } else {
    s = s.replace(/\\s+/g, '');
    try {
      const head = atob(s.slice(0, Math.min(s.length, 32)));
      if (head.startsWith('\\x89PNG')) mime = 'image/png';
      else if (head.startsWith('\\xff\\xd8\\xff')) mime = 'image/jpeg';
      else if (head.startsWith('GIF8')) mime = 'image/gif';
      else if (head.startsWith('RIFF') && head.includes('WEBP')) mime = 'image/webp';
      else if (head.trim().startsWith('<svg') || head.startsWith('<?xml')) mime = 'image/svg+xml';
      else mime = 'application/octet-stream';
    } catch (e){ b2iShowError('✗ Not valid Base64 — check for stray characters or whitespace.'); return; }
  }
  let bytes;
  try {
    const bin = atob(s);
    bytes = bin.length;
  } catch (e){ b2iShowError('✗ Base64 decoding failed — payload appears malformed.'); return; }
  if (mime === 'application/octet-stream'){
    b2iShowError('⚠ Could not identify image format from the first bytes — payload may not be an image.');
  }
  b2iDataUrl = 'data:' + mime + ';base64,' + s;
  b2iExt = (mime.split('/')[1] || 'bin').replace('+xml', '').replace('jpeg', 'jpg');
  const img = new Image();
  img.onload = () => {
    document.getElementById('b2i-prev').innerHTML = '<img src="' + b2iDataUrl + '" style="max-width:100%;max-height:320px;border-radius:4px">';
    document.getElementById('b2i-meta').innerHTML = '<strong>' + mime + '</strong> · ' + img.naturalWidth + ' × ' + img.naturalHeight + ' px · ' + (bytes/1024).toFixed(1) + ' KB decoded';
    document.getElementById('b2i-dl-btn').disabled = false;
  };
  img.onerror = () => {
    // SVG and unknown formats may not load as <img> data URI in all cases; show meta anyway
    if (mime === 'image/svg+xml'){
      document.getElementById('b2i-prev').innerHTML = '<img src="' + b2iDataUrl + '" style="max-width:100%;max-height:320px;border-radius:4px">';
      document.getElementById('b2i-meta').innerHTML = '<strong>' + mime + '</strong> · ' + (bytes/1024).toFixed(1) + ' KB decoded';
      document.getElementById('b2i-dl-btn').disabled = false;
    } else {
      b2iShowError('✗ Decoded successfully but browser refused to render — file may be corrupted or not a supported image.');
    }
  };
  img.src = b2iDataUrl;
}

function b2iDownload(){
  if (!b2iDataUrl) return;
  const a = document.createElement('a');
  a.href = b2iDataUrl;
  a.download = 'decoded.' + b2iExt;
  a.click();
}
</script>
""",
    "help": {
        "en": """
<h2>What is this for?</h2>
<p>This tool is the inverse of the image-to-Base64 encoder: it takes a <code>data:</code> URI (or a raw Base64 payload) and decodes it back into a viewable, downloadable image. It auto-detects the format from the MIME type in the URI, or — if you pass just the raw Base64 — sniffs the first few bytes (PNG signature, JPEG SOI marker, GIF8 header, RIFF…WEBP, SVG markup) to identify the image type. Everything runs in the browser via <code>atob</code> and an <code>&lt;img&gt;</code> data URI.</p>

<h3>When to use it</h3>
<ul>
  <li>Recovering the original image when you've only got a data URI copied out of CSS, HTML, or a Markdown file.</li>
  <li>Inspecting a Base64 string from an API response or JWT-like token to check what it actually contains.</li>
  <li>Debugging a Base64 image that won't render somewhere — round-trip it through this tool to confirm the payload is valid.</li>
  <li>Pulling an embedded asset out of a single-file HTML demo or self-contained email.</li>
</ul>

<h3>Common gotchas</h3>
<ul>
  <li><strong>Both forms are accepted.</strong> A full <code>data:image/png;base64,iVBORw0…</code> or just the raw <code>iVBORw0…</code> payload — but if you pass raw Base64 of a format we can't sniff, the download will be tagged as <code>.bin</code>.</li>
  <li><strong>URL-safe Base64 isn't quite the same.</strong> Standard Base64 uses <code>+ /</code>; URL-safe Base64 uses <code>- _</code>. JWT payloads use URL-safe — replace the chars before pasting here.</li>
  <li><strong>Whitespace inside the payload is OK.</strong> The decoder strips line breaks and spaces, so multi-line strings work.</li>
  <li><strong>Browsers won't render a corrupted image</strong> — if the preview is blank but the decode succeeded, the bytes are valid Base64 but probably not a real image (or the format isn't supported).</li>
</ul>
""",
        "de": """
<h2>Wofür ist das?</h2>
<p>Dieses Tool ist die Umkehr des Bild-zu-Base64-Encoders: Es nimmt eine <code>data:</code>-URI (oder Roh-Base64) und dekodiert sie zurück zu einem anzeigbaren, herunterladbaren Bild. Das Format wird automatisch aus dem MIME-Typ der URI erkannt — oder, wenn du nur Roh-Base64 lieferst, anhand der ersten Bytes (PNG-Signatur, JPEG-SOI-Marker, GIF8-Header, RIFF…WEBP, SVG-Markup) bestimmt. Alles läuft im Browser über <code>atob</code> und ein <code>&lt;img&gt;</code> mit Data URI.</p>

<h3>Wann verwenden</h3>
<ul>
  <li>Das Originalbild zurückgewinnen, wenn du nur ein Data URI aus CSS, HTML oder einer Markdown-Datei kopiert hast.</li>
  <li>Einen Base64-String aus einer API-Antwort oder einem JWT-ähnlichen Token inspizieren.</li>
  <li>Ein Base64-Bild debuggen, das woanders nicht rendert — durch dieses Tool drehen, um den Payload zu bestätigen.</li>
  <li>Ein eingebettetes Asset aus einer Single-File-HTML-Demo oder Self-Contained-E-Mail extrahieren.</li>
</ul>

<h3>Häufige Stolpersteine</h3>
<ul>
  <li><strong>Beide Formen werden akzeptiert.</strong> Komplettes <code>data:image/png;base64,iVBORw0…</code> oder nur Roh-<code>iVBORw0…</code> — aber bei nicht erkennbarem Format wird der Download als <code>.bin</code> getaggt.</li>
  <li><strong>URL-safe Base64 ist nicht ganz dasselbe.</strong> Standard-Base64 nutzt <code>+ /</code>, URL-safe nutzt <code>- _</code>. JWT-Payloads sind URL-safe — vor dem Einfügen umwandeln.</li>
  <li><strong>Whitespace im Payload ist okay.</strong> Der Decoder entfernt Zeilenumbrüche und Leerzeichen, mehrzeilige Strings funktionieren.</li>
  <li><strong>Browser rendern kein kaputtes Bild.</strong> Wenn die Vorschau leer bleibt, aber das Decoding klappt, ist der Payload zwar gültiges Base64, aber wahrscheinlich kein echtes Bild (oder ein nicht unterstütztes Format).</li>
</ul>
""",
        "es": """
<h2>¿Para qué sirve?</h2>
<p>Esta herramienta es la inversa del codificador imagen-a-Base64: toma un <code>data:</code> URI (o un payload Base64 crudo) y lo decodifica de vuelta a una imagen visualizable y descargable. Detecta automáticamente el formato desde el tipo MIME del URI, o — si pasas solo el Base64 crudo — olfatea los primeros bytes (firma PNG, marcador SOI de JPEG, cabecera GIF8, RIFF…WEBP, markup SVG) para identificar el tipo. Todo corre en el navegador vía <code>atob</code> y un <code>&lt;img&gt;</code> con data URI.</p>

<h3>Cuándo usarlo</h3>
<ul>
  <li>Recuperar la imagen original cuando solo tienes un data URI copiado de CSS, HTML o Markdown.</li>
  <li>Inspeccionar un string Base64 de una respuesta de API o un token similar a JWT.</li>
  <li>Depurar una imagen Base64 que no renderiza en algún sitio — pásala por aquí para confirmar que el payload es válido.</li>
  <li>Sacar un asset embebido de una demo HTML de un solo archivo o un email autocontenido.</li>
</ul>

<h3>Errores comunes</h3>
<ul>
  <li><strong>Aceptamos ambas formas.</strong> Un <code>data:image/png;base64,iVBORw0…</code> completo o solo el payload <code>iVBORw0…</code> — pero si pasas Base64 crudo de un formato que no podemos olfatear, la descarga se etiquetará como <code>.bin</code>.</li>
  <li><strong>El Base64 URL-safe no es exactamente lo mismo.</strong> El Base64 estándar usa <code>+ /</code>; el URL-safe usa <code>- _</code>. Los payloads JWT son URL-safe — sustitúyelos antes de pegar aquí.</li>
  <li><strong>Los espacios dentro del payload son OK.</strong> El decodificador quita saltos de línea y espacios, así que cadenas multilínea funcionan.</li>
  <li><strong>Los navegadores no renderizan una imagen corrupta.</strong> Si la previsualización sale en blanco pero el decode tuvo éxito, los bytes son Base64 válido pero probablemente no una imagen real (o el formato no se soporta).</li>
</ul>
""",
        "fr": """
<h2>À quoi ça sert ?</h2>
<p>Cet outil est l'inverse de l'encodeur image-vers-Base64 : il prend une <code>data:</code> URI (ou un payload Base64 brut) et la décode en image visualisable et téléchargeable. Il détecte automatiquement le format depuis le type MIME de l'URI, ou — si vous fournissez juste du Base64 brut — renifle les premiers octets (signature PNG, marqueur SOI JPEG, en-tête GIF8, RIFF…WEBP, markup SVG) pour identifier le type. Tout tourne dans le navigateur via <code>atob</code> et un <code>&lt;img&gt;</code> data URI.</p>

<h3>Quand l'utiliser</h3>
<ul>
  <li>Récupérer l'image originale quand vous n'avez qu'un data URI copié depuis du CSS, HTML ou Markdown.</li>
  <li>Inspecter une chaîne Base64 venant d'une réponse d'API ou d'un token façon JWT.</li>
  <li>Déboguer une image Base64 qui ne s'affiche pas ailleurs — faites-la transiter par cet outil pour confirmer le payload.</li>
  <li>Extraire un asset embarqué d'une démo HTML mono-fichier ou d'un email autonome.</li>
</ul>

<h3>Pièges courants</h3>
<ul>
  <li><strong>Les deux formes sont acceptées.</strong> Un <code>data:image/png;base64,iVBORw0…</code> complet ou juste le payload <code>iVBORw0…</code> — mais si vous passez du Base64 brut d'un format qu'on ne peut pas renifler, le téléchargement sera étiqueté <code>.bin</code>.</li>
  <li><strong>Le Base64 URL-safe n'est pas pareil.</strong> Le Base64 standard utilise <code>+ /</code> ; le URL-safe utilise <code>- _</code>. Les payloads JWT sont URL-safe — remplacez les caractères avant de coller ici.</li>
  <li><strong>Les espaces dans le payload sont OK.</strong> Le décodeur retire les retours à la ligne et espaces, donc les chaînes multi-lignes fonctionnent.</li>
  <li><strong>Les navigateurs n'affichent pas une image corrompue.</strong> Si la prévisualisation est vide mais que le décode a réussi, les octets sont du Base64 valide mais probablement pas une vraie image (ou format non supporté).</li>
</ul>
""",
        "it": """
<h2>A cosa serve?</h2>
<p>Questo strumento è l'inverso dell'encoder immagine-a-Base64: prende una <code>data:</code> URI (o un payload Base64 grezzo) e la decodifica di nuovo in un'immagine visualizzabile e scaricabile. Rileva automaticamente il formato dal tipo MIME nella URI, o — se passi solo Base64 grezzo — fiuta i primi byte (firma PNG, marker SOI di JPEG, header GIF8, RIFF…WEBP, markup SVG) per identificare il tipo. Tutto gira nel browser via <code>atob</code> e un <code>&lt;img&gt;</code> con data URI.</p>

<h3>Quando usarlo</h3>
<ul>
  <li>Recuperare l'immagine originale quando hai solo un data URI copiato da CSS, HTML o Markdown.</li>
  <li>Ispezionare una stringa Base64 da una risposta API o un token tipo JWT.</li>
  <li>Debuggare un'immagine Base64 che non si renderizza altrove — passala da qui per confermare il payload.</li>
  <li>Estrarre un asset embedded da una demo HTML single-file o un'email autocontenuta.</li>
</ul>

<h3>Errori comuni</h3>
<ul>
  <li><strong>Entrambe le forme sono accettate.</strong> Un <code>data:image/png;base64,iVBORw0…</code> completo o solo il payload <code>iVBORw0…</code> — ma se passi Base64 grezzo di un formato che non possiamo fiutare, il download sarà taggato <code>.bin</code>.</li>
  <li><strong>Il Base64 URL-safe non è proprio uguale.</strong> Il Base64 standard usa <code>+ /</code>; quello URL-safe usa <code>- _</code>. I payload JWT sono URL-safe — sostituiscili prima di incollare qui.</li>
  <li><strong>Spazi nel payload vanno bene.</strong> Il decodificatore rimuove a capo e spazi, quindi le stringhe multilinea funzionano.</li>
  <li><strong>I browser non renderizzano un'immagine corrotta.</strong> Se l'anteprima è vuota ma il decode è riuscito, i byte sono Base64 valido ma probabilmente non un'immagine reale (o il formato non è supportato).</li>
</ul>
""",
        "pt": """
<h2>Pra que serve?</h2>
<p>Esta ferramenta é o inverso do encoder imagem-pra-Base64: recebe um <code>data:</code> URI (ou um payload Base64 cru) e decodifica de volta numa imagem visualizável e baixável. Detecta automaticamente o formato pelo tipo MIME na URI, ou — se você passar só o Base64 cru — fareja os primeiros bytes (assinatura PNG, marcador SOI do JPEG, header GIF8, RIFF…WEBP, markup SVG) pra identificar o tipo. Tudo roda no browser via <code>atob</code> e um <code>&lt;img&gt;</code> com data URI.</p>

<h3>Quando usar</h3>
<ul>
  <li>Recuperar a imagem original quando você só tem um data URI copiado de CSS, HTML ou Markdown.</li>
  <li>Inspecionar uma string Base64 vinda de uma resposta de API ou token tipo JWT.</li>
  <li>Debugar uma imagem Base64 que não renderiza em algum lugar — passe por aqui pra confirmar o payload.</li>
  <li>Extrair um asset embutido de uma demo HTML em arquivo único ou e-mail autocontido.</li>
</ul>

<h3>Cuidados comuns</h3>
<ul>
  <li><strong>Aceitamos as duas formas.</strong> Um <code>data:image/png;base64,iVBORw0…</code> completo ou só o payload <code>iVBORw0…</code> — mas se você passar Base64 cru de um formato que não conseguimos farejar, o download sairá como <code>.bin</code>.</li>
  <li><strong>Base64 URL-safe não é igual.</strong> Base64 padrão usa <code>+ /</code>; o URL-safe usa <code>- _</code>. Payloads JWT são URL-safe — troque os caracteres antes de colar aqui.</li>
  <li><strong>Espaços no payload são OK.</strong> O decoder tira quebras de linha e espaços, então strings multilinha funcionam.</li>
  <li><strong>Browsers não renderizam imagem corrompida.</strong> Se a prévia fica em branco mas o decode deu certo, os bytes são Base64 válidos mas provavelmente não uma imagem real (ou formato não suportado).</li>
</ul>
""",
        "pl": """
<h2>Do czego to służy?</h2>
<p>To narzędzie jest odwrotnością enkodera obrazek-do-Base64: bierze <code>data:</code> URI (albo surowy Base64 payload) i dekoduje go z powrotem do dającego się wyświetlić i pobrać obrazka. Automatycznie wykrywa format z MIME w URI, albo — jeśli podajesz tylko surowy Base64 — wąchaje pierwsze bajty (sygnatura PNG, marker SOI w JPEG, nagłówek GIF8, RIFF…WEBP, markup SVG), żeby zidentyfikować typ. Wszystko działa w przeglądarce przez <code>atob</code> i <code>&lt;img&gt;</code> z data URI.</p>

<h3>Kiedy tego użyć</h3>
<ul>
  <li>Odzyskiwanie oryginalnego obrazka, gdy masz tylko data URI skopiowany z CSS, HTML albo Markdown.</li>
  <li>Inspekcja stringa Base64 z odpowiedzi API albo tokena JWT-podobnego.</li>
  <li>Debugowanie obrazka Base64, który gdzieś się nie renderuje — przepuść go tu, żeby potwierdzić payload.</li>
  <li>Wyciąganie osadzonego assetu z dema HTML w jednym pliku albo samowystarczalnego maila.</li>
</ul>

<h3>Częste pułapki</h3>
<ul>
  <li><strong>Akceptujemy obie formy.</strong> Pełny <code>data:image/png;base64,iVBORw0…</code> albo tylko payload <code>iVBORw0…</code> — ale jeśli podasz surowy Base64 formatu, którego nie umiemy wyniuchać, pobranie zostanie otagowane jako <code>.bin</code>.</li>
  <li><strong>URL-safe Base64 to nie to samo.</strong> Standardowy Base64 używa <code>+ /</code>; URL-safe używa <code>- _</code>. Payloady JWT są URL-safe — podmień znaki przed wklejeniem.</li>
  <li><strong>Białe znaki wewnątrz payloadu są OK.</strong> Dekoder usuwa łamania linii i spacje, więc wieloliniowe stringi działają.</li>
  <li><strong>Przeglądarki nie wyrenderują uszkodzonego obrazka.</strong> Jeśli podgląd jest pusty, a dekodowanie się udało, bajty są poprawnym Base64, ale prawdopodobnie nie są prawdziwym obrazkiem (albo format jest nieobsługiwany).</li>
</ul>
""",
        "ja": """
<h2>用途</h2>
<p>これは image-to-Base64 エンコーダーの逆方向です。<code>data:</code> URI（または生の Base64 ペイロード）を受け取り、表示・ダウンロード可能な画像へデコードします。URI 内の MIME タイプから形式を自動判定し、生の Base64 だけが渡された場合は先頭バイト（PNG シグネチャ、JPEG SOI マーカー、GIF8 ヘッダ、RIFF…WEBP、SVG マークアップ）を覗いて種別を判定します。すべてブラウザ内で <code>atob</code> と <code>&lt;img&gt;</code> の data URI を使って動きます。</p>

<h3>使うべきタイミング</h3>
<ul>
  <li>CSS・HTML・Markdown から data URI だけがコピーされた状態で、元画像を復元したい時。</li>
  <li>API レスポンスや JWT 風トークンに含まれる Base64 文字列の中身を確認したい時。</li>
  <li>どこかでレンダリングされない Base64 画像のデバッグ — この場で往復させてペイロードの正当性を確認。</li>
  <li>シングルファイル HTML デモや自己完結型メールから埋め込みアセットを取り出す時。</li>
</ul>

<h3>よくある注意点</h3>
<ul>
  <li><strong>どちらの形式も受け付けます。</strong> 完全な <code>data:image/png;base64,iVBORw0…</code> でも、生の <code>iVBORw0…</code> ペイロードでも OK ですが、判定できない形式を生で渡すとダウンロードは <code>.bin</code> 扱いになります。</li>
  <li><strong>URL-safe Base64 は別物です。</strong> 標準 Base64 は <code>+ /</code>、URL-safe は <code>- _</code> を使います。JWT ペイロードは URL-safe なので、貼り付け前に置換してください。</li>
  <li><strong>ペイロード内の空白は無視されます。</strong> 改行やスペースを取り除いてからデコードするので、複数行の文字列も動作します。</li>
  <li><strong>壊れた画像はブラウザがレンダリングしません。</strong> プレビューが空でもデコード自体が成功していれば、バイト列は Base64 として有効ですが、実際の画像でない（または未サポート形式である）可能性が高いです。</li>
</ul>
""",
        "nl": """
<h2>Waarvoor is dit?</h2>
<p>Deze tool is de inverse van de afbeelding-naar-Base64-encoder: hij neemt een <code>data:</code> URI (of een rauwe Base64-payload) en decodeert die terug naar een te bekijken en te downloaden afbeelding. Het formaat wordt automatisch gedetecteerd uit het MIME-type in de URI, of — als je alleen rauwe Base64 geeft — uit de eerste bytes (PNG-signatuur, JPEG SOI-marker, GIF8-header, RIFF…WEBP, SVG-markup). Alles draait in de browser via <code>atob</code> en een <code>&lt;img&gt;</code> met data URI.</p>

<h3>Wanneer gebruiken</h3>
<ul>
  <li>De originele afbeelding herstellen als je alleen een data URI hebt uit CSS, HTML of Markdown.</li>
  <li>Een Base64-string uit een API-response of JWT-achtige token inspecteren.</li>
  <li>Een Base64-afbeelding debuggen die ergens niet rendert — laat hem hier doorheen lopen om te bevestigen dat de payload valide is.</li>
  <li>Een embedded asset uit een single-file HTML demo of zelfstandige email trekken.</li>
</ul>

<h3>Veelvoorkomende valkuilen</h3>
<ul>
  <li><strong>Beide vormen worden geaccepteerd.</strong> Een volledige <code>data:image/png;base64,iVBORw0…</code> of alleen de rauwe <code>iVBORw0…</code> payload — maar als je rauwe Base64 geeft van een formaat dat we niet kunnen sniffen, wordt de download getagd als <code>.bin</code>.</li>
  <li><strong>URL-safe Base64 is niet hetzelfde.</strong> Standaard Base64 gebruikt <code>+ /</code>; URL-safe gebruikt <code>- _</code>. JWT-payloads zijn URL-safe — vervang de chars voor je hier plakt.</li>
  <li><strong>Whitespace binnen de payload is OK.</strong> De decoder strips line breaks en spaties, dus multi-line strings werken.</li>
  <li><strong>Browsers renderen een corrupte afbeelding niet.</strong> Als de preview leeg blijft maar de decode slaagde, zijn de bytes valide Base64 maar waarschijnlijk geen echte afbeelding (of formaat niet ondersteund).</li>
</ul>
""",
        "tr": """
<h2>Bu ne işe yarar?</h2>
<p>Bu araç görsel'den Base64'e enkoder'in tersidir: bir <code>data:</code> URI (veya ham Base64 payload) alır ve onu görüntülenebilir, indirilebilir bir görsele geri çevirir. URI'daki MIME tipinden formatı otomatik tespit eder, ya da — sadece ham Base64 verirsen — ilk byte'lara bakarak (PNG imzası, JPEG SOI markeri, GIF8 başlığı, RIFF…WEBP, SVG markup) türü tanımlar. Her şey tarayıcıda <code>atob</code> ve bir <code>&lt;img&gt;</code> data URI üzerinden çalışır.</p>

<h3>Ne zaman kullanılır</h3>
<ul>
  <li>CSS, HTML veya Markdown'dan kopyalanmış bir data URI'den orijinal görseli geri kazanmak.</li>
  <li>Bir API yanıtından veya JWT benzeri token'dan gelen Base64 string'i incelemek.</li>
  <li>Başka bir yerde render olmayan Base64 görseli debug etmek — payload'ın geçerli olduğunu doğrulamak için buradan geçir.</li>
  <li>Tek dosyalı bir HTML demosundan veya kendi kendine yeten e-postadan gömülü bir varlığı çekmek.</li>
</ul>

<h3>Sık yapılan hatalar</h3>
<ul>
  <li><strong>Her iki form da kabul edilir.</strong> Tam <code>data:image/png;base64,iVBORw0…</code> veya sadece ham <code>iVBORw0…</code> payload — ama koklayamadığımız bir formatın ham Base64'ünü verirsen indirme <code>.bin</code> olarak etiketlenir.</li>
  <li><strong>URL-safe Base64 tam olarak aynı değil.</strong> Standart Base64 <code>+ /</code> kullanır; URL-safe ise <code>- _</code>. JWT payload'ları URL-safe'dir — buraya yapıştırmadan önce karakterleri değiştir.</li>
  <li><strong>Payload içindeki boşluklar sorun değil.</strong> Decoder satır sonlarını ve boşlukları temizler, çok satırlı stringler çalışır.</li>
  <li><strong>Tarayıcılar bozuk bir görseli render etmez.</strong> Önizleme boşsa ama decode başarılı olduysa, byte'lar geçerli Base64 ama muhtemelen gerçek bir görsel değil (veya format desteklenmiyor).</li>
</ul>
""",
        "id": """
<h2>Untuk apa ini?</h2>
<p>Tool ini adalah kebalikan dari encoder gambar-ke-Base64: ia mengambil sebuah <code>data:</code> URI (atau payload Base64 mentah) dan mendekodenya kembali menjadi gambar yang dapat dilihat dan diunduh. Format dideteksi otomatis dari tipe MIME di URI, atau — kalau kamu hanya memberikan Base64 mentah — mengendus byte-byte pertama (tanda tangan PNG, marker SOI JPEG, header GIF8, RIFF…WEBP, markup SVG) untuk mengidentifikasi tipenya. Semua berjalan di browser via <code>atob</code> dan sebuah data URI <code>&lt;img&gt;</code>.</p>

<h3>Kapan digunakan</h3>
<ul>
  <li>Mengembalikan gambar asli ketika kamu hanya punya data URI yang disalin dari CSS, HTML, atau Markdown.</li>
  <li>Memeriksa string Base64 dari respons API atau token mirip JWT untuk melihat isinya.</li>
  <li>Debugging gambar Base64 yang tidak ter-render di tempat lain — putar lewat tool ini untuk memastikan payload-nya valid.</li>
  <li>Mengeluarkan aset yang ditempelkan dari demo HTML satu-file atau email mandiri.</li>
</ul>

<h3>Kesalahan umum</h3>
<ul>
  <li><strong>Kedua bentuk diterima.</strong> <code>data:image/png;base64,iVBORw0…</code> lengkap atau hanya payload <code>iVBORw0…</code> — tapi kalau kamu memberi Base64 mentah dari format yang tidak bisa kami endus, unduhan akan ditandai sebagai <code>.bin</code>.</li>
  <li><strong>Base64 URL-safe tidak sama persis.</strong> Base64 standar memakai <code>+ /</code>; yang URL-safe memakai <code>- _</code>. Payload JWT itu URL-safe — ganti karakter-karakter itu sebelum menempel di sini.</li>
  <li><strong>Whitespace di dalam payload tidak masalah.</strong> Decoder membuang line break dan spasi, jadi string multi-baris bisa.</li>
  <li><strong>Browser tidak akan me-render gambar rusak.</strong> Kalau preview kosong tapi decode sukses, byte-nya valid Base64 tapi mungkin bukan gambar nyata (atau formatnya tidak didukung).</li>
</ul>
""",
        "vi": """
<h2>Công cụ này để làm gì?</h2>
<p>Công cụ này là chiều ngược của bộ mã hóa ảnh-sang-Base64: nó nhận một <code>data:</code> URI (hoặc payload Base64 thô) và giải mã trở lại thành một ảnh có thể xem và tải về. Định dạng được tự động nhận từ kiểu MIME trong URI, hoặc — nếu bạn chỉ đưa Base64 thô — nó "ngửi" vài byte đầu (chữ ký PNG, marker SOI của JPEG, header GIF8, RIFF…WEBP, markup SVG) để xác định loại. Mọi thứ chạy trong trình duyệt thông qua <code>atob</code> và một <code>&lt;img&gt;</code> data URI.</p>

<h3>Khi nào nên dùng</h3>
<ul>
  <li>Khôi phục ảnh gốc khi bạn chỉ có một data URI sao chép từ CSS, HTML hoặc Markdown.</li>
  <li>Kiểm tra một chuỗi Base64 từ phản hồi API hoặc token kiểu JWT để xem nó thực sự chứa gì.</li>
  <li>Debug một ảnh Base64 không hiển thị ở đâu đó — đưa qua công cụ này để xác nhận payload hợp lệ.</li>
  <li>Trích xuất một tài nguyên nhúng từ một demo HTML một-file hoặc email tự chứa.</li>
</ul>

<h3>Lưu ý thường gặp</h3>
<ul>
  <li><strong>Cả hai dạng đều được chấp nhận.</strong> <code>data:image/png;base64,iVBORw0…</code> đầy đủ hoặc chỉ payload <code>iVBORw0…</code> — nhưng nếu bạn đưa Base64 thô của một định dạng chúng tôi không thể ngửi được, tải về sẽ được gắn nhãn <code>.bin</code>.</li>
  <li><strong>Base64 URL-safe không giống hệt.</strong> Base64 chuẩn dùng <code>+ /</code>; URL-safe dùng <code>- _</code>. Payload JWT là URL-safe — đổi ký tự trước khi dán vào đây.</li>
  <li><strong>Khoảng trắng trong payload OK.</strong> Decoder loại bỏ ngắt dòng và khoảng trắng, nên chuỗi nhiều dòng vẫn hoạt động.</li>
  <li><strong>Trình duyệt sẽ không render ảnh hỏng.</strong> Nếu preview trống nhưng decode thành công, byte vẫn là Base64 hợp lệ nhưng có lẽ không phải ảnh thật (hoặc định dạng không được hỗ trợ).</li>
</ul>
""",
        "hi": """
<h2>यह किसके लिए है?</h2>
<p>यह tool image-to-Base64 encoder का उल्टा है: यह एक <code>data:</code> URI (या raw Base64 payload) लेता है और इसे viewable, downloadable image में वापस decode करता है। यह URI के MIME type से format auto-detect करता है, या — अगर आप सिर्फ़ raw Base64 pass करते हैं — पहले कुछ bytes (PNG signature, JPEG SOI marker, GIF8 header, RIFF…WEBP, SVG markup) sniff करके image type पहचानता है। सब कुछ browser में <code>atob</code> और एक <code>&lt;img&gt;</code> data URI के माध्यम से चलता है।</p>

<h3>कब इस्तेमाल करें</h3>
<ul>
  <li>Original image recover करना जब आपके पास सिर्फ़ CSS, HTML, या Markdown file से copy किया एक data URI है।</li>
  <li>API response या JWT जैसे token से एक Base64 string inspect करना यह जाँचने के लिए कि उसमें वास्तव में क्या है।</li>
  <li>एक Base64 image debug करना जो कहीं render नहीं हो रही — payload valid है यह confirm करने के लिए इस tool से round-trip करें।</li>
  <li>एक single-file HTML demo या self-contained email से embedded asset निकालना।</li>
</ul>

<h3>आम गलतियाँ</h3>
<ul>
  <li><strong>दोनों forms accepted हैं।</strong> पूरा <code>data:image/png;base64,iVBORw0…</code> या सिर्फ़ raw <code>iVBORw0…</code> payload — पर अगर आप ऐसे format का raw Base64 pass करते हैं जिसे हम sniff नहीं कर सकते, तो download <code>.bin</code> के रूप में tag होगा।</li>
  <li><strong>URL-safe Base64 बिल्कुल same नहीं है।</strong> Standard Base64 <code>+ /</code> use करता है; URL-safe <code>- _</code> use करता है। JWT payloads URL-safe होते हैं — यहाँ paste करने से पहले characters replace करें।</li>
  <li><strong>Payload के अंदर whitespace OK है।</strong> Decoder line breaks और spaces strip करता है, इसलिए multi-line strings काम करते हैं।</li>
  <li><strong>Browsers एक corrupted image render नहीं करेंगे।</strong> अगर preview blank है पर decode succeeded, तो bytes valid Base64 हैं पर शायद real image नहीं (या format supported नहीं)।</li>
</ul>
""",
        "sk": """
<h2>Načo to slúži?</h2>
<p>Tento nástroj je opakom kodéra obrázok-na-Base64: vezme <code>data:</code> URI (alebo surový Base64 payload) a dekóduje ho späť na zobraziteľný a stiahnuteľný obrázok. Formát automaticky deteguje z MIME typu v URI, alebo — ak podáš len surový Base64 — pričuchne k prvým bajtom (PNG signatúra, JPEG SOI marker, GIF8 hlavička, RIFF…WEBP, SVG markup), aby identifikoval typ. Všetko beží v prehliadači cez <code>atob</code> a <code>&lt;img&gt;</code> s data URI.</p>

<h3>Kedy to použiť</h3>
<ul>
  <li>Obnovenie originálneho obrázka, keď máš len data URI skopírovaný z CSS, HTML alebo Markdown.</li>
  <li>Skontrolovať Base64 reťazec z API odpovede alebo JWT-podobného tokenu, aby si videl, čo skutočne obsahuje.</li>
  <li>Debugovanie Base64 obrázka, ktorý sa niekde nerendruje — prejdi ho cez tento nástroj, aby si potvrdil, že payload je platný.</li>
  <li>Vytiahnutie vloženého assetu z HTML dema v jednom súbore alebo samostatne stojaceho e-mailu.</li>
</ul>

<h3>Časté chyby</h3>
<ul>
  <li><strong>Obe formy sú akceptované.</strong> Celé <code>data:image/png;base64,iVBORw0…</code> alebo len surový <code>iVBORw0…</code> payload — ale ak podáš surový Base64 vo formáte, ktorý nevieme rozpoznať, stiahnutie bude označené ako <code>.bin</code>.</li>
  <li><strong>URL-safe Base64 nie je úplne to isté.</strong> Štandardný Base64 používa <code>+ /</code>; URL-safe používa <code>- _</code>. JWT payloady sú URL-safe — pred vložením tu znaky nahraď.</li>
  <li><strong>Biele znaky vnútri payloadu sú v poriadku.</strong> Dekodér odstráni zalomenia a medzery, takže viacriadkové reťazce fungujú.</li>
  <li><strong>Prehliadače nevykreslia poškodený obrázok.</strong> Ak je náhľad prázdny, ale dekódovanie prebehlo úspešne, bajty sú platný Base64, ale pravdepodobne to nie je skutočný obrázok (alebo formát nie je podporovaný).</li>
</ul>
""",
        "cs": """
<h2>K čemu to slouží?</h2>
<p>Tento nástroj je opakem kodéru obrázek-na-Base64: vezme <code>data:</code> URI (nebo surový Base64 payload) a dekóduje ho zpět na zobrazitelný a stažitelný obrázek. Formát automaticky detekuje z MIME typu v URI, nebo — pokud podáš jen surový Base64 — přičichne k prvním bajtům (PNG signatura, JPEG SOI marker, GIF8 hlavička, RIFF…WEBP, SVG markup), aby identifikoval typ. Vše běží v prohlížeči přes <code>atob</code> a <code>&lt;img&gt;</code> s data URI.</p>

<h3>Kdy to použít</h3>
<ul>
  <li>Obnovení originálního obrázku, když máš jen data URI zkopírované z CSS, HTML nebo Markdown.</li>
  <li>Zkontrolovat Base64 řetězec z API odpovědi nebo JWT-podobného tokenu, abys viděl, co skutečně obsahuje.</li>
  <li>Debugování Base64 obrázku, který se někde nerenderuje — projeď ho tímto nástrojem, abys potvrdil, že payload je platný.</li>
  <li>Vytažení vloženého assetu z HTML dema v jednom souboru nebo samostatně stojícího e-mailu.</li>
</ul>

<h3>Časté chyby</h3>
<ul>
  <li><strong>Obě formy jsou akceptovány.</strong> Celé <code>data:image/png;base64,iVBORw0…</code> nebo jen surový <code>iVBORw0…</code> payload — ale pokud podáš surový Base64 ve formátu, který neumíme rozpoznat, stažení bude označeno jako <code>.bin</code>.</li>
  <li><strong>URL-safe Base64 není úplně totéž.</strong> Standardní Base64 používá <code>+ /</code>; URL-safe používá <code>- _</code>. JWT payloady jsou URL-safe — před vložením tu znaky nahraď.</li>
  <li><strong>Bílé znaky uvnitř payloadu jsou v pořádku.</strong> Dekodér odstraní zalomení a mezery, takže víceřádkové řetězce fungují.</li>
  <li><strong>Prohlížeče nevykreslí poškozený obrázek.</strong> Pokud je náhled prázdný, ale dekódování proběhlo úspěšně, bajty jsou platný Base64, ale pravděpodobně to není skutečný obrázek (nebo formát není podporován).</li>
</ul>
""",
    },
    "related": ["image-to-base64", "base64-encoder", "qr-code-generator"],
    "howto": {"flow": "transform", "action": "decode", "noun": "image"},
}
